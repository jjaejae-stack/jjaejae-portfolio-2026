# 2026 포트폴리오 — 진행 상황

최종 업데이트: 2026-08-18

## 세션 요약 — 갤러리 클릭-롤링+확대버튼 재설계, Neo QLED 깨진 이미지, 빌더 흰색 테마 리디자인(레퍼런스 사이트) + 좌측 리스트 넘버링, 미리보기 데스크탑 시뮬레이션, 3탭 가로폭 리사이즈 (2026-08-16~18)

**1) 프로젝트 상세 갤러리 — 클릭하면 롤링, 우상단 "+"로 확대**
"이미지를 클릭하면 좌우로 롤링되게 하고, 우상단에 작은 + 버튼을 눌러야 확대되게" 요청. 처음엔 존(zone) 클릭(좌/우 22%=이전/다음, 가운데=확대) 방식으로 만들었다가, 사용자가 명확히 방향을 바꿔서(이미지 전체 클릭=롤링, "+" 버튼 전용으로 확대) 전면 재작업.
- `mediaFrameInner()`가 사진에 `<button class="gallery-zoom-btn">+</button>`를 같이 렌더링(멀티모드 그리드에서도 프레임별로 정확히 앵커되도록 `.gallery-frame{position:relative}` 추가).
- 드래그/스와이프(Pointer Events)는 그대로 유지, 드래그 후 발생하는 클릭이 또 롤링을 트리거하지 않도록 `dataset.dragged` 플래그로 구분.
- **라이트박스 열려있을 때 Escape 누르면 홈(WORK/PLAY)으로 나가버리던 버그** — 1차 수정(조건 추가)으로는 안 고쳐짐: `document`에 등록된 두 keydown 리스너(라이트박스 닫기 / Escape→홈)가 등록 순서대로 실행되는데, 첫 번째가 라이트박스를 이미 닫아버린 뒤 두 번째가 "라이트박스 열려있나" 검사하면 항상 닫힌 뒤라 조건을 통과해 홈까지 나가던 게 진짜 원인. `stopImmediatePropagation()`으로 같은 이벤트에 대한 나머지 리스너 실행 자체를 막아서 해결.
- **모아보기(mode-multi) 갤러리 하단 빈 공간**: `.gallery{min-height:280px}` 고정값이 row 방향 mode-multi에도 그대로 적용되어, 실제 콘텐츠 높이가 그보다 작으면 아래 검은 여백이 남던 버그(3장 이미지 갤러리 스크린샷으로 리포트). `.gallery.mode-multi{height:auto; max-height:none; min-height:0;}`로 방향 상관없이 전부 해소.

**2) Neo QLED 8K 프로젝트 — jjaejae.com에서만 이미지 깨져 보이던 버그**
빌더 미리보기에선 멀쩡한데 실제 사이트에서만 깨짐 → `PROJECTS` 배열의 gif 9개(Private Training/Gaming Hub/Gaming Lounge 블록)가 `"Neo QLED 8K Digital/"` 폴더 프리픽스 없이 파일명만 적혀있던 게 원인 — 전부 프리픽스 추가해서 수정.

**3) 빌더 흰색 테마 전면 리디자인 — symbolstudio.pl 레퍼런스**
사용자가 링크(symbolstudio.pl)와 화면 녹화 영상(SERVICES 리스트 hover 시 대시→점+텍스트 색 변화 인터랙션)을 레퍼런스로 제공. 흰색(mono) 테마는 원래 "네오브루탈리즘"(검은 테두리+6px 검은 그림자) 스타일이었는데, 이를 얇은 회색 구분선 기반 미니멀 스타일로 교체. `data-theme="dark"` 전용 override 블록과 동일한 패턴(`body:not([data-theme])`로 selector 그룹 묶어서 border/shadow 걷어내기)을 재사용해서 blue/dark 테마는 완전히 그대로 유지.
- **좌측 프로젝트 리스트**: 카드+그림자 → 얇은 구분선 리스트 행으로 전환, `001/002/...` 넘버링 추가(전 테마 공통). hover/active 시 강조색은 `--accent`(mono 테마에서 `--fg`와 같은 무채색이라 안 보임) 대신 프로젝트 고유 색(`--pc`, 이미 존재하던 색상 점 데이터 재사용)으로 텍스트가 물들게 구현.
- 버튼(pill 모양)/카드/모달/입력창(언더라인 스타일)/탭 등도 같은 override 블록에서 일괄 처리.

**4) 빌더 좌/중/우 탭 가로폭 드래그 리사이즈 + 사이드바 반응형 컬럼 확장**
- 두 경계선에 드래그 핸들 추가 — `#app`의 grid-template-columns를 퍼센트(`minmax(px, %)`)로 제어해서 창 크기가 바뀌어도 비율 유지, 드래그 결과는 localStorage에 저장돼 새로고침 후에도 유지.
- **사이드바를 넓게 드래그하면** 좌측 리스트가 번호/제목만 있는 압축형에서 번호·제목·카테고리·연도가 각 칼럼으로 펼쳐지는 테이블형으로 전환 — `#sidebar`에 `container-type:inline-size` 걸어서 뷰포트가 아니라 **패널 자체의 실제 폭**(리사이즈로 넓어진 만큼)을 기준으로 `@container` 쿼리 적용(약 460px 이상일 때 전환). 흰색 테마 전용.

**5) 빌더 PC 미리보기 — 진짜 데스크탑 브라우저처럼 보이게 (세 번의 시행착오)**
"미리보기 탭 폭이 좁아서 사이트 CSS(vw/clamp 기반 타이틀 박스 크기)가 실제와 다르게 계산돼 텍스트가 박스 밖으로 삐져나온다"는 근본 문제 + "그럼에도 미리보기 패널 크기에 맞게 축소되어 보여야 한다"는 요구를 동시에 만족시켜야 했음.
- 1차: iframe을 미리보기 컬럼 폭에 맞춰 그대로 렌더링 → 좁은 폭 때문에 텍스트 오버플로우 재현됨.
- 2차: 바깥(빌더)에서 iframe을 1440px 고정폭으로 렌더링한 뒤 `transform:scale`/`zoom`으로 축소 + 별도 wrapper로 화면상 크기를 수동 계산 → 브라우저마다(특히 사파리) 계산이 어긋나 확대되어 보이는 등 불안정.
- **최종(3차, 안정화)**: 스케일링을 iframe **바깥이 아니라 안**에서 처리 — srcdoc 문서 자체에 `#pf-desktop-sim`(1440px 고정폭) 래퍼와 스케일링 스크립트를 심어서, `window.innerWidth/1440` 비율로 `transform:scale` 적용 후 `getBoundingClientRect()`로 실제 렌더링 크기를 읽어와 shell 높이에 반영. 브라우저가 계산한 실제 크기를 그대로 읽어오기 때문에 바깥에서 따로 계산해서 어긋날 여지가 구조적으로 없음. 바깥쪽 iframe은 그냥 패널 크기만큼 채우기만 하면 됨(단순 CSS, 별도 로직 불필요). 모바일 모드는 이 시뮬레이션 대상 아님(390px 자체가 이미 실제 모바일 폭).
- 부수 효과로 **미리보기 스크롤 위치가 매 수정마다 최상단으로 리셋되던 문제**도 같이 해결 — srcdoc 교체 전 iframe의 현재 스크롤 위치를 저장해뒀다가 로드 후 복원(단, 같은 프로젝트/INFO를 계속 편집 중일 때만 — 다른 프로젝트로 전환하거나 INFO↔프로젝트를 오갈 땐 의미 없는 위치라 최상단으로 리셋).
- **타이틀 박스 텍스트 오버플로우 자체도 사이트 CSS 레벨에서 안전장치 추가**: `.p-colorbox h1`에 `overflow-wrap:break-word; max-width:100%` — 어떤 극단적인 폰트/박스 크기 조합에서도 텍스트가 박스 밖으로 안 삐져나오게(`index.html`).

**6) 그 외 빌더 버그 수정**
- **소셜 아이콘(유튜브/비메오/인스타)·SUIT 폰트가 미리보기에서만 안 보이던 버그**: `icons/*.png`, `fonts/*.woff2` 같은 상대경로가 원인 — 미리보기 iframe은 `srcdoc`이라 실제 페이지 주소가 없어 상대경로가 안 풀림(jjaejae.com은 실제 주소가 있어서 문제없이 동작). 빌더 자신의 실제 위치 기준으로 절대 URL을 만드는 `previewAssetURL()` 헬퍼를 추가해서 해결.
- **메인 히어로 "직접 업로드" 파일명과 "다듬기"/"제거" 버튼이 겹치던 버그**: 공용 `.fname` 클래스에 말줄임(`text-overflow:ellipsis`) 처리가 빠져있어서 긴 파일명이 2줄로 줄바꿈되며 옆 버튼과 겹쳤음 — 기본 규칙에 추가해서 이 클래스를 쓰는 다른 곳(타이틀 폰트 이름 등)까지 한 번에 해결.

**참고**: 이 세션도 다른 곳에서 동시에 열려있던 builder.html 세션의 Publish(`git add -A`)에 위 수정사항 전부가 묻어서 커밋됨 — `c52394f`/`d4a57ae`("Publish: NOSCARNA") 두 커밋에 포함, `origin/main`까지 push 완료 확인([[project_2026_portfolio_publish_git_add_a]]). 커밋 메시지가 실제 작업 내용을 설명하진 않지만 데이터 손실 없음, 히스토리 재정리는 이미 push된 상태라 보류(사용자 확인).

**세션 마무리 (2026-08-18)** — `git status` clean, `origin/main`과 동기화 확인. 브라우저별 IndexedDB가 분리되어 있어(크롬/사파리) 프로젝트 목록이 브라우저마다 다르게 보이는 건 정상(버그 아님) — "다른 기기로 내보내기/가져오기" 기능으로 동기화 가능.

## 세션 요약 — 인트로 인사말 마퀴 속도 조정 (2026-08-16)

**요청**: 메인 화면 인트로(5개 언어 인사말이 좌우로 흐르는 마퀴)의 글자 이동 속도가 첨부한 참고 영상(Cargo 데모 사이트 화면 녹화)의 속도 정도로 느리게 보이길 원함.

- 참고 영상은 화면 녹화라 "몇 초"가 아니라 실제 화면 대비 얼마나 움직이는지가 중요 — ffmpeg로 프레임을 뽑고 각 프레임의 특정 줄(가로 스트립)을 그레이스케일 신호로 만들어 프레임 간 cross-correlation으로 이동 픽셀을 측정. 4개 시점(1~4초)에서 거의 정확히 초당 140px(프레임 폭 2500px 기준, 즉 뷰포트 폭의 5.6%/초)로 일정하게 측정됨.
- 실제 사이트 속도도 같은 방식으로 측정: Playwright로 `.lang-track`의 `transform: translateX(...)` 값을 2초 간격으로 읽어 뷰포트 폭 대비 속도 계산 → 초당 뷰포트 폭의 21.8% (참고 영상보다 약 3.9배 빠름).
- `index.html`의 `GREETINGS` 배열(각 언어 줄의 `duration`, 원래 76/100/84/94/78초)을 전부 3.9배로 늘려 참고 영상과 거의 정확히 일치(재측정 시 5.59%/초, 목표 5.60%/초)시킴. `builder.html`은 이 인트로 마퀴 화면 자체를 렌더링하지 않아 수정 불필요.
- 이후 사용자가 "그것보다 아주 살짝만 빠르게" 요청 → duration을 약 10% 줄여서(266/351/295/330/274초) 재측정 시 6.22%/초(약 11% 빠름)로 확인, `index.html`만 커밋(`76bc26b`)해서 push 완료 → jjaejae.com에 반영.
- **재사용 가능한 기법**: 참고 영상/GIF 속도를 코드 애니메이션과 맞춰야 할 때, 초 단위로 눈대중하지 말고 (1) 참고 영상 프레임을 ffmpeg로 뽑아 가로 스트립 신호의 프레임 간 cross-correlation으로 px/s 측정 → 프레임 폭으로 나눠 "뷰포트 폭 대비 %/초"로 정규화, (2) 실제 사이트도 Playwright로 `getComputedStyle().transform`을 시간차를 두고 읽어 같은 단위로 측정, (3) 두 값의 비율만큼 `duration`(또는 속도에 반비례하는 다른 값)을 스케일. 뷰포트 폭 대비 비율로 정규화하면 녹화본의 실제 해상도/DPR과 무관하게 정확히 비교 가능.

## 세션 요약 — 유튜브 Shorts 자동재생 오류 수정 + 이미지 백필 + Samsung TV push 재발 + 영상 100MB 캡 + 영상/사진 간격·순서 배치 + 빈 필드 오역 방지 (2026-08-16)

**1. 빌더에서 유튜브 자동재생 넣으면 오류나던 버그 수정**
- 원인: 블록에 영상 링크가 유튜브/비메오인지 판별하는 정규식이 `watch?v=`, `youtu.be/`만 인식하고 **Shorts 링크**(`youtube.com/shorts/...`)는 놓침. Shorts 링크를 넣으면 일반 동영상 파일처럼 취급돼서 서버가 ffmpeg로 다운로드 시도 → 실패 → "동영상 처리 실패" 오류.
- `builder.html` 2곳(판별 정규식, 렌더링 정규식) + `index.html` 1곳(렌더링 정규식)을 shorts/embed/live 형식까지 인식하도록 수정.
- 참고로 `https://youtu.be/xxx?si=...` 같은 공유 링크는 정상 동작 확인 — 다만 로컬 `file://`로 미리보기 열면 유튜브가 임베드를 차단해서 "오류 153"이 뜨는 건 원래 유튜브 정책(실제 배포된 jjaejae.com에서는 문제없음), 코드 버그 아님.

**2. 레거시 대용량 이미지 백필**
- `save_project_image`/`crop_image`에 이미 있던 2000px 리사이즈 로직이 생기기 전에 올라간 옛날 이미지들(예: `띵크어띵/TAT-B_White.jpg` 5400px/15MB)은 그대로 남아있던 걸 발견, 20개 이미지를 2000px 캡으로 재인코딩.
- quality를 92→100으로 올렸는데, **WEBP는 quality 100에서 오히려 원본보다 커지는 역효과** 발견 (근접 무손실이라 압축이 거의 안 됨) → WEBP만 quality 90으로 재조정, JPG는 100 유지. `server.py`도 이 기준으로 3곳 수정.

**3. Samsung TV Sports Leadership Campaign push 막힘 — 배스킨라빈스와 똑같은 패턴 재발**
- 60초 영상이 110MB로 최적화됐다가 84MB로 재최적화됐는데, 첫 110MB 버전이 로컬 커밋 히스토리에 남아 push가 막힘. `git reset --soft origin/main` + 재커밋으로 해결 (`[[project_2026_portfolio_git_push_100mb_history]]` 참고).

**4. 영상 최적화가 100MB 넘지 않도록 재발 방지**
- `server.py`의 `_run_ffmpeg`가 기존엔 crf 18 고정이라 길거나 움직임 많은 영상은 100MB를 넘을 수 있었음(3번 문제의 근본 원인). 이제 **1차 인코딩 후 95MB 넘으면 자동으로 2-pass 비트레이트 인코딩으로 재인코딩**해서 무조건 95MB 이하로 떨어지도록 수정. 실제 ffmpeg로 테스트 완료.

**5. 영상+사진 섞인 블록에 간격/배치 방식 기능 추가**
- 기존엔 영상과 사진이 위아래로 딱 붙어서 렌더링됐는데(gap 0), `block.mixedGap`/`block.mixedDirection` 필드로 간격(0~120px)과 방향(위·아래/좌·우)을 조절할 수 있게 됨.
- 배치 방식을 3가지 중 선택하는 라디오로 확장: **고정**(기존, 영상 항상 위/아래) / **순서대로**(새로 추가 — `block.mediaSequential`, 사진/영상 목록 순서 그대로 배치하고 연속된 사진끼리는 자동으로 모아보기 하나로 묶임) / **완전 통합**(기존 `mediaLayoutCombined`).
- 중간에 CSS 버그 발견/수정: 좌우 배치에 모바일 자동 축소 미디어쿼리를 넣었었는데, 빌더의 PC 미리보기 iframe 자체가 종종 760px보다 좁게 렌더링돼서 PC 모드에서도 잘못 걸림 → 다른 레이아웃 모드들도 모바일 자동 축소가 없길래 일관성 맞춰서 규칙 자체를 제거.

**6. Publish 시 빈 필드 자동번역 방지**
- `fillMissingKoreanTranslations`가 영문/한글 둘 다 실질적으로 비어있는 블록은 원래도 스킵하게 돼있었는데, 공백 문자(스페이스/줄바꿈만 있는 경우)를 "내용 있음"으로 잘못 취급하는 버그가 있었음 → `hasText`/`hasAnyText` 헬퍼로 trim 기반 판정하도록 수정, claude가 빈 입력에 엉뚱한 안내문을 지어내는 경우 방지.

**세션 마무리 (2026-08-16)**: 이번 세션 코드 변경은 대부분 사용자의 동시 Publish 액션(`git add -A`)에 쓸려서 커밋/푸시됨 — `[[project_2026_portfolio_publish_git_add_a]]` 참고.

## 세션 요약 — 배스킨라빈스 push 막힘 근본 수정 + 영상별 자동재생 온오프 + 영상 썸네일 백필 + 타이틀 글자 크기 슬라이더 (2026-08-13)

**1) 배스킨라빈스 프로젝트가 계속 push 안 되던 문제 — 근본 원인/수정**
지금 폴더에는 100MB 넘는 파일이 없는데도 `git push`가 계속 GitHub의 100MB 파일 제한에 막혔음 — 원인은 폴더가 아니라 **커밋 히스토리**: 예전에 올렸다가 나중에 지우거나 더 작은 버전으로 교체한 대용량 GIF/MP4(최대 191MB)가, 아직 origin에 안 올라간 로컬 커밋 9개 안에 그대로 남아있었음(git push는 그 파일이 지금 있는지가 아니라 커밋 히스토리 전체를 봄). 이 9개 커밋이 전부 로컬에만 있고 origin엔 없었으므로(다른 사람과 공유된 적 없음) 안전하게, 사용자 승인 받고 `git reset --soft origin/main` → 지금 폴더 상태 기준으로 커밋 1개(`8edfa75`)로 재작성 → push 성공. 이후 이 방식으로 계속 정상 push됨.

**2) 영상별 자동재생 온오프 설정 (빌더 요청 기능)**
"어떤 영상은 자동재생, 어떤 건 직접 눌러야 재생되게" 요청 — 갤러리의 전체 영상 자동재생(스크롤 들어오면 무음 자동재생) 기능은 이전 세션에 이미 만들어져 있었는데, 이번엔 그걸 영상별로 켜고 끌 수 있게 확장.
- **데이터 모델**: `block.noAutoplay` — 자동재생을 끈 영상들의 relPath 배열(옵션, 없으면 전부 자동재생 = 기존 동작 그대로, 하위호환).
- **빌더 UI**: 사진/영상 목록에서 영상 행마다 "자동재생" 체크박스 추가(기본 체크). 끄면 그 영상은 스크롤로 들어와도 재생 안 되고, 방문자가 직접 눌러야 재생되며 그때는 소리도 정상 나옴(자동재생 영상은 브라우저 정책상 무음 필수지만, 수동재생은 그 제약이 없음).
- **적용 범위**: `galleryFrameInner`/`mediaFrameInner`부터 `galleryModeHTML`/`galleryBlockHTML`/`galleryHTML`까지 전부 `noAutoplaySet`을 관통해서 전달하도록 수정(index.html·builder.html 양쪽, 프리뷰 iframe 포함).
- **버그 발견/수정**: 사진+영상이 섞였는데 별도 "동영상 레이아웃"을 안 정한 블록은 영상이 페이지 넘김 없는 고정 스택(`.gallery-video-stack`)으로 렌더되는데, 이 영역이 자동재생 감시(`IntersectionObserver`) 등록 루프에서 통째로 빠져 있어서 **체크박스를 켜도 실제로는 자동재생이 전혀 안 되던 버그**를 발견 → `.gallery-video-stack video:not([data-no-autoplay])`도 관찰 대상에 추가해서 수정.

**3) 영상 재생 전 검은 화면 → 첫 프레임 썸네일 백필**
"첫 프레임 됐던 썸네일이 보였으면 좋겠다"는 요청은 이전 세션에 `server.py`가 새로 인코딩/다듬기 하는 영상에 한해 이미 구현돼 있었는데, **그 이전에 이미 올라가 있던 영상들은 썸네일이 아예 생성된 적이 없어서** 여전히 검은 화면이었음(BASKIN ROBBINS "Film" 영상 등). 사이트 전체를 스캔해서 썸네일 없는 영상 **24개**(BASKIN ROBBINS, Galaxy Tab S9, Neo QLED 8K, Um burger&wings, NOSCARNA, Samsung TV History Film)를 찾아 `ffmpeg`로 첫 프레임 썸네일(`-poster.jpg`)을 일괄 생성.

**4) 빌더 타이틀 타이포그래피 탭 — "글자 크기" 슬라이더 추가**
"폰트를 바꿀 때마다 타이틀 크기가 체감상 달라진다"는 피드백 — 폰트마다 실제 글자가 차지하는 시각적 크기(x-height 등)가 달라서 생기는 자연스러운 현상이라, CSS `font-size` 값 자체를 자동으로 보정하기보다는 **수동으로 보정할 수 있는 슬라이더**를 추가하는 방향으로 처리(자간/행간/박스 크기 슬라이더와 같은 패턴).
- `titleFontScale`(기본 1 = 100%, 범위 50~180%) 필드 신설. CSS `--title-font-scale` 커스텀 속성으로 `.p-colorbox h1`의 `font-size: calc(clamp(...) * var(--title-font-scale,1))`에 곱해지는 방식이라, PC/모바일 반응형 `clamp()` 값은 그대로 유지하면서 배율만 얹음.
- 기본값(100%)일 땐 내보내기 코드에 아예 안 찍혀서 기존 프로젝트 전부 영향 없음.
- 빌더의 폰트 미리보기 박스도 슬라이더에 맞춰 실시간으로 커지고 작아짐.

**세션 마무리 (2026-08-13)** — `git status` clean, `origin/main`과 동기화 확인. 이번에도 사용자가 빌더에서 여러 프로젝트를 Publish해서(`git add -A` 방식) 이 세션 코드 수정 전부가 그 Publish 커밋들에 자연스럽게 묻어 들어감([[project_2026_portfolio_publish_git_add_a]]) — 데이터 손실 없이 전부 반영·push 완료 확인.

## 세션 요약 — 프로젝트 EN/한글 토글 기능 신설 + OG 메타태그 + 인트로 힌트 재배치 (2026-08-12, 계속)

**1) 링크 공유 시 미리보기(OG 메타태그) 추가**
카톡 등에 링크 공유하면 제목/설명 없이 이미지도 아무거나(히어로 동영상 검은 첫 프레임) 잡혀 나오던 문제 — `og:title`/`og:description`/`og:image`/`twitter:card` 등 메타태그를 `<head>`에 추가. 썸네일 이미지는 사용자가 고른 `info-bg.jpg`로 지정. 카카오는 링크별로 미리보기를 캐시하므로, 반영 후 확인하려면 [카카오 디버거](https://developers.kakao.com/tool/debugger/sharing)로 강제 재요청 필요.

**2) 프로젝트 상세페이지 EN/KOR 토글 신설 (제일 큰 작업)**
"사이트가 영어로만 되어있는데 한글로도 볼 수 있게 하고 싶다, 빌더에서 블록마다 한글 탭 따로, 한글 비어있으면 Publish 때 자동번역" 요청 — 사용자와 스코프 확정(토글은 프로젝트 상세페이지 안에만·첫 블록 위, 자동번역은 Publish 시점에 미리 번역해서 static 사이트에 박아넣는 방식, 프로젝트 title/tag/client/agency/credits는 이번엔 번역 대상 아님 — 블록의 label/heading/paragraphs만).
- **데이터 모델**: block에 `labelKo`/`headingKo`/`paragraphsKo` 3개 필드 추가(옵션, 없으면 그 블록은 그냥 영어로 남음).
- **서버(`server.py`)**: 기존 `/translate`(`TRANSLATE_SCHEMA`)에 `blocks[].label` 필드 추가(하위호환 유지, 기존 "KR → EN"/"EN → KR" 덮어쓰기 번역 버튼과는 완전히 별개 경로).
- **빌더(`builder.html`)**: 블록 카드마다 EN/KR 탭 신설(`blockKoTabOpen` Set으로 재렌더링 후에도 탭 상태 유지) — KR 탭엔 labelKo/headingKo/paragraphsKo 입력 + "이 블록 영→한 자동번역해서 채우기" 버튼(`runTranslateBlockToKorean`, 영문 원본은 절대 안 건드림). **PUBLISH 시점**엔 `fillMissingKoreanTranslations()`가 영문은 있는데 한글이 빈 블록만 모아 자동 번역 후 채워 넣고 IndexedDB에도 저장(발행 한 번으로 끝나는 게 아니라 계속 남음) → 그다음 `exportProjectCode()`가 채워진 필드를 조건부로 export.
- **사이트(`index.html`)**: 한글 콘텐츠가 있는 프로젝트에서만(`hasKorean` 체크) 첫 블록 위에 ENG/KOR 토글 pill(`.lang-toggle`, 갤러리 넘기기 pill과 같은 톤) 표시. 토글은 `scrollRoot`를 통째로 다시 안 그리고(스크롤 위치·재생 중인 히어로 동영상 안 끊기게) 블록별 라벨/헤딩/본문 텍스트만 갈아끼움(`blockText()`/`applyProjectLang()`). 세션 동안만 유지되는 선택(새로고침하면 항상 영어부터, prev/next 넘어갈 땐 유지).
- **빌더 프리뷰에도 동일 토글 미러링**: 프리뷰 iframe 안의 토글 클릭을 `postMessage`로 부모(빌더 메인 페이지)에 알려서 `previewLang` 바꾸고 다시 그림 — 발행 전에 미리 한글 버전 확인 가능.
- **검증**: 서버/빌더/사이트 문법 확인, 발행된 16개 프로젝트 전부 열어 에러 없음 확인, 합성 데이터로 빌더 입력→저장→재렌더 유지, Publish 시 필요한 블록만 정확히 번역 요청 나가고 결과가 export 코드에 반영, 사이트에서 토글 시 히어로 동영상 DOM 노드 안 바뀌는지까지 확인. 실제 로컬 claude CLI 번역 품질 자체는 샌드박스에 네트워크가 없어 확인 못 함 — 사용자가 직접 로컬에서 확인.
- **마이그레이션**: 자동 채우기는 그 프로젝트를 다시 Publish해야 발동 — 사용자가 이후 16개 프로젝트를 순서대로 전부 다시 Publish해서, 현재 **16개 중 13개 프로젝트(71블록 중 49블록)에 한글 콘텐츠 반영 완료**.

**⚠️ 알려진 버그와 수정 — 빈 블록에 번역 결과가 잘못 채워지던 문제 (2건 발생, 완전히 해결됐는지는 다음 세션에 한 번 더 스캔 권장)**
- 헤딩/본문이 아예 없는 블록(사진/영상만 있는 블록)도 자동번역 대상에 포함시켰더니, claude가 "번역할 내용이 없습니다..." 같은 안내문을 지어내거나 라벨을 그대로 heading에 복사해서 그게 실제 사이트에 올라가는 버그 발견(스크린샷으로 리포트받음).
- **수정**: `fillMissingKoreanTranslations`/`runTranslateBlockToKorean` 둘 다, **원본(영문) 필드가 비어있으면 번역 응답이 뭐가 오든 그 필드는 절대 안 채우도록** 가드 추가 — 이후로는 재발 안 함.
- **이미 발행된 데이터 정리**: Galaxy Tab S9/Neo QLED 8K/NOSCARNA 3개 프로젝트 7블록 정리 → 이후 icecream-homerun/yunjac/toss-fraud-2020/toss-securities-qper9/brandi/eider/samsung-tv-history-film **20블록에서 또 발견**되어 추가 정리(스크립트로 "영문 필드가 비었는데 그 Ko 필드만 있는" 블록을 전체 스캔해서 해당 줄만 제거).
- **재발 원인으로 추정되는 것**: 코드 수정을 마친 뒤에도 사용자가 **이미 열어둔 빌더 탭**으로 계속 여러 프로젝트를 연달아 Publish해서, 그 탭이 새로고침 전까지는 수정 전 로직을 계속 실행했을 가능성이 큼(파일은 고쳐져도 이미 로드된 페이지의 JS는 안 바뀜). **다음 세션에서 빌더로 뭔가 하기 전엔 꼭 새로고침(F5)부터 하는 걸 권장**, 그리고 한 번 더 위와 같은 스캔(아래 스니펫)을 돌려서 남은 게 없는지 확인하면 좋음:
  ```js
  // index.html이 있는 폴더에서: node -e "..." 로 PROJECTS를 eval해서
  // block.heading이 비었는데 block.headingKo가 있거나, paragraphs가 비었는데
  // paragraphsKo가 있는 블록을 찾아 출력 — 이 세션에서 두 번 이렇게 정리함.
  ```

**3) 인트로 화면 하단 힌트(스크롤/색 바꾸기) 재배치**
기존엔 "Scroll" 힌트가 하단, "Click for color" 힌트가 상단으로 나뉘어 있어서 둘 다 있다는 게 잘 안 보인다는 피드백 → 하단에 나란히 배치(`.intro-cues` 래퍼). 스크롤 힌트는 점 대신 아래를 가리키는 SVG 쉐브런 아이콘으로 바꾸고 바운스 애니메이션을 줘서 "스크롤하라"는 의도가 더 직관적으로 보이게 함.

**세션 마무리 (2026-08-12)** — `git status` clean, `origin/main`과 동기화 확인(`556e473`까지 push 완료). 사용자가 빌더에서 여러 프로젝트를 연달아 Publish해서(`git add -A` 방식이라) 이 세션 코드 수정 대부분이 그 Publish 커밋들에 나눠 묻어 들어감([[project_2026_portfolio_publish_git_add_a]]) — 데이터 손실 없이 전부 반영됨. 로컬 서버(`python3 server.py`, 8420)는 계속 켜져 있을 수 있음.

## 세션 요약 — 좌상단 로고 대비 자동전환 버그 근본 수정 + 갤러리 페이지 넘기기를 리스트 하단 바처럼 분리된 화살표 탭으로 재구성 + 배경음 재생 버그 등 (2026-08-11~12)

이전 세션(요약 압축으로 앞부분 요약만 남음)에 이미 진행된 것들 먼저 정리, 그 다음이 이번에 실제로 대화한 부분:

**이전 세션에서 이미 끝낸 것(요약으로만 남아있던 부분)**
- Samsung TV History Film 프로젝트에서 이미지가 안 불러와지던 버그 — `inferProjectFolder()`의 제목 폴백이 줄바꿈(`\n`)이 섞인 제목을 그대로 폴더명으로 써서 생긴 문제. 클라이언트 추론/서버 새니타이즈(`_sanitize_folder`)/기존 IndexedDB 자동 복구 마이그레이션 3단계로 방어 + 디스크에 이미 잘못 생성된 폴더도 수동 이동.
- 히어로 동영상에 "이미 올린 동영상에서 선택" 버튼 추가(블록 동영상 모달 재사용).
- 블록 기능 4종 배치: 블록 순서를 드래그 없이 버튼(⤒▲▼)으로도 옮기기, 사진/동영상을 헤드라인·본문보다 위로 배치하는 옵션, 사진·동영상 레이아웃을 독립적으로도/통합해서도 지정 가능하게, 동영상 다듬기(업로드 시 + 나중에 재편집).
- 프로젝트 상세뷰를 닫아도(모바일에서 특히) 동영상 소리가 백그라운드에서 계속 나던 버그 — `closeProject()`가 뷰를 `display:none`으로 숨기기만 하고 실제 미디어(video/iframe)는 안 멈춘 게 원인. `scrollRoot.innerHTML=""`로 내용 자체를 비워서 확실히 정지시킴(iframe은 pause 방법이 없어서 노드 자체를 없애는 게 유일한 방법).
- 이 배치 전부는 커밋 안 한 상태였는데, 이후 사용자가 빌더에서 여러 번 Publish(각 프로젝트별)를 누르면서 `git add -A` 방식 때문에 그 안에 같이 묻어 들어가 커밋됨([[project_2026_portfolio_publish_git_add_a]]) — `5ebfc49`/`390c3c3`/`8bcae63` 등 "Publish: Samsung TV History Film" 커밋들에 흩어져 포함, 전부 `origin/main`에 반영 완료.

**1) 좌상단 jjaejae 로고 대비 자동전환 버그 — 두 번의 시행착오 끝에 진짜 원인 발견**
사용자가 PC/모바일 스크린샷으로 "로고가 밝은 배경에서도 어두운 배경에서도 흐릿하게 보인다"고 리포트.
- **1차 시도(틀림)**: `filter:invert(1)`과 `mix-blend-mode:difference`를 같은 `<img>`에 같이 걸면 Safari에서 깨진다는 알려진 버그를 의심 → `.brand-logo-blend` 래퍼를 하나 더 넣어서 필터/블렌드를 두 엘리먼트로 분리. Playwright로 헤드리스 크로미움에서 확인했더니 **여전히 흰색으로 뭉개져 나옴** — 이 가설이 틀렸다는 뜻.
- **진짜 원인**: `mix-blend-mode`가 걸린 엘리먼트가 `position:fixed` 조상 **안의, 그 자체는 위치값 없는 자식**일 때 크로미움이 실제 페이지 배경과 제대로 블렌딩을 못 하고 그냥 흰색 그대로 그려버리는 별개의 브라우저 동작 — 순수 HTML로 최소 재현해서 확인함(`mix-blend-mode`를 `.brand-logo`(고정 위치 엘리먼트) **자신**에게 걸면 정상 동작, 안에 있는 위치값 없는 래퍼에 걸면 깨짐). 헤더 nav(`#site-header`)는 처음부터 `position:fixed` 엘리먼트 자기 자신에 블렌드를 걸고 있어서 항상 정상이었던 것.
- **수정**: 래퍼 없이 `.brand-logo`(고정 위치 엘리먼트) 자체에 `mix-blend-mode:difference`, 안의 `<img>`에만 `filter:invert(1)` — Safari 버그(같은 엘리먼트에 필터+블렌드 금지)와 이번 크로미움 버그(블렌드는 반드시 fixed 엘리먼트 자신에 있어야 함) 둘 다 피하는 유일한 조합.
- 데스크탑/모바일 뷰포트, 밝은/어두운 배경, Info 페이지(예외 처리 유지) 전부 Playwright로 재확인. 커밋 `42ef91f`.

**2) Work/Play 리스트 배경 동영상 로딩 — 지연 로드 시도했다가 다시 되돌림**
"로고 수정 후 리스트에서 동영상 자동재생이 안 되거나 로딩이 길어졌다"는 리포트로 시작(실제로는 로고 커밋과 무관 — 애초에 리스트를 열 때 항목 전부의 Vimeo/영상을 동시에 로드하던 기존 설계가 원인이었음).
- 처음엔 "호버/선택된 항목만 그때 로드"하는 지연 로딩으로 바꿨는데, Vimeo iframe이 그 시점에야 새로 붙기 시작하면서 선택할 때마다 몇 초간 정지 썸네일만 보이는 문제가 발생(포스터 위에 iframe이 로드되면 페이드인시키려 했지만 `load` 이벤트 타이밍이 브라우저마다 달라 아예 안 뜨는 경우까지 있었음).
- **최종 결론 — 사용자 명확한 요청으로 원래 방식(리스트 열 때 전부 동시 프리로드)으로 되돌림**: "다른 포폴 사이트들은 선택하자마자 로딩 없이 바로 재생된다"는 게 기준. 리스트를 여는 첫 순간 항목 수만큼 동시 로딩되는 트레이드오프는 있지만, 선택 시점엔 이미 재생 중인 걸 보여주기만 하면 되므로 끊김이 없음 — **이후 세션에서 다시 지연 로딩을 시도하지 않아도 됨(한 번 시도해서 되돌린 결론)**.

**3) 갤러리(모아보기) 페이지 넘기기를 리스트 하단 바 스타일의 분리된 화살표 탭으로 재구성**
"동영상끼리 모아보기했을 때 클릭하면 재생만 되고 다음 장으로 안 넘어간다" + "카운터/hint 문구를 사진 위에 겹치지 말고 위/아래 탭으로 따로 빼줬으면" 요청.
- 기존엔 사진/영상 위에 반투명으로 겹쳐 보이는 카운터·"Click for next" hint뿐이라, 동영상 프레임(자체 재생 컨트롤이 있어 클릭이 재생으로 먹힘)에서는 다음 장으로 넘길 방법이 아예 없었음.
- 리스트 하단 CLOSE/이름/화살표 바(`.list-footer`)와 같은 패턴으로, 사진/영상 박스 **밖**에 별도의 알약(pill) 바(`.gallery-nav`)를 두고 이전/다음 버튼 + 카운터를 배치 — 항상 보이고 항상 클릭 가능. 기존 오버레이 카운터(`.gallery-counter` 절대위치)·hint(`.gallery-hint`) CSS/마크업은 제거.
- 동영상/iframe 위를 누른 클릭은 페이지 넘기기 대상에서 제외(재생 버튼 누르다 실수로 페이지가 넘어가던 문제 방지), 대신 새 화살표 버튼은 항상 동작.
- 페이지를 넘기면 이전 페이지에서 재생 중이던 동영상을 자동으로 일시정지(`pauseGalleryVideos`) — "동영상 넘어갔는데 소리만 계속 난다"는 문제 해결.
- **카운터 표시 형식 변경**: 처음엔 "01-04 / 08"처럼 보이는 사진/동영상 개수 기준이었는데, 사용자 요청으로 "1/3"처럼 **페이지 번호/총 페이지 수** 기준으로 변경.
- 사진 모아보기(roll 모드, 한 장씩)와 동영상 페이지 모아보기(mode-multi, 여러 장씩) 둘 다 동일하게 적용.
- **빌더(builder.html) 미리보기에도 위 변경사항 전부 포팅** — "빌더 미리보기가 index.html이랑 다르게 보인다"는 요청으로, 미리보기 iframe에 주입되는 별도의 (문자열로 짜여진 ES5) 렌더링/런타임 JS 사본에도 동일하게 반영. 실측해보니 사이즈 계산 로직 자체는 원래도 두 파일이 일치했고, 실제로 달라 보였던 건 이 카운터/화살표 UI 차이였음(미리보기 패널 자체가 실제 브라우저보다 좁아서 절대 크기가 작게 보이는 건 정상 — 그건 못 없앰).
- **검증**: 실제 발행된 프로젝트(Galaxy Tab S9의 8개 영상 mode-multi 갤러리, SSF SHOP의 2장짜리 롤 갤러리 등)를 Playwright로 열어서 이전/다음 버튼 클릭 시 페이지 전환·카운터 갱신·영상 일시정지가 실제로 동작하는지 확인, 발행된 13개 프로젝트 전부 열어봐도 JS 에러 없는지 회귀 확인.
- 커밋 `fa3c470`, `origin/main` push 완료.

**참고**: 이 세션 도중 사용자가 빌더에서 BRANDI를 Publish 했을 때, 그 시점에 작업 중이던 index.html 수정(리스트 지연로딩 시도 + 갤러리 pause 수정)이 `git add -A` 때문에 "Publish: BRANDI"(`ac35a7c`) 커밋에 같이 묻어 들어감 — 데이터 손실은 없고 내용도 다 맞지만, 커밋 메시지가 실제 diff 내용을 다 설명하진 않음(참고만 하면 됨).

**세션 마무리 (2026-08-12)** — `git status` clean, `origin/main`과 완전 동기화 확인(`fa3c470`까지 push 완료). 로컬 서버(`python3 server.py`, 8420)는 계속 켜져 있을 수 있음, 다음 세션 시작 시 `curl localhost:8420/ping`으로 확인.

## 세션 요약 — Toss 프로젝트 추가, 크레딧 Client/Agency 자동 고정, 리스트 UX 개선, 갤러리 버그 수정 (2026-08-10)

- **Toss | Fraud Bank Account Search Service (2020) 프로젝트 추가**: 비핸스(behance.net/gallery/128198713)에서 텍스트/카피는 WebFetch로 가져와 빌더 IndexedDB에 seed 마이그레이션으로 주입(ministock 때와 동일한 패턴). 이미지 2장은 처음 WebFetch가 놓친 게 있어서 실제로는 사용자가 더 받아서 직접 빌더의 "사진 추가"로 정리 완료.
- **크레딧에 Client/Agency 필드 신설 + 항상 맨 위 고정**: 프로젝트 편집 화면의 "카테고리" 밑 필드를 Client/Agency 두 칸으로 분리(기존엔 사이트에 전혀 안 보이던 죽은 `meta` 텍스트 필드였음). 여기 입력하면 크레딧 리스트 맨 위에 자동으로 붙고 드래그로 못 옮기게 고정, 나머지 수동 크레딧들은 그 아래부터. 기존 프로젝트들(크레딧 첫 줄이 이미 Client/Agency였던 것들)은 빌더 로드 시 자동 마이그레이션됨. "붙여넣기로 추가" 모달도 Client/Agency 줄을 자동으로 이 필드로 라우팅.
  - 처음엔 `grid-column:1/-1` divider 방식으로 만들었다가, CSS grid auto-placement가 그 자리에서 강제로 줄바꿈시켜서 Client/Agency만 빈 칸 여러 개를 남긴 채 혼자 한 줄을 차지하는 버그를 발견 → Client/Agency를 별도의 작은 grid로 분리하고 나머지 grid의 첫 줄 자체 테두리가 구분선 역할을 하도록 재설계.
  - "CREDITS" 제목 밑줄 + 크레딧 첫 줄 테두리가 겹쳐 보이던 것도 제목 쪽 밑줄을 없애서 해결.
- **크레딧 값의 `@계정` 자동 하늘색 표시 + 선택적 링크**: `@handle`은 항상 하늘색(#38bdf8)으로 표시. `@handle(URL)` 형식으로 쓰면 그 URL로 이동하는 링크가 됨(화면엔 URL 안 보이고 `@handle`만 보임). URL 없이 `@handle`만 쓰면 색만 입혀지고 클릭은 안 됨 — "웹사이트는 내가 직접 넣겠다"는 요청 반영.
- **프로젝트 상세뷰 우상단 ✕ Close → 홈 대신 Work/Play 리스트로 복귀**: 하단 "All Work/Play" 링크와 동일한 목적지로 통일. 로고 클릭/Escape는 기존처럼 홈으로 이동(변경 안 함).
- **Work/Play 리스트가 10개 넘으면 창 밖으로 계속 안 늘어나고 내부 스크롤**: `.list-rows`에 `max-height:700px`(≈10줄) 지정. 화면이 아무리 커도 리스트 박스 크기가 일정하게 유지됨.
- **리스트 열어두고 5초간 가만히 있으면 다음 프로젝트 미리보기로 자동 전환**: 호버·위/아래 버튼 클릭 시 카운트다운 리셋, 리스트 닫으면 타이머도 정지.
- **Neo QLED 8K "Idea" 블록 이미지 밑 검은 여백 버그 수정**: 원인은 `.gallery`의 CSS `min-height:280px` 고정값 — 아주 넓은 이미지(1400×219px, 6.4:1)는 정상 계산 높이가 193px로 이 최소값보다 작아서, 박스는 280px를 유지한 채 나머지가 배경색(검정)으로 남아 보이던 것. 이미지 크기에 맞춰 박스 높이를 정할 때 `min-height`도 함께 갱신하도록 수정 — 특정 이미지가 아니라 갤러리 로직 자체를 고쳐서 앞으로 비슷한 극단적 가로/세로 비율 이미지에도 재발 안 함.
- **참고**: 이 세션 내내 여러 수정사항이 사용자가 빌더에서 직접 Publish/Sync order를 누를 때마다 `git add -A`로 같이 커밋됨([[project_2026_portfolio_publish_git_add_a]]) — 별도로 커밋한 것도 있고(`6ce3044`, `2491486` 등) 사용자 Publish에 묻어간 것도 있음(BRANDI, Toss Securities, Samsung Neo QLED 8K 등 커밋들). 세션 종료 시점 `git status` clean, `origin/main`과 동기화 확인됨.

**세션 마무리 (2026-08-10)** — 사용자가 빌더에서 NOSCARNA 크레딧 확인 후 직접 Publish 진행. 빌더의 Publish가 `git add -A`를 쓰는 구조라([[project_2026_portfolio_publish_git_add_a]]) 이 세션에서 만든 코드 수정(server.py 동영상 dedupe/raw업로드/화질개선, builder.html 폴더선택 탭·크레딧 붙여넣기 기능, .gitignore)이 NOSCARNA 미디어 파일들과 함께 `4f1b94b`("Publish: NOSCARNA") 커밋에 같이 들어갔고, 이어진 `5672730`/`ddc23b1` 커밋까지 전부 `origin/main`에 이미 push 완료(`git status` 확인 결과 clean, "up to date with origin/main"). 별도로 커밋/push할 것 없음 — 다음 세션은 이 상태를 최신으로 보고 시작하면 됨. 로컬 서버(`python3 server.py`, 8420)는 이 세션 수정사항이 반영된 채로 계속 켜져 있을 수 있음.

## 세션 요약 — 크레딧 "붙여넣기로 추가" 기능 신설 (2026-08-10, 계속)

NOSCARNA 프로젝트 크레딧에 30줄 가까운 에이전시 크레딧 블록(Client/Agency/::FILM::/::Key Visual:: 섹션 포함)을 추가해달라는 요청. **직접 대신 입력해줄 수 없었던 이유**: 빌더의 프로젝트 데이터는 각자의 브라우저 IndexedDB에만 있고 저장소의 파일이 아니라서, 이 세션(터미널/파일 접근만 가능, 실제 브라우저 조작 불가 — [[feedback_sandbox_no_browser_automation]])에서는 사용자의 실제 브라우저를 열어 그 프로젝트를 직접 편집할 방법이 없음. 대신 붙여넣기만 하면 되는 기능을 새로 만들어서 사용자가 직접 몇 클릭으로 끝내도록 함.

- **크레딧 섹션에 "붙여넣기로 추가" 버튼 신설**(기존 "+ 추가" 옆). 누르면 모달이 뜨고, 클라이언트/에이전시가 준 크레딧 텍스트를 그대로(줄바꿈 포함) 붙여넣으면 `parseCreditsText()`가 "라벨 : 내용" 형식인 줄만 골라 `project.credits`(label,value 쌍의 flat 배열 — 기존 index.html의 다른 프로젝트 크레딧들도 전부 이 구조라 섹션 구분 자체가 원래 지원 안 됨)에 한 번에 추가.
- 파싱 규칙: 빈 줄, `[CREDIT]` 같은 대괄호 줄, `::FILM::`/`::Key Visual::` 같은 콜론 두 개짜리 구분선은 자동으로 걸러짐. 콜론은 첫 번째 것만 기준으로 라벨/내용을 나눠서 "L.Producer"처럼 라벨 자체에 마침표가 있어도, "Account Executive : 강선규, 홍혜영, 조보은 @bbonybony , 김태율"처럼 내용에 쉼표·인스타 핸들이 섞여 있어도 그대로 보존됨.
- **검증**: jsdom으로 실제 사용자가 요청한 NOSCARNA 크레딧 텍스트 전체를 붙여넣기 모달에 넣고 확인 버튼을 눌러서, 28개 행이 정확히 생성되고(섹션 마커 3줄은 제외) 핸들/쉼표 목록이 안 깨지는지 확인 — 전부 통과.
- **사용자가 해야 할 일**: 로컬에서 `python3 server.py` 켜고 builder.html 열어 NOSCARNA 프로젝트 선택 → 크레딧 섹션의 "붙여넣기로 추가" 클릭 → 원본 크레딧 텍스트 그대로 붙여넣기 → 추가 클릭. (이 기능 자체는 서버 호출이 없어 로컬 서버가 꺼져 있어도 동작하지만, 다른 기능들 때문에 평소처럼 켜두는 게 안전함.)
- 아직 커밋 전.

## 세션 요약 — 동영상 웹 최적화 화질/해상도 개선 (2026-08-10, 계속)

"첨부된 동영상은 늘 웹최적화 해줘야해. 화질 변화 최소화(1080p 유지 가능하면 최고)" 요청.

- **세로 영상 해상도 캡 버그 수정**: 기존 ffmpeg 스케일 필터(`scale=min(1920,iw):-2`)는 항상 **가로(width)** 기준으로만 1920을 캡했음 — 가로 영상엔 맞지만 세로 영상은 iw(가로 길이)가 원래 짧은 변이라 4K 세로 영상(2160x3840)을 올리면 오히려 1920x3413로 커져버려서 "1080p"가 아니었음. `scale='if(gt(iw,ih),min(1920,iw),-2)':'if(gt(iw,ih),-2,min(1920,ih))'`로 바꿔서 **긴 변**을 항상 1920으로 캡(가로 영상 → 1920x1080, 세로 영상 → 1080x1920)하도록 수정. 원본이 이미 1080p/1080x1920 이하면 리사이즈 자체가 안 걸려서 해상도 그대로 보존됨.
- **화질 설정 상향**: `-crf 23 -preset medium` → `-crf 18 -preset slow`(오디오도 128k→192k)로 올려서 시각적으로 거의 무손실 수준으로 인코딩 — 로컬에서 업로드 시 한 번만 도는 작업이라 인코딩 시간이 좀 늘어도(타임아웃도 900s→1800s로 늘림) 화질 우선.
- **"폴더에서 선택" 목록이 항상 최적화된 파일만 보이게 필터링**: 어제 만든 "폴더에서 선택" 탭이 폴더 안의 mp4/webm/mov/m4v를 전부 나열했는데, 만약 사용자가 앱을 거치지 않고 원본 영상을 프로젝트 폴더에 직접 복사해뒀다면 그 미최적화 원본도 목록에 뜰 수 있었음 — "항상 웹최적화"를 보장하기 위해 `/scan-videos`가 파일명이 `_unique_path`가 항상 붙이는 `-web`/`-web2`... 접미사로 끝나는 파일만(=우리 ffmpeg 파이프라인을 거친 결과물만) 반환하도록 필터 추가.
- **검증**: 스크래치 디렉토리에서 4K 가로/4K 세로/이미 1080x1920인 세로 영상 3개를 실제로 인코딩해서 각각 1920x1080, 1080x1920, 1080x1920(리사이즈 없이 그대로)이 나오는지 확인 — 전부 통과. 폴더에 최적화 파일 3개 + "원본을 직접 복사해 넣은 척"한 파일 1개를 두고 `/scan-videos` 필터를 돌려서 원본이 목록에서 빠지는지도 확인 — 통과.
- 로컬 서버 재시작해서 반영.
- 아직 커밋 전.

## 세션 요약 — 블록 "폴더에서 선택" 동영상 재사용 + 세로 영상 첨부 시 오류 페이지 수정 (2026-08-10, 계속)

콘텐츠 해시 캐시(바로 아래 항목)를 넣어도 여전히 "같은 동영상을 블록에서 또 쓰니 폴더에 중복 생성된다"는 리포트. 원인: 해시 캐시는 "완전히 동일한 원본 바이트"만 잡아내는데, 사용자가 실제로 한 행동은 원본 파일을 다시 고른 게 아니라 히어로에 이미 올려서 폴더에 생긴 **결과물(이미 한 번 ffmpeg로 인코딩된 mp4)**을 파일 선택창에서 다시 골라 블록에 첨부한 것 — 이걸 서버에 다시 올리면 "이미 인코딩된 파일을 한 번 더 인코딩"해서 원본과 바이트가 달라지므로 해시가 안 맞고 새 파일이 또 생김. 애초에 "이미 첨부된 걸 재사용"할 방법 자체가 빌더에 없었던 게 근본 원인.

- **동영상 추가 모달에 "폴더에서 선택" 탭 신설**: 파일 첨부/링크 첨부 사이에 탭 하나 추가. 열면 `GET /scan-videos?folder=...`(server.py에 새로 추가 — `/scan-images`와 동일한 패턴, `find_project_folder`로 프로젝트 폴더를 찾아 그 안의 mp4/webm/mov/m4v를 재귀적으로 나열)로 그 프로젝트 폴더에 이미 있는 동영상 목록을 받아와서, 클릭 한 번으로 relPath만 그대로 `block.images`에 추가 — 서버에 아예 다시 안 올리니 재인코딩도 중복 생성도 원천적으로 없음. (사용자 피드백 메모리: 벌크 버튼보다 항목별 액션 선호 — 목록 각 줄에 "선택" 버튼을 달아 항목 단위로 처리.)
- **세로형(portrait) 영상 첨부 시 "오류 페이지"로 바뀌는 문제**: 원인은 세로/방향이 아니라 **파일 용량** — 지금까지 동영상 파일 업로드는 `FileReader.readAsDataURL`로 파일 전체를 base64 문자열(원본보다 약 33% 커짐)로 메모리에 만든 뒤 그걸 다시 `JSON.stringify`로 감싸서 fetch로 보냈음. 폰으로 찍은 세로 영상은 보통 가로 영상보다 용량이 훨씬 커서(고화질 세로 촬영), 이 거대한 문자열을 만드는 과정에서 탭 메모리가 감당 못 해 브라우저가 죽고 "오류가 발생했습니다" 페이지로 바뀐 것으로 판단됨.
  - **수정**: `server.py`에 `POST /optimize-video-raw` 신설 — JSON이 아니라 파일 바이트를 **그대로** 요청 본문에 담아 보냄(`fetch(url, {body: file})`, `folder`/`filename`은 쿼리스트링). 브라우저가 File을 문자열로 바꾸지 않고 네트워크로 스트리밍해서 보내기 때문에 거대한 base64 문자열이 아예 안 생김. 서버 쪽 `optimize_video()`도 `optimize_video_from_bytes(raw, folder, filename)`로 인코딩/해시 캐시 로직을 분리해서 raw 경로와 기존 dataUrl 경로(링크 모드는 그대로 유지) 둘 다 재사용하도록 리팩터링.
  - 빌더의 히어로 "직접 업로드"와 블록 동영상 첨부(파일 탭) 둘 다 공용 헬퍼 `optimizeVideoRaw(file, folder)`로 통일해서 `/optimize-video-raw`를 타도록 전환. 링크 첨부(비메오/유튜브가 아닌 직접 mp4 링크)는 파일 자체를 브라우저가 들고 있지 않으므로(서버가 URL을 직접 받아서 처리) 그대로 기존 JSON 경로 유지.
- **검증**: (1) 실제 `server.py`를 스크래치 디렉토리에 복사해 `ffmpeg`으로 세로(40x80, width<height) 테스트 영상을 만들어 `/optimize-video-raw`에 실제 HTTP 요청으로 두 번 올려봄 — 첫 번째는 정상 인코딩, 두 번째는 `reused:true`로 재사용, 폴더에 파일 1개만 남는 것 확인. `/scan-videos`도 실제 서버로 호출해서 방금 만든 파일이 목록에 정확히 뜨는지 확인. (2) jsdom으로 빌더의 동영상 모달을 열어 "폴더에서 선택" 탭 전환 시 `/scan-videos` 호출과 목록 렌더링, 항목 클릭 시 `/optimize-video`류 API를 전혀 안 부르고 바로 relPath만 추가되는지, 파일 탭에서 파일 선택 후 "추가"를 누르면 예전 `/optimize-video`(JSON)가 아니라 새 `/optimize-video-raw`로 나가는지 확인 — 전부 통과. 기존 히어로/드래그앤드롭 회귀 테스트도 재실행해서 이상 없음 확인.
- 로컬 서버(`python3 server.py`, 8420)를 재시작해서 이 수정이 실제로 반영된 상태로 켜져 있음.
- 아직 커밋 전.

## 세션 요약 — 동영상 업로드 중복 생성 방지 (서버 콘텐츠 해시 캐시) (2026-08-10, 새 세션)

"히어로에 쓴 동영상을 다른 블록에서도 쓰려니 로컬 폴더에 파일이 2개로 중복 생성된다" 리포트. 원인: `/optimize-video`(server.py)가 매번 `_unique_path`로 새 파일명을 만들어 ffmpeg 재인코딩까지 새로 돌리는 구조라, 같은 원본 파일을 히어로용으로 한 번, 블록용으로 한 번 — 이렇게 두 번 올리면 (같은 파일인데도) 서로 다른 이름의 mp4가 두 개 생겼음. 빌더에 "이미 올린 미디어 재사용" 같은 선택 UI가 아예 없었던 것도 원인.

- **서버(`server.py`) 콘텐츠 해시 캐시**: `mode:"upload"`로 들어온 원본 파일의 바이트를 sha256으로 해시 → `.video_cache.json`(BASE_DIR 최상단, git-ignore 처리)에 `해시 → {relPath, width, height}` 기록. 같은 해시가 다시 들어오면(=완전히 같은 파일을 다른 자리에 또 첨부) ffmpeg 재인코딩 자체를 건너뛰고 기존 relPath를 그대로 재사용 응답(`reused:true`). 캐시된 relPath의 실제 파일이 사라진 상태(수동 삭제 등)라면 캐시를 무시하고 정상적으로 재인코딩 — 캐시 불일치로 깨지는 경우 없게 처리.
- **빌더(`builder.html`)**: 히어로 업로드(`uploadHeroVideoFile`)와 블록 동영상 첨부 모달(video-modal 확인 버튼) 둘 다 응답의 `reused:true`를 확인해서 "이미 최적화된 동일한 영상을 재사용했습니다" 안내를 띄우도록 추가 — 재인코딩 없이 즉시 끝나니 사용자가 오류로 착각하지 않게.
- **검증**: 실제 `server.py`를 스크래치 디렉토리로 복사해 격리된 BASE_DIR에서 (1) 같은 영상을 두 번 업로드하면 폴더에 파일이 1개만 남고 두 번째 응답이 `reused:true`인지, (2) 캐시에 있는 relPath의 파일을 수동으로 지운 뒤 같은 영상을 다시 올리면 정상적으로 재인코딩되어 파일이 복구되는지 — 둘 다 통과. `python3 -m py_compile server.py`로 문법 확인.
- **참고**: 이 캐시는 "완전히 동일한 파일"만 잡아냄 — 같은 영상이라도 파일이 조금이라도 다르면(재압축·트리밍 등) 다른 해시로 취급되어 새로 인코딩됨(의도된 동작).
- 아직 커밋 전.

**세션 마무리 (2026-08-10)** — 커밋 `21f05bd`까지 완료(메인 히어로 유튜브/업로드 + 드래그앤드롭). push는 사용자가 직접 하기로 함 — 다음 세션 시작 시 `git log origin/main..HEAD`로 반영됐는지 확인할 것. 로컬 서버(`python3 server.py`, 포트 8420)는 이 세션에서 백그라운드로 켜둔 상태로 남아있을 수 있음.

## 세션 요약 — 빌더 파일 첨부 자리 전부 드래그앤드롭 지원 (2026-08-10, 계속)

기존엔 블록 사진/동영상 첨부와 타이틀 폰트 첨부(`#font-preview-box`)만 드래그앤드롭이 됐고, 메인 히어로(이미지/동영상)와 INFO 배경 사진(PC/모바일)은 버튼으로만 첨부 가능했음 — "파일 첨부는 언제나 드래그앤드롭 가능하게" 요청으로 나머지도 전부 채움.

- **공용 헬퍼 `attachFileDrop(selector, onFiles)`** 신설 — 기존 블록 드롭이 쓰던 것과 동일한 판별 방식(내부 카드 재정렬 드래그 `dragItem`이나 실제 OS 파일이 아닌 드래그와 안 부딪히게 `dataTransfer.types.includes("Files")`로 걸러냄)을 재사용 가능한 형태로 뽑아냄. `editorEl`에 `dragover`/`dragleave`/`drop` 세 개를 등록하고, `data-drop-target="..."` 속성이 붙은 컨테이너를 `closest()`로 찾아 강조 표시 후 콜백 호출.
- **메인 히어로**: 섹션 전체(`data-drop-target="hero-media"`)가 하나의 드롭존 — 사진을 놓으면 "이미지" 모드로, 동영상을 놓으면 자동으로 "직접 업로드" 모드로 전환하면서 그 자리에서 바로 서버 최적화까지 진행(블록 드롭이 사진/동영상을 자동 구분하던 것과 동일한 패턴). 기존 버튼 클릭 로직도 `setHeroImageFile(file)`/`uploadHeroVideoFile(file)` 함수로 뽑아내서 버튼 경로와 드롭 경로가 완전히 같은 코드를 타도록 정리.
- **INFO 배경 사진 PC/모바일**: 각 행(`data-drop-target="info-bg-pc"`/`"info-bg-mobile"`)에 드롭하면 지난 세션에 이미 파라미터화해둔 `uploadInfoBgImage(file, target)`을 그대로 호출 — PC 칸에 놓으면 PC용, 모바일 칸에 놓으면 모바일용으로 정확히 분리됨.
- CSS `.file-drop-target.file-drag-over`를 공용 강조 스타일로 추가(기존 블록 카드/폰트 박스 강조와 톤 맞춤, 점선 아웃라인).
- **검증**: jsdom으로 가짜 `dataTransfer`(types:["Files"], files:[...])를 담은 dragover/drop 이벤트를 각 드롭존에 직접 쏴서 (1) 메인 히어로에 이미지를 놓으면 히어로 이미지가 바뀌는지, (2) 동영상을 놓으면 `/optimize-video` 호출과 함께 heroType이 자동으로 "upload"로 바뀌는지, (3) INFO 배경 PC/모바일에 각각 놓으면 `/upload-info-bg`에 정확히 `target:"pc"`/`"mobile"`로 나가는지, (4) 지원 안 하는 파일 형식(pdf 등)을 놓으면 조용히 실패하지 않고 알림이 뜨는지까지 확인 — 전부 통과. 테스트 중 jsdom이 `structuredClone`/`URL.createObjectURL`을 기본 제공 안 해서 걸린 것도 발견 — 실제 브라우저엔 둘 다 있어서 무해함(테스트 환경에만 폴리필 추가해서 우회).
- **범위 참고**: 프로젝트/데이터 가져오기(JSON import) 버튼은 이번에 포함 안 함 — 사진/동영상/폰트 같은 "미디어 첨부" 자리들 위주로 처리했고, JSON 가져오기도 드래그앤드롭이 필요하면 말씀해주세요.
- 아직 커밋 전.

## 세션 요약 — 메인 히어로에 유튜브 링크 / 직접 업로드 추가 (Vimeo 외) (2026-08-10, 계속)

지금까지 빌더의 "메인 히어로" 동영상은 Vimeo 링크(정확히는 숫자 ID)만 지원했음 — 유튜브 링크나 직접 업로드한 파일도 되게 해달라는 요청. `heroType`을 `"image"`/`"video"`(Vimeo, 기존)에 `"youtube"`/`"upload"` 두 가지를 새로 추가하는 방식으로 확장(기존 프로젝트 데이터는 전혀 안 건드림 — `heroType:"video"`가 여전히 Vimeo를 뜻함).

- **데이터 모델**: `heroYoutubeId`(문자열), `heroVideoFile`(relPath 문자열) 두 필드 신설.
- **빌더 UI**: "메인 히어로" 라디오가 이미지/비메오/유튜브/직접 업로드 4개로 늘어남. 유튜브는 링크(watch?v=, youtu.be/, shorts/)나 순수 ID를 붙여넣어도 정규식으로 11자리 ID만 뽑아 저장. 직접 업로드는 블록 동영상 첨부(video-modal)가 이미 쓰던 `/optimize-video`(ffmpeg 웹 최적화, mode:"upload") 경로를 그대로 재사용 — 별도 서버 변경 없음. 라디오를 바꾸면 해당 타입의 입력 필드만 보이도록(예전엔 비메오 ID 입력창이 이미지를 선택해도 항상 보였음 — 이번에 조건부로 개선).
- **렌더링(빌더 프리뷰 + 실제 index.html 둘 다)**: `heroMedia` 생성 로직에 유튜브(`youtube.com/embed/...&loop=1&playlist=ID` — 유튜브는 `loop=1` 하나만으론 안 돌아가고 `playlist`에 같은 ID를 같이 줘야 실제로 반복재생됨, 잘 알려진 임베드 특성)와 업로드(`<video autoplay muted loop playsinline>`, iframe이 아니라 네이티브 video라 `object-fit:cover`를 직접 지원해서 오히려 Vimeo/유튜브의 "화면 꽉 채우기 위해 iframe을 과도하게 키우는" 트릭이 필요 없음) 분기 추가.
- **리스트 배경 프리로드 + 즉시재생 재사용(이전 세션에 만든 기능)도 3가지 타입 전부 대응**: `buildHeroMediaEl(p)` 하나로 리스트 배경 생성과 "선택 시 재사용" 판별 로직을 통일 — `rescuePreloadedHero`가 이제 Vimeo/유튜브 iframe뿐 아니라 업로드 `<video>` 엘리먼트도 똑같이 "이동만 시켜서" 재사용함(video 엘리먼트는 iframe과 달리 브라우징 컨텍스트 문제가 아예 없어서 오히려 더 안전하게 재사용됨). 재사용 슬롯에 iframe이 들어갈 땐 `.p-hero-video` 클래스를 붙여야 화면 꽉 채우기 CSS가 걸리고, video는 클래스 없이도(자손 선택자라) 정상 동작 — 재사용 코드에서 이 케이스를 분기해서 처리.
- **exportProjectCode**: heroType이 youtube/upload일 때 각각 `heroYoutubeId`/`heroVideoFile`을 실제 발행 코드에 내보내도록 추가.
- **검증**: `node --check`로 index.html/builder.html 문법 확인. index.html은 실제 PROJECTS 배열에 유튜브/업로드 타입 테스트용 프로젝트 2개를 임시로 주입한 스크래치 사본을 만들어서(원본은 안 건드림) jsdom으로 (1) 리스트 배경에 유튜브는 iframe으로 정확한 embed URL(loop+playlist 파라미터 포함)로, 업로드는 `<video>`로 프리로드되는지, (2) 호버하면 즉시(재로드 없이) 활성화되는지, (3) 클릭해서 프로젝트로 들어가면 유튜브는 iframe이, 업로드는 video가 리스트에서 쓰던 것과 완전히 동일한 DOM 노드로 재사용되는지 확인 — 전부 통과. 기존 Vimeo 경로 회귀 테스트도 재실행해서 이상 없음 확인. 빌더는 새 프로젝트를 만들어 라디오 4개 전환·유튜브 URL 붙여넣기 시 ID 추출·업로드 버튼 노출 조건까지 jsdom으로 확인(테스트 중 `fake-indexeddb/auto`가 jsdom의 별도 컨텍스트에 안 먹혀서 나던 크래시를 발견 — `window.indexedDB`에 직접 연결하는 방식으로 우회, 메모리에 기록해둠).
- **실제 브라우저에서 확인 안 한 부분**: 유튜브 임베드가 실제로 `controls=0` 등 파라미터로 깔끔하게 배경처럼 보이는지, 직접 업로드한 영상이 실제 ffmpeg 최적화 후 화질/용량이 괜찮은지, 세 방식 전환 시 빌더 프리뷰가 매끄럽게 바뀌는지는 브라우저 + 로컬 서버(`python3 server.py`, 지금 켜져 있음)로 직접 확인 필요.
- 아직 커밋 전.

## 세션 요약 — Vimeo 서버 preconnect 추가 (2026-08-10, 계속)

"동영상 로딩을 아예 없애려면 직접 업로드(자체 호스팅)뿐인지" 질문 — 맞다고 답변(Vimeo iframe을 쓰는 이상 플레이어 부트스트랩 시간 자체는 못 없앰, 완전 무로딩은 자체 호스팅 `<video>`로 갈아타야만 가능). 다만 영상 재업로드 없이 공짜로 얻을 수 있는 개선 하나를 바로 적용:
- `index.html` `<head>`에 `<link rel="preconnect">`를 Vimeo 관련 도메인(`player.vimeo.com`, `i.vimeocdn.com`, `f.vimeocdn.com`) 앞으로 추가 — 실제 iframe을 만들기 전에 DNS/TLS 핸드셰이크를 미리 끝내둬서, 리스트를 열거나 프로젝트에 들어갈 때 Vimeo로의 연결 수립 시간을 조금 단축함. 완전한 해결책은 아니고 "공짜로 얻는 약간의 개선" 수준.
- 검증: `node --check`로 스크립트 문법만 재확인(순수 `<link>` 태그 추가라 리스크 없음). 실제 체감 개선 정도는 브라우저에서 확인 필요.
- 아직 커밋 전.

## 세션 요약 — 로컬 서버 재기동 + INFO 번역을 통짜 1개가 아니라 인사말/문단별로 분리 (2026-08-10, 계속)

**1) "빌더에서 info 수정하려니 로컬 서버 켜져있는지 확인하라는 오류" — 실제로 `python3 server.py`가 안 켜져 있었음**
버그가 아니라 로컬 백엔드(포트 8420)가 꺼져 있어서 뜨는 정상 오류였음(`lsof -i :8420` 확인 결과 아무 것도 안 떠 있었음). 지금 세션에서 `python3 server.py`를 백그라운드로 다시 켜둠(`/ping` 확인 완료) — 지금은 정상 동작해야 함. 참고로 맥을 재시동하거나 터미널을 닫으면 다시 꺼지니, 그럴 땐 이 폴더에서 `python3 server.py`를 다시 실행해야 함(반복되면 터미널을 아예 하나 띄워두고 쓰는 걸 권장).

**2) INFO 번역을 "한 번에 전체" 대신 인사말·문단 각각 따로 번역하도록 변경**
- 기존엔 상단 버튼줄에 KR→EN/EN→KR 하나씩만 있어서 눌리면 인사말+모든 문단이 한 번에 번역됐음 — "한 군데만 마음에 안 들어도 전체를 다시 해야 하는" 불편함이 있어서, 프로젝트 편집 화면의 블록별 번역(`translate-block-ko-en`/`en-ko`, 블록마다 따로 있음)과 같은 방식으로 바꿈.
- 상단 버튼줄의 통짜 번역 버튼 2개는 제거. 대신 (1) 인사말 필드 라벨 옆에 작은 `KR → EN`/`EN → KR` 아이콘 버튼 추가, (2) 각 본문/태그라인 문단 카드 헤더(스타일/삭제 버튼 옆)에도 동일한 아이콘 버튼 쌍을 추가 — 이제 원하는 한 군데만 눌러서 번역 가능.
- 서버 쪽은 새 엔드포인트 없이 지난번에 만든 `/translate-info`(lead 문자열 + paragraphs 문자열 배열)를 그대로 재사용 — 인사말만 보낼 땐 `paragraphs`를 빈 배열로, 문단 하나만 보낼 땐 `lead`를 빈 문자열로 보내는 식으로 같은 스키마 안에서 "일부만 번역"을 표현.
- **검증**: jsdom으로 (1) 상단 통짜 버튼이 완전히 사라졌는지, (2) 인사말과 각 문단 카드에 독립된 번역 버튼이 있고 문단마다 `data-para-id`가 서로 다른지, (3) 문단 1개만 번역했을 때 그 요청이 `lead:""`에 `paragraphs` 배열 길이 1로 정확히 나가는지, 인사말과 다른 문단은 전혀 안 바뀌는지, (4) 반대로 인사말만 번역했을 때 `paragraphs:[]`로 나가고 이미 번역해둔 문단은 그대로 유지되는지까지 실제 fetch 요청 내용과 DOM 반영 결과로 확인 — 전부 통과.
- 이 변경도 아직 커밋 전 — 확인 후 알려주시면 커밋하겠음.

## 세션 요약 — 프로젝트 상세뷰 히어로 동영상도 리스트처럼 즉시 재생 + INFO 페이지 KR↔EN 번역 탭 추가 (2026-08-10, 계속)

**1) 리스트에서 프로젝트로 들어갈 때도 히어로 동영상 로딩이 심하던 문제**
리스트 배경은 프리로드로 해결했지만, 그 프로젝트를 클릭해서 상세페이지(`.p-hero-video`)로 들어가면 거기서 또 새 iframe을 처음부터 로드해서 마찬가지로 로딩이 보였음. 리스트에서 이미 재생 중이던(선택된) 그 iframe을 상세뷰로 그대로 옮겨서 재사용하도록 수정 — DOM 노드를 파괴 후 재생성하지 않고 `appendChild`로 "이동"만 시키면 브라우징 컨텍스트(재생 상태)가 유지된다는 점을 이용함(주의: `.remove()` 후 나중에 다시 넣는 식의 2단계 처리는 그 사이에 파괴되므로 안 됨 — 반드시 한 번의 `appendChild` 이동이어야 함).
- `projectCard`의 클릭 핸들러에서 `rescuePreloadedHero(p, i)`를 먼저 호출 — 그 프로젝트가 heroType:video면 `listBgLayers[i]`에 있던 iframe을 찾아 (숨김 처리한 채로) `document.body`로 옮겨서 `pendingHeroIframe`에 보관해둠. 그 직후 `goProject()`가 `closeList()`를 호출해 리스트 배경 전체(`listBgMedia.innerHTML=""`)를 비우지만, 이 iframe은 이미 다른 곳으로 옮겨져 있어서 같이 파괴되지 않음.
- `renderProject()`는 `pendingHeroIframe.id`가 지금 열려는 프로젝트와 일치하면 새 iframe을 만드는 대신 빈 슬롯(`#p-hero-video-slot`)만 템플릿에 남겨두고, `innerHTML` 렌더링 직후 그 자리에 보관해뒀던 iframe을 옮겨 넣음(숨김 스타일 해제). 일치하지 않는 경우(리스트를 거치지 않고 직접 URL로 들어온 경우, prev/next 화살표로 이동한 경우 등)는 기존처럼 새로 로드 — 이 최적화가 없어도 항상 정상 동작하는 안전한 폴백.
- **검증**: jsdom으로 (1) 리스트를 열면 특정 프로젝트의 배경 레이어에 iframe이 이미 있는지, (2) 그 행을 클릭해서 상세뷰에 들어갔을 때 `.p-hero-video` 안의 iframe이 리스트에서 쓰던 것과 **완전히 동일한 DOM 노드**인지(`===` 비교)와 `src`가 전혀 안 바뀌었는지(재로드 안 됐다는 증거), 임시로 붙였던 숨김 인라인 스타일이 지워졌는지, `#p-hero-video-slot` id가 안 남았는지 확인. (3) 이미지 히어로인 프로젝트는 이 로직이 전혀 개입 안 하고 기존처럼 `<img>`가 렌더되는지도 함께 확인. 리스트 프리로드/전환/닫기 관련 기존 테스트도 재실행해서 회귀 없음 확인.

**2) 빌더 INFO 페이지 편집 화면에 KR→EN / EN→KR 번역 버튼 추가**
기존 프로젝트 편집 화면에는 이미 전체/블록 단위 번역 버튼이 있었는데(`/translate` 엔드포인트), INFO 편집 화면에는 없었음 — 동일한 로컬 서버(`claude` CLI) 경로로 새로 추가.
- `server.py`: `INFO_TRANSLATE_SCHEMA`(`lead` 문자열 + `paragraphs` 문자열 배열)와 `build_info_translate_prompt()`를 새로 추가하고, `/generate` vs `/generate-info`와 동일한 패턴으로 `/translate` 옆에 `/translate-info` 엔드포인트를 신설(허용 경로 화이트리스트에도 추가). 기존 `/translate`(프로젝트용, title/tag/meta/blocks 구조)는 전혀 안 건드림 — INFO는 데이터 모양이 달라서(문단이 객체 배열이고 스타일 필드도 섞여있음) 별도 스키마/엔드포인트로 분리하는 게 안전하다고 판단.
- `builder.html`: INFO 편집 화면 상단 버튼줄에 `KR → EN`/`EN → KR` 버튼 추가(SAVE와 PUBLISH 사이, 프로젝트 편집 화면과 동일한 위치 관례). `runInfoTranslate(direction)`이 `infoData.lead`와 `infoData.paragraphs[].text`만 뽑아서 서버에 보내고, 응답이 오면 각 문단의 `id`/`type`/스타일 필드는 그대로 두고 `text`만 교체 — 링크(`links[].label/url`)는 번역 대상에서 제외(URL이 있고, 라벨이 "Instagram ↗"처럼 고유명사/짧은 라벨이라 번역이 오히려 부적절).
- **알려진 제약**: INFO 편집은 GENERATE 전용 1단계 되돌리기 슬롯만 있고 일반 undo 스택이 없어서, 번역도 되돌리기 버튼 지원 안 됨(GENERATE와 동일한 기존 한계, 새로 생긴 문제 아님). 번역 후 결과가 마음에 안 들면 프로젝트 편집처럼 그냥 다시 번역을 누르거나 직접 수정하면 됨.
- **검증**: `server.py` `python3 -m py_compile`로 문법 확인. jsdom으로 실제 builder.html을 열어 INFO 편집 화면 진입 → 두 버튼이 존재하는지 → `fetch`를 스텁해서 `/translate-info`로 정확한 요청(원본 lead 문자열, paragraphs가 순수 문자열 배열, direction)이 가는지 → 응답을 받으면 lead 입력창과 첫 문단 textarea 값이 번역 결과로 바뀌는지 → `localStorage`(`pf-builder-info`)에도 최종 반영되는지까지 확인.
- **실제 시각 확인(동영상 재사용 시 정말 로딩 없이 이어지는지, 번역 버튼 클릭 시 실제 claude CLI 응답 흐름)은 브라우저 + 로컬 서버(`python3 server.py`)로 직접 확인 필요.**

## 세션 요약 — jjaejae 로고 클릭 시 홈 스크롤 리셋 버그 수정 + 메인 마퀴 "클릭하면 색 바뀜" 힌트 추가 (2026-08-10, 계속)

**1) 리스트(WORK/PLAY) 등 오버레이가 열린 상태에서 좌상단 jjaejae 로고를 눌러도 페이지 스크롤 위치가 그대로 남아있던 버그**
- 원인: `goHome()`이 프로젝트/Info/리스트 오버레이만 닫고(`closeProject/closeInfo/closeList`) 실제 메인 페이지의 `window` 스크롤 위치는 전혀 건드리지 않았음. 리스트가 열려있는 동안은 `body.list-open{overflow:hidden}`이라 메인 페이지 스크롤이 그 시점 위치에 그대로 얼어붙어 있다가, 로고로 홈에 돌아가면 오버레이만 사라지고 스크롤은 "WORK/PLAY를 누르기 직전에 있던 위치"(사용자 표현: "어중간한 메인페이지 위치")에 그대로 남아있던 것.
- 수정: `goHome()`에 `window.scrollTo(0, 0)` 추가 — 오버레이를 닫으면서 메인 페이지도 항상 맨 위(인사말 마퀴)로 리셋되게 함. 이미 있던 우하단 "Top ↑" 버튼의 스크롤 로직과는 별개(그건 명시적으로 "지금 보고 있는 화면을 위로"라는 다른 의도라 손 안 댐).
- **검증**: jsdom으로 `window.scrollTo`를 스파이 처리해서, WORK 리스트를 연 상태에서 로고(`[data-go="home"]`)를 클릭하면 리스트가 닫히는 것과 동시에 `scrollTo(0,0)`(또는 `{top:0}`)이 호출되는지 확인.

**2) 메인 화면 5줄 마퀴가 "클릭하면 색이 바뀐다"는 걸 알려주는 힌트 추가**
사용자에게 세 가지 방향(상시 노출 알약 힌트 / 첫 방문에만 보이고 이후 안 보임 / 마우스를 올릴 때만 살짝 보임)을 제시하고 **상시 노출 알약 힌트**로 확정(기존 하단 중앙의 "● Scroll" 힌트와 통일된 스타일).
- `#intro` 상단 중앙에 `.color-cue`(`"● Click for color"`)를 새로 추가 — 기존 `.scroll-cue`와 동일한 반투명 검정 알약 스타일로 위/아래 대칭 배치. 점은 흰 점 대신 `conic-gradient`로 만든 미니 색상환을 3초 주기로 회전시켜서, 텍스트뿐 아니라 시각적으로도 "색"이라는 내용이 바로 연상되게 함.
- 기존 `#intro` 클릭 핸들러(`applyPalette(paletteIndex + 1)`)나 5줄 마퀴 렌더링 로직은 전혀 안 건드림 — 순수 안내 UI만 추가.
- **검증**: jsdom으로 `.color-cue`가 `#intro` 안에 정상 삽입되고 기존 `.scroll-cue`는 그대로인지 확인. 클릭 시 팔레트가 실제로 바뀌는지도 확인하려 했는데, 이 페이지 스크립트 안의 무관한 `IntersectionObserver` 두 곳이 jsdom에서 미구현이라 스크립트 실행이 그 지점에서 멈춰버려(그 아래 줄에 있는 `#intro` 클릭 리스너 등록 자체가 실행 안 됨) 처음엔 오탐(false)이 나왔음 — `IntersectionObserver`를 더미로 스텁한 재검증에서 클릭 시 팔레트가 정상적으로 바뀌는 것을 확인해서, 실제 버그가 아니라 이 세션 전부터 있던 jsdom 환경 한계임을 확인.
- **실제 시각 확인(상단 힌트 배지 위치·색상환 회전 느낌, 로고 클릭 후 스크롤 리셋 체감)은 브라우저에서 직접 확인 필요.** 커밋/배포 안 함.

## 세션 요약 — 리스트 배경 동영상 프리로드(선택 즉시 재생) + 하단 바를 리스트 박스와 완전 분리 (2026-08-10, 계속)

바로 위 세션의 디바운스+크로스페이드로도 "동영상 로딩이 심하다"는 피드백 — 근본 원인은 그 방식이 여전히 **호버한 시점에 그제서야** 새 Vimeo iframe을 만들기 시작했다는 것(디바운스는 로딩 횟수만 줄였지 로딩 자체를 없애진 못함). 레퍼런스 영상처럼 선택 즉시 재생되려면 선택 시점이 아니라 **목록을 여는 시점에 전부 미리 깔아둬야** 함.

- **`buildListBgLayers(list)`**: 목록을 열 때(또는 필터 전환 시) 그 목록에 있는 프로젝트 전부에 대해 `.list-bg-layer`(각각 자기 프로젝트의 Vimeo 배경 임베드 또는 `<img>`)를 한 번에 미리 생성해서 `#list-bg-media`에 전부 넣어둠. 전부 `autoplay=1`이라 `opacity:0`으로 안 보이는 상태에서도 이미 로딩·재생이 진행되고 있음.
- **`setListPreview()`는 이제 새로 만들지 않고 opacity만 토글**: 마우스를 올리면 해당 인덱스의 레이어에 `.is-active`(opacity:1)만 붙이고 나머지는 뗌 — 이미 재생 중이던 영상을 그대로 드러내는 것이므로 로딩 없이 즉시 재생됨. 기존의 크로스페이드용 `load` 이벤트 대기, 550ms 이전 레이어 정리, 160ms 디바운스 타이머를 전부 제거(더 이상 필요 없음 — 방금 만든 레이어가 아니라 이미 떠 있던 레이어를 고르는 것뿐이라 지연시킬 이유가 없음).
- 트레이드오프를 사용자에게 공유할 필요: 목록을 여는 "그 순간"에 목록 안의 프로젝트 수만큼 Vimeo 임베드가 동시에 로드되기 시작함(현재 WORK/PLAY 각 카테고리 프로젝트 수 기준 — 필터 전환 시에도 그 필터의 항목들을 다시 한 번에 프리로드). 개별 영상은 Vimeo의 `background=1` 저해상도 모드라 가볍지만, 리스트를 여는 첫 순간의 체감 로딩은 오히려 늘 수 있음(선택할 때마다 조금씩 로딩되던 게, 여는 순간 한 번에 몰림) — "선택하면 바로 재생"을 위해 의도적으로 선택한 트레이드오프.
- **하단 바(Close/이름/화살표)를 리스트 박스와 완전히 분리**: "레퍼런스처럼 리스트 탭이랑 아예 분리해달라"는 재요청 — 기존엔 `.list-footer`가 `.list-panel`(둥근 박스) *안의* 마지막 섹션이라 사실상 같은 박스 하나로 보였음. `.list-panel`과 `.list-footer`를 감싸기만 하고 자기 배경은 없는 `.list-stack` 래퍼로 묶고, `.list-footer`를 `.list-panel` *밖으로* 꺼내 형제 요소로 만들어서, 이제 리스트 박스(둥근 다크 패널)와 하단 바가 서로 다른 배경/테두리를 가진, 위아래로 살짝 떨어진 별개의 떠 있는 패널 두 개로 보임(레퍼런스와 동일한 레이아웃).
- **검증**: jsdom으로 (1) 목록을 열면 그 즉시(대기 없이) 행 개수만큼 배경 레이어가 전부 만들어지고 첫 항목만 active인지, (2) 다른 행에 마우스를 올리면 대기 없이 즉시 active 레이어가 바뀌고 레이어 개수는 그대로인지(재로드 없음 확인), (3) `.list-stack`이 `.list-panel`과 `.list-footer`를 둘 다 담고 있지만 `.list-panel` 안에는 `.list-footer`가 없고 그 반대도 아닌지(구조적 분리 확인), (4) 닫으면 레이어가 전부 정리되는지, (5) 기존 위/아래 이동·wrap·CLOSE 동작이 여전히 정상인지까지 확인 — 전부 통과.
- **실제 시각 확인(체감 로딩 정도, 리스트/하단 바가 실제로 분리돼 보이는 간격, 필터 전환 시 재로딩 체감)은 브라우저에서 직접 확인 필요.** 커밋/배포 안 함.

## 세션 요약 — 로고/헤더 배경 대비 수정 + 리스트 하단 바 분리·다크 반전·동영상 로딩 크로스페이드 (2026-08-10, 계속)

**1) 헤더 nav / jjaejae 로고가 배경에 안 맞게 흐리게 보이던 문제**
스크린샷 피드백: 좌우 하단 날짜/시계(`#frame-day`/`#frame-clock`)는 배경에 맞춰 항상 선명한데, 좌상단 jjaejae 로고와 우상단 WORK/PLAY/INFO 네비게이션은 어두운 배경 위에서 흐릿하게 보임.
- **로고**: `jjaejae-wordmark-black.png`가 블렌드 모드 없는 순수 검정 이미지라서, 날짜/시계가 쓰는 `mix-blend-mode:difference` 기법(흰 글자 + difference 블렌드 = 어떤 배경에서도 자동 대비)을 그대로 못 씀 — 검정 소스에 difference를 걸면 배경과의 차이가 0이라 오히려 완전히 안 보이게 됨(이게 기존 코드에 남아있던 주석에 설명돼 있었음). 새 흰색 에셋을 안 만들고 `filter:invert(1)`로 이미지를 흰색으로 뒤집은 다음 `mix-blend-mode:difference`를 걸어서, 날짜/시계와 똑같은 방식으로 자동 대비되게 함. Info 페이지(배경이 컬러풀한 사진)에서는 기존처럼 `filter:none; mix-blend-mode:normal`로 원래의 솔리드 블랙으로 되돌리는 예외를 frame-item과 동일하게 추가.
- **헤더 nav**: 코드 주석에 "Work 리스트에서는 또렷하게"라고 명시돼 있었는데 실제 선택자에는 `body.list-open`이 40%-옅게 규칙에 섞여 들어가 있어서 주석과 실제 동작이 어긋나 있었음(진짜 버그) — `list-open`을 그 규칙에서 빼고 `opacity:1`로 항상 또렷하게 나오도록 수정. project-open/info-open의 40%-옅게 동작은 그대로 유지.

**2) 리스트 하단 바를 레퍼런스처럼 분리된 알약(chip) 3개로 재구성**
기존엔 CLOSE/이름/카운트/위아래 버튼이 하나의 `.list-footer` 바에 텍스트만 나열돼 있었음 — 레퍼런스 영상처럼 서로 분리된 떠 있는 박스로 보이도록 `.list-chip` 클래스를 만들어 (1) CLOSE 버튼(흰 배경 필박튼), (2) 이름+"N/전체" 카운트(반투명 다크 칩), (3) 위/아래 버튼(반투명 다크 칩)을 각각 독립된 배경/테두리/둥근모서리를 가진 요소로 분리.

**3) 리스트 패널 컬러 반전 — 화이트 텍스트가 메인**
`.list-panel` 배경을 라이트 프로스티드(`rgba(255,255,255,.4)`)에서 다크 프로스티드(`rgba(10,10,10,.42)`)로 반전, 제목/필터/행 텍스트를 전부 화이트 계열로 바꿈. 강조(호버/active) 상태도 레퍼런스와 동일하게 "채워진 화이트 배경 + 검정 텍스트"로 반전(기존엔 반대였음 — 채워진 블랙 배경 + 흰 텍스트).

**4) 동영상 배경 전환 시 로딩이 덜 보이게 (크로스페이드 + 디바운스)**
"이 동영상들 로딩 없이 나오게 할 수 있나" 요청 — 두 가지 기법을 함께 적용:
- **크로스페이드**: 배경 미디어를 `innerHTML`로 통째로 갈아끼우던 방식(즉시 사라짐→새로 로드) 대신, 새 `.list-bg-layer`를 이전 레이어 위에 얹고 `opacity:0`으로 시작 → 실제 `<img>`/`<iframe>`의 `load` 이벤트가 뜨면 `.is-ready`(opacity:1)로 페이드인, 이전 레이어는 페이드가 끝날 시간(550ms)만큼 그대로 남겨뒀다가 제거. 즉 다음 영상이 뜨는 동안 화면이 까맣게 비지 않고 이전 화면이 계속 보임.
- **디바운스**: 마우스가 목록 위를 훑고 지나갈 때마다 매번 새 Vimeo iframe을 로드하면 로딩이 계속 겹쳐 보이므로, 호버 시 실제 배경 교체는 160ms 지연 후에만 실행(짧게 스쳐 지나간 항목은 아예 로드 자체를 안 함). 강조 표시(active 행)와 하단 이름/카운트는 디바운스 없이 즉시 갱신되어 반응성은 그대로 유지.
- 참고로 실제 Vimeo Player 준비 완료 이벤트(postMessage 기반)까지는 안 썼음 — iframe의 `load`는 플레이어 문서가 로드된 시점이라 실제 영상 프레임 재생 시작보다는 약간 이를 수 있지만, 별도 SDK 없이 체감 개선 효과가 커서 이 정도로 충분하다고 판단.
- **검증**: jsdom으로 (1) 목록을 열자마자는 배경이 비어있다가(디바운스) 일정 시간 후 채워지는지, (2) 여러 행에 빠르게 연속 mouseenter를 발생시켜도 그 순간엔 레이어가 쌓이지 않는지, (3) 다 멈춘 뒤엔 마지막 항목만 남고 이전 레이어들이 정리되는지, (4) CLOSE/이름/위아래가 각각 DOM상 별개 요소인지, (5) 기존 위아래 이동/wrap/닫기 동작이 여전히 정상인지 재확인 — 전부 통과. `scrollIntoView`/vimeo iframe 네트워크 콘솔 에러는 이전과 동일하게 jsdom 환경 한계.
- **실제 시각 확인(크로스페이드 타이밍 체감, 다크 패널 대비, 로고 대비 등)은 브라우저에서 직접 확인 필요.** 커밋/배포 안 함.

## 세션 요약 — WORK/PLAY 리스트 패널 반투명화 + 색점 제거 + 하단 바(CLOSE/이름/N of M/위아래) 추가 (2026-08-10, 계속)

바로 위 세션에서 만든 몰입형 리스트에 대한 다듬기 요청 3가지:
- **패널 반투명도 강화**: `.list-panel` 배경을 `rgba(255,255,255,.86)`(거의 불투명) → `rgba(255,255,255,.4)`로 낮추고 blur를 20px→26px로 올림, 테두리(`rgba(255,255,255,.5)`)도 살짝 추가해서 반투명 유리 느낌 강화 — 레퍼런스 영상처럼 뒤 배경 미디어가 패널을 통해 비쳐 보임.
- **프로젝트명 옆 색점 제거**: `projectCard()`에서 `<span class="dot">` 마크업과 관련 CSS(`.work-card .meta .name .dot`) 삭제.
- **하단 바 추가** (레퍼런스의 CLOSE / 현재 재생 중인 이름 / "N /M" / 위아래 화살표를 그대로 재현): `.list-panel` 안에 `.list-footer`를 새로 넣어 (1) CLOSE 버튼(기존 `[data-go="home"]` 델리게이션을 그대로 재사용해서 별도 JS 없이 홈으로 나감), (2) 현재 배경에 재생 중인 프로젝트명(`#list-current-name`), (3) "N / 전체개수" 카운트(`#list-current-count`), (4) 위/아래 원형 버튼(`#list-nav-up`/`#list-nav-down`)을 배치.
  - 위/아래 버튼은 `navListPreview(dir)`이 현재 미리보기 인덱스(`listIndex`)를 기준으로 필터링된 목록(`listItems`) 안에서 이전/다음 항목으로 순환 이동(맨 위에서 위로 가면 맨 아래로 wrap) — 배경 미디어·강조 행·하단 이름/카운트를 모두 같이 갱신하고 `scrollIntoView`로 해당 행을 리스트 안에서 보이게 함.
  - `setListPreview(p, rowEl, index)`가 이제 인덱스도 받아서 하단 카운트 텍스트까지 같이 관리하도록 확장(호버로 미리보기를 바꿀 때도 `projectCard`가 자기 인덱스를 같이 넘김). `renderGrid()`가 필터링된 배열을 `listItems`에 저장해서 `navListPreview`가 필터(태그) 전환 후에도 정확한 범위 안에서 순환하도록 함. `closeList()`도 `listItems`/`listIndex`/하단 텍스트까지 전부 리셋하도록 확장.
- **검증**: jsdom으로 (1) 행에 `.dot`이 하나도 없는지, (2) 목록을 열면 하단 이름/카운트가 첫 항목("1 / N")과 일치하는지, (3) 아래 화살표 클릭 시 2번째 행이 active로 바뀌고 하단도 "2 / N"으로 갱신되는지, (4) 위 화살표로 되돌아오는지, (5) 맨 처음에서 위 화살표를 누르면 마지막 항목으로 wrap되는지("N / N"), (6) 하단 CLOSE 버튼을 눌러 실제로 리스트가 닫히고 배경/이름/카운트가 전부 비워지는지까지 실제 DOM 이벤트로 확인(전부 통과). `scrollIntoView`/vimeo iframe 네트워크 관련 콘솔 에러는 jsdom 환경 자체의 한계(실제 브라우저는 둘 다 정상 지원)라 무시함.
- **실제 시각적 확인(반투명 유리 느낌 정도, 하단 바 레이아웃, 위아래 버튼 아이콘 크기 등)은 사용자가 브라우저에서 직접 확인 필요.** 커밋/배포 안 함.

## 세션 요약 — WORK/PLAY 리스트를 몰입형 동영상 레퍼런스 방식으로 교체 (2026-08-10, 계속)

- 요청: 첨부한 화면 녹화(Vimeo Showcase 위젯 — 전체화면 배경 동영상이 항상 재생되고, 오른쪽에 반투명 리스트 패널이 떠서 항목을 고르면 배경이 그 동영상으로 바뀌는 방식) 같은 걸 프로젝트 리스트에 적용하고 싶다는 요청. "메인페이지부터 바꿀지 vs Work/Play 탭에서만 적용할지" 먼저 상의 → 메인은 지금 의도적으로 조용한 인사말+WORK/PLAY 텍스트 링크뿐이라 톤이 깨지고 로딩 부담도 커진다고 추천드렸고, **Work/Play 탭(`#list-view`)에만 적용하기로 확정**. 추가로 "프로젝트 키 이미지가 동영상이면 동영상, 이미지면 이미지로 배경에 나오면 좋겠다"는 요청.
- 기존엔 `#list-view`가 2열 masonry 그리드(`work-card` 썸네일 카드들)였음 — 이걸 통째로 교체하지 않고, **전체화면 배경 미디어 레이어(`#list-bg-media`) + 그 위에 뜨는 프로스티드(반투명 블러) 리스트 패널(`list-panel`)** 구조로 다시 짬. 리스트 패널 안의 각 행(`.work-card`, 클래스명은 재사용)은 작은 썸네일+제목+색점+태그만 있는 컴팩트한 줄로 바뀌었고, 마우스를 올리면(desktop hover) 배경 미디어가 그 프로젝트로 바로 바뀜(`setListPreview()`). 클릭하면 기존과 동일하게 프로젝트 상세페이지로 이동(`goProject`) — 미리보기와 이동을 분리.
- **"동영상이면 동영상, 이미지면 이미지" 배경 전환 로직**은 이미 프로젝트 상세페이지 히어로(`.p-hero`, `heroMedia` 변수)에서 쓰던 것과 완전히 동일한 규칙을 재사용: `project.heroType === "video"`면 Vimeo 배경 임베드(`autoplay=1&loop=1&muted=1&background=1`), 아니면 `project.hero` 정지 이미지. 새 필드 추가 없이 기존 `heroType`/`heroVideoId`/`hero` 데이터를 그대로 씀.
- 목록을 열면 기본으로 첫 번째 항목이 배경에 재생되고(`.active` 표시), 필터 탭(All/태그별)을 바꾸면 필터링된 목록의 첫 항목으로 미리보기가 리셋됨. 목록을 닫으면(`closeList`) 배경 미디어를 비워서 안 보이는 Vimeo iframe이 계속 재생되며 리소스를 먹지 않도록 함.
- 기존 masonry 2열 분배 코드(`numCols`/`.list-col`)는 새 세로 리스트 구조에 필요 없어져서 제거. `.work-card`의 "호버해야 이름 보이는" 방식도 컴팩트 리스트 행에는 안 맞아서 항상 이름이 보이는 걸로 바꿈(대신 호버/active 시 채워진 다크 배경으로 강조).
- `builder.html`은 원래 WORK/PLAY 리스트 페이지 자체를 미리보기하지 않는 구조(프로젝트 상세/INFO 프리뷰만 있음)라 이번 변경은 index.html에만 해당, builder.html은 건드릴 필요 없었음.
- **검증**: `node --check`로 index.html의 `<script>` 전체 문법 확인. 이 환경은 실제 브라우저 자동화가 안 되므로, 로컬 `python3 -m http.server`로 index.html을 띄운 뒤 jsdom으로 실제 WORK 클릭 → 리스트 열림/기본 미리보기(첫 항목 active) → 두 번째 행에 mouseenter → active가 옮겨가고 배경 미디어가 갱신되는지 → PLAY로 전환 시 재렌더 → 홈으로 나가면 배경 미디어가 비워지는지까지 실제 DOM 이벤트로 확인(전부 통과). **실제 시각적 확인(패널 블러 느낌, 동영상 실제 재생, 모바일 레이아웃)은 사용자가 브라우저에서 직접 봐야 함.**
- 커밋/배포 안 함 — 로컬에서 확인 후 원하는 대로 다듬고 나서 git 반영 필요.

## 세션 요약 — INFO 배경 사진 PC/모바일 분리 (2026-08-10)

- 요청: "빌더 info 페이지 수정할때 배경 이미지를 PC하고 모바일 분리해서 지정할수 있으면 좋겠어. 모바일에서 info를 보니까 비율이 안맞아서." 지금까지 INFO 배경은 `info-bg.jpg` 하나뿐이라 PC 기준 크롭(`center 35%`)이 모바일 세로 화면에서는 원하는 부분이 안 보이는 문제였음.
- **`server.py`**: `save_info_bg_image()`가 `payload.target`("pc"|"mobile", 기본 pc)을 보고 `info-bg.jpg` 또는 새로 추가한 `info-bg-mobile.jpg` 중 하나에만 저장하도록 분기. 엔드포인트(`/upload-info-bg`) 자체는 안 바뀜(페이로드에 필드만 추가).
- **`index.html`**: `#info-view .info-bg` 배경 이미지에 `@media (max-width:760px)` 오버라이드 추가 — 이 브레이크포인트는 다른 반응형 규칙들과 동일하게 이미 쓰이던 값. 좁은 화면에서는 `info-bg-mobile.jpg`를 씀.
- **`builder.html`**: INFO 편집 화면의 "배경 사진" 섹션을 "배경 사진 (PC)"/"배경 사진 (모바일)" 두 섹션으로 분리, 각각 독립적인 파일 첨부 버튼·썸네일·캐시버스트 버전(`infoBgVersion`/`infoBgMobileVersion`) 보유. 우측 프리뷰(`buildInfoPreviewBodyHTML`)도 PC/Mobile 토글 상태(`currentView`)에 따라 두 배경 중 맞는 쪽을 보여주도록 수정 — 모바일 토글로 전환하면 모바일용 배경이 바로 미리보기에 반영됨.
- **마이그레이션**: 기존엔 `info-bg-mobile.jpg` 파일이 아예 없어서 그대로 두면 모바일에서 배경이 깨짐 — 배포 전에 기존 `info-bg.jpg`를 그대로 복사해 `info-bg-mobile.jpg`를 만들어둠(사용자가 새 모바일 전용 사진을 첨부하기 전까지는 기존 PC 사진과 동일하게 보여서 당장 깨지지 않음).
- **검증**: `server.py`를 직접 import해서 `save_info_bg_image({..., target:"mobile"})` / `target:"pc"`를 각각 호출 — PC 파일은 안 건드리고 모바일 파일만 바뀌는지, 반대도 마찬가지인지 바이트 비교로 확인 후 원본 파일 복원. `node --check`로 builder.html/index.html의 `<script>` 블록 문법도 확인. **실제 브라우저 검증은 못 함**(이 환경은 브라우저 자동화 불가) — 사용자가 `python3 server.py` 실행 후 빌더에서 INFO 편집 → PC/모바일 각각 새 사진 첨부 → 우측 프리뷰 PC/Mobile 토글로 각각 확인 → PUBLISH까지 직접 확인 필요.
- 로컬 서버가 켜져 있었다면 이번 `server.py` 변경을 반영하려면 재시작 필요(이번 세션 확인 시점엔 실행 중 아니었음).

## 세션 요약 — 업로드 이미지 자동 웹 최적화(긴 변 2000px 제한) (2026-08-04)

- 요청: "동영상 웹최적화 한것처럼 이미지도 최적화됐으면 좋겠다, 원본 화질은 너무 떨어뜨리지 말고 2000px 정도로." 소급 적용이 아니라 **앞으로 업로드하는 것부터** 적용해달라는 것으로 확인.
- `server.py`에 `_resize_cap()`/`_resize_cap_frames()` 헬퍼 추가 — 긴 변이 2000px(`MAX_IMAGE_DIM`)을 넘으면만 비율 유지한 채 축소(그 이하는 손대지 않음). 애니메이션 GIF는 프레임별로 동일하게 리사이즈해서 애니메이션 보존.
- 적용 지점: `save_project_image`(히어로 이미지 업로드, 그리고 이번에 새로 block 사진 업로드도 이 함수를 타도록 함), `crop_image`(크롭 결과물도 동일 상한 적용).
- **부수적으로 발견한 실제 버그도 같이 고침**: 블록에 사진 추가할 때 "사진 추가" 버튼(일반 파일 선택)/드래그앤드롭으로 넣은 파일은 지금까지 서버에 실제로 업로드된 적이 없었음 — `relPath`만 추정해서 기록하고 실제 바이트는 브라우저 메모리(blob)에만 있었던 것. 폴더째로 불러오기(webkitRelativePath 있는 경우)는 이미 디스크에 있는 파일이라 문제 없었지만, 일반 선택/드롭은 원본 파일이 프로젝트 폴더 밖에 있으면 Publish 후 404가 날 수 있는 잠재 버그였음. 이번 최적화 작업 김에 히어로 이미지와 동일한 `persistImageFileToDisk()` 패턴(구 `persistHeroFileToDisk`를 일반화)으로 서버에 실제로 저장하도록 고쳐서, 이 클래스의 버그도 같이 막음.
- **검증**: 서버에 직접 3200x2100 JPG를 올려 2000x1333으로 정확히 축소되는지, 2400x1600 애니메이션 GIF(3프레임)가 2000x1333으로 축소되면서 3프레임 모두 보존되는지 raw HTTP 요청으로 확인. 실제 빌더에서 큰 이미지를 "사진 추가"로 올려 IndexedDB에 서버 confirmed relPath로 반영되고(blob 아님), 디스크 파일도 정확히 리사이즈된 것까지 Playwright로 종단 확인.
- **커밋 `af5ace5`는 로컬에만 있고 아직 push 안 됨** — 이 세션 끝나갈 때 네트워크가 완전히 끊겨서(1.1.1.1도 응답 없음, 이 세션 초반에도 한 번 있었던 것과 동일 증상) push가 계속 실패했고, 사용자가 맥북을 재시동해야 해서 세션을 여기서 마무리함. **다음 세션 시작 시 가장 먼저 `git push origin main` 재시도할 것.**

## 세션 요약 — 비핸스 프로젝트 수동 가져오기(ministock) + 사진/동영상 순서 버그 + 크레딧 순서 변경 기능 + WORK/PLAY 리스트 확장 (2026-08-03, 계속)

**1) 비핸스(Behance) 프로젝트를 빌더로 가져오기 — "ministock" 프로젝트**
- 요청: 비핸스에 정리해둔 프로젝트를 빌더로 가져올 수 있는지 문의. 완전 자동화(URL 입력→자동 채움 기능)는 비핸스가 정식 공개 API가 없어 비공식 스크래핑에 의존해야 하고, 이미지-블록 배치는 결국 사람이 판단해야 해서 자동화 이득이 적다고 판단 — 1회성으로 내가 직접 내용을 가져와 채워주는 방식으로 진행하기로 함(사용자 확인).
- 텍스트(제목/설명/문단/크레딧 26개)는 WebFetch로 비핸스 페이지에서 가져옴. 이미지는 이 작업 환경이 외부 네트워크가 완전히 막혀있어(curl로 구글도 접속 불가 확인) 내가 직접 다운로드할 수 없었음 — 사용자가 직접 7장을 받아 `Ministock 2021/` 폴더에 저장(webp로 저장됨).
- **중요 이슈**: 처음엔 내 자동화 테스트용 브라우저(Playwright)의 IndexedDB에만 프로젝트를 써넣었는데, 이건 사용자가 실제로 쓰는 브라우저와 완전히 별개의 저장소라 빌더에 안 보이는 문제 발생. 콘솔 붙여넣기는 이전 세션에 계속 깨졌던 전례가 있어, 이번에도 `builder.html`의 `boot()`에 있는 자동 마이그레이션과 같은 방식으로 임시 시드 코드를 넣어 "새로고침 한 번"으로 사용자의 실제 브라우저에 데이터가 채워지도록 함 → 확인 후 시드 코드는 바로 제거(일회성 데이터 주입이라 반복 실행될 필요가 없어서, NFC/relPath 리페어 마이그레이션과 달리 영구적으로 남겨두지 않음).
- 사용자가 직접 확인 후 Publish + 순서 반영까지 진행함(커밋 `126ed3e`, `1961512`, `47b37c9`, `8dd9505`).

**2) 블록 안에서 사진/동영상이 뒤섞일 때, 실제 순서를 무시하고 항상 동영상이 먼저 뜨던 버그**
- 기존엔 (이전 세션에 "동영상은 레이아웃과 무관하게 항상 사진 위에 고정 표시" 설계를 일부러 넣었었는데) 사진을 동영상보다 먼저 넣어도 무조건 동영상 스택이 위로 가게 하드코딩돼 있었음 — 사용자가 사진을 위로 두었는데 동영상이 먼저 나오는 걸 오류로 지적.
- 수정: 사진/동영상을 여전히 서로 다른 레이아웃 계산에서 분리해서 다루는 기존 설계는 유지하되(크기 계산에 섞이지 않음), 원본 이미지 배열에서 사진과 동영상 중 어느 쪽이 먼저 나오는지를 비교해서 그 순서대로 두 스택(동영상 스택 vs 사진 갤러리)을 쌓도록 변경. 기존 프로젝트(Neo QLED 8K 등, 동영상을 배열 맨 앞에 넣어둔 경우)는 그대로 동영상이 먼저 나오고, 사진을 먼저 넣은 새 블록만 사진이 먼저 나옴. index.html·builder.html 양쪽 반영, Playwright로 두 순서 케이스 모두 검증.
- 참고: 이 수정 코드가 index.html에 반영된 시점과 사용자가 "순서·숨김 반영" 버튼을 누른 시점이 겹쳐서, `git add -A` 방식 때문에 이 수정이 사용자의 "Publish: Sync project order" 커밋에 같이 딸려 들어감(동작엔 문제 없음, 커밋 메시지만 안 맞음 — [[project_2026_portfolio_publish_git_add_a]] 참고).

**3) 크레딧 순서를 드래그로 바꿀 수 있는 기능 추가**
- 기존 블록/사진 리스트에 이미 있던 `[data-sort-item]`/`[data-sort-scope]` 드래그 정렬 메커니즘을 크레딧 목록에도 재사용. 크레딧은 `[라벨, 값]` 튜플이라 고유 id가 없어서, 렌더링 시점의 배열 인덱스를 담은 합성 id(`credit-<index>`)를 부여하고, 드롭 시 그 id로 드래그 전 위치를 역추적해서 배열을 재구성 → 이후 바로 `renderEditor()`로 다시 그려서 라벨/값 입력창의 인덱스가 새 순서와 어긋나지 않게 함(재렌더 없이 두면 입력 시 엉뚱한 크레딧이 수정되는 버그가 생길 뻔했음 — Playwright로 실제 DragEvent를 직접 dispatch해서 드래그 후 편집까지 인덱스가 정확히 맞는지 검증).

**4) WORK/PLAY 리스트 카드 영역 가로폭 확장**
- `#list-view .list-inner`의 `max-width`를 1400px → 1600px → (한 번 더 요청받아) 1800px로 확장. 두 컬럼 masonry 레이아웃이 안 깨지는지 각 단계마다 Playwright 스크린샷으로 확인 후 커밋.

**커밋**: `126ed3e`/`1961512`/`47b37c9`/`8dd9505`(ministock Publish + 순서 반영, 사용자가 직접 실행), `516991f`(크레딧 드래그 정렬 추가 — 사진/동영상 순서 수정은 위 이유로 `47b37c9`에 이미 포함됨), `24a086e`(리스트 1600px), `b866056`(리스트 1800px). 전부 push 완료.

## 세션 요약 — NIKE 프리그리드 겹침/블랙갭/크롭 연쇄 버그 수정 + GIF STICKERS 크기 축소 (2026-08-03, 계속)

직전 세션에서 만든 "직접 지정 그룹핑" + 아이스크림 홈런 NFC 수정을 사용자가 실제 라이브 사이트에서 검증하다가 NIKE 프로젝트에서 새 버그를 여러 건 연달아 발견 — 하나씩 원인 추적해서 고침. 이 과정에서 재발했던 아이스크림 홈런 NFC 문제도 이번엔 프로젝트 한정이 아니라 전체 프로젝트 대상 자동 마이그레이션으로 완전히 durable하게 막음.

**1) NIKE "Result" 블록 2장 겹쳐 보임**
- 원인: 블록의 `freeLayout` 배열 마지막 항목이 이전에 다른 장수(9장 3x3)로 편집했을 때 남은 좌표(`x/y/w/h`)를 그대로 갖고 있어서, 실제 6장짜리 2x3 배치와 안 맞아 한 칸이 비고 다른 사진이 그 위에 겹쳐 렌더링됨. 좌표값 하나만 직접 정정.

**2) Free 모드 모아보기 블랙 갭 → object-fit:cover로 시도 → 크롭 규제션 → 최종 justified-grid 알고리즘**
- 처음엔 `object-fit:cover`로 바꿔 갭을 없앴더니 이번엔 실제 이미지 내용(텍스트/UI)이 잘려 보이는 규제션 발생 — 사용자가 즉시 지적해서 `contain`으로 원복.
- 최종 해결: free 모드 중 "균일한 폭 그리드"로 판별되는 블록(`mode-free-grid`)에 대해, 한 행에 들어가는 사진들의 실제 종횡비 합으로 그 행의 높이를 역산해서 **행 폭을 정확히 꽉 채우는** `layoutFreeGrid()` 알고리즘을 새로 작성 — 크롭도 갭도 전혀 없이 항상 가로폭에 딱 맞게 채워짐(각 사진의 실제 비율 그대로 유지). index.html/builder.html(실 코드+PREVIEW_RUNTIME_JS) 3곳 모두 반영.

**3) 커스텀 그룹 페이지가 과도하게 커 보임 (baseHeight 오염 버그)**
- 원인: `gallery.dataset.baseHeight = gallery.clientHeight || 280` 코드가 `height:auto`인 갤러리(다중 페이지/free-grid)에서는 안전하지 않음 — JS가 사진 크기를 계산해 넣기 전 최초 측정 시점엔 이미지가 자기 원본 픽셀 크기로 브라우저에 렌더링돼 있어서, 그 순간의 `clientHeight`가 그 갤러리의 "기준 높이"로 영구 고정돼버림(실제 확인된 사례: 사진 원본 높이 1750px가 그대로 baseHeight로 박제되어 페이지가 1725px 높이로 렌더링됨). `mode-1`(단일 슬라이드형)은 CSS에 명시적 높이(`60vh`, `max-height:640px`)가 있어서 이 방식이 안전하지만, 다중 페이지/free-grid는 아님.
- 수정: DOM 측정에 의존하지 않는 `defaultBaseHeight()`(= `max(280, min(640, 뷰포트높이*0.6))`, 1장짜리 갤러리 CSS 공식을 JS로 그대로 재현)를 만들어 다중 페이지 레이아웃 계산에 사용하도록 교체.

**4) 다음 페이지로 넘어갈 때 이전 사진이 살짝 겹쳐 보임 (프레임 밀림 버그)**
- 원인: `.gallery.mode-multi .gallery-track{gap:2px}` — 예전엔 안 보이던 프레임을 `display:none`으로 숨겼어서 갭이 몇 개 프레임 사이에서만 적용돼 무해했지만, 이번 세션에 추가한 슬라이드 애니메이션은 모든 프레임을 항상 화면에 나열해두는 구조라 프레임 개수만큼 2px 갭이 계속 누적됨. 반면 JS가 계산하는 슬라이드 이동 거리는 갭이 0이라고 가정하고 계산해서, 페이지가 넘어갈수록 실제 위치와 계산 위치가 어긋나(예: 3번째 경계에서 6px 오차) 갤러리 클리핑 경계 밖으로 옆 프레임이 살짝 삐져나와 보였음. `gap:2px` 규칙 자체를 제거해서 해결.
- (테스트 중 처음엔 자체 제작한 크롭 검사 스크립트가 "805px 크롭"이라는 오탐을 냈는데, 이 스크립트가 커스텀 그룹(가변 장수)을 고려 안 하고 `page*perPage`로 프레임을 나눠서 생긴 착시였음 — 실제 화면 좌표 기준(`getBoundingClientRect`)으로 다시 짠 스크립트로 재검증해서 오탐임을 확인하고 넘어감.)

**5) NIKE "GIF STICKERS" 블록만 유독 너무 커 보임**
- 원인: `layoutFreeGrid()`가 항상 "행 폭을 정확히 채운다"는 방식이라 `높이 = 가용폭 / 그 행 사진들 비율의 합`으로 계산되는데, 이 식에는애초에 baseHeight가 아예 안 들어가서(계산상 소거됨) 처음엔 그리드를 작게 만들 방법 자체가 없었음. 정사각형에 가까운 GIF 스티커가 3행으로 쌓인 이 블록만 유독 세로 총합이 ~1448px까지 자라버림.
- 수정: 각 행의 "꽉 채운" 자연 크기를 먼저 계산해서 총 높이(`naturalTop`)를 구하고, 이게 다른 갤러리들과 동일한 상한선인 640px을 넘으면 `scale = 640/naturalTop`으로 모든 행·사진의 폭/높이를 똑같은 비율로 축소(비율 유지, 크롭·갭 없음 그대로 보존)하고 그리드 박스 자체 폭도 줄여서 가운데 정렬되도록 함. 640px 이하인 다른 free-grid 블록(예: Result, 6장)은 이 로직이 개입 안 해서 그대로 유지됨(검증: 축소 전/후 각각 `1380x562`(영향 없음) vs GIF STICKERS `1380x1448`→`610x640`(축소 적용)).

**검증**: 매 수정마다 Playwright로 전 프로젝트/전 블록 스윕(크롭 0건/겹침 0건/삐져나옴 0건) 재확인 후 커밋. index.html/builder.html 양쪽 `<script>` 블록 문법 파싱도 매번 확인.

**커밋**: `f9ae4ec`(Result 블록 겹침 + free 모드 블랙갭 1차 수정), `57f0ede`(object-fit:cover 크롭 규제션 원복 + NFC 경로 수정을 전체 프로젝트 대상 범용 마이그레이션 `repairNfcPaths()`로 교체 — 재발 완전 차단), `3da06d6`(justified-grid `layoutFreeGrid` 알고리즘으로 크롭·갭 동시 해결), `2976ace`(baseHeight 오염 버그 + 프레임 밀림 gap:2px 버그 수정), `6334b7a`(GIF STICKERS 블록 640px 상한 축소). 전부 push 완료.

## 세션 요약 — 라이브 사이트 이미지 깨짐 근본 원인 2종 수정 + 갤러리 페이지 넘김 슬라이드 애니메이션 + 블록 사진 커스텀 그룹핑 (2026-08-03)

이번 세션 초반엔 지난 세션에서 고친 갤러리 크롭 버그를 마무리 보고했고, 이후 사용자가 퍼블리시 직후 라이브 사이트(jjaejae.com)에서 이미지가 계속 깨진다고 반복 제보 — 두 가지 서로 다른 근본 원인을 찾아 각각 코드 레벨에서 고쳤습니다. 그 다음 "사진 넘어갈 때 부드러운 효과"와 "블록 안에서 1장은 개별로, 뒤 3장은 묶어서 보이게 커스터마이징" 요청을 이어서 처리했습니다.

**1) SSF SHOP / Neo QLED 8K 이미지 경로 오류 (재발 방지까지 완료)**
- 원인: 히어로(썸네일) 이미지를 빌더에서 "이미지 변경" 버튼(단일 파일 선택 `<input type=file>`)으로 지정하면, 폴더 선택 picker와 달리 `webkitRelativePath`가 없어서 relPath가 폴더 없이 파일명만 남는 버그. 블록 사진 첨부 쪽은 이전 세션에 이미 고쳤지만 히어로 업로드 경로는 놓쳐 있었음.
- 코드 수정: `server.py`에 `/upload-project-image` 엔드포인트 신설 — 브라우저에서 고른 파일을 실제로 프로젝트 폴더에 디스크 저장하고, 그 결과 relPath를 돌려받아 기록(경로를 추측하지 않고 서버가 실제로 써서 보장). `builder.html`의 히어로 업로드 핸들러가 이걸 호출하도록 변경.
- 기존에 이미 깨져 있던 SSF SHOP(히어로+블록 이미지 4개)/Neo QLED 8K(gif 9개) 경로는 index.html에서 직접 정정 완료, 그리고 **콘솔 스크립트 붙여넣기가 계속 깨져서(IME/붙여넣기 이슈)** 결국 방식을 바꿔 `builder.html`이 로드될 때 자동으로 한 번 이 두 프로젝트의 IndexedDB 데이터를 고치는 마이그레이션 코드로 대체함(`boot()` 함수 안, `repairKnownBadRelPaths`) — 이제 브라우저 콘솔을 만질 필요 없이 빌더를 새로고침하기만 하면 됨.

**2) 아이스크림 홈런 전체 이미지 깨짐 (NFC/NFD 유니코드 정규화 불일치)**
- 원인: 이 프로젝트의 모든 이미지 경로 문자열이 index.html에 **유니코드 분해형(NFD, 자모 낱개)**으로 저장돼 있었는데, 실제 디스크/git의 파일명은 **결합형(NFC, 완성형 한글)**이었음. 시각적으로는 완전히 똑같아 보이고 macOS는 파일 시스템 조회 시 알아서 두 형태를 같은 걸로 처리해줘서 로컬(`file://`)에서는 전혀 문제가 없었지만, Vercel(Linux)은 바이트 단위로 정확히 일치해야 파일을 찾기 때문에 이 프로젝트 사진 전체(썸네일 포함)가 라이브에서만 깨졌던 것.
- 이 저장소에서 한글 폴더명을 쓰는 프로젝트가 "아이스크림 홈런"뿐이라 다른 프로젝트는 영향 없음(확인 완료).
- 수정: index.html 내 관련 문자열 30곳을 전부 NFC로 정규화. 재발 방지로 `builder.html`에 `nfc()` 헬퍼를 추가해서 파일 업로드로 만들어지는 모든 relPath/name(블록 사진 첨부, 히어로 업로드, 동영상 업로드)을 저장 전에 강제로 NFC 정규화하도록 함 — 앞으로 한글(또는 분해 가능한 유니코드) 파일명을 다시 추가해도 같은 문제가 재발하지 않음.

**3) 여러 장 갤러리("N장씩 보기") 페이지 넘김에 슬라이드 애니메이션 추가**
- 기존엔 2장/3장씩 보기 모드에서 클릭하면 사진이 뚝 끊기듯(`display:none`↔`block`) 바뀌었음(움직임 전혀 없음) — 대부분 프로젝트가 이 모드를 쓰고 있어서 체감상 가장 눈에 띄는 문제였음.
- 모든 사진 프레임을 항상 나란히 배치해두고(숨기지 않음), 클릭 시 `.gallery-track`의 transform을 슬라이드시켜 다음 페이지가 보이게(컨테이너는 `overflow:hidden`) 바꿈 — 1장짜리 갤러리가 이미 쓰던 것과 동일한 트랜지션 재사용. 컨테이너가 커지는 방향으로 높이가 바뀔 때는 트랜지션을 순간적으로 끄고 강제 리플로우 후 복원하는 방식(지난 세션 크롭 버그 수정과 동일 패턴)으로 잘림도 방지.
- 세로 방향(위아래로 넘기기) 모드는 이번 변경 대상에서 제외(사용자가 명시적으로 "좌우로"라고 표현했고, 세로 스택은 슬라이드시킬 축이 마땅치 않음) — 기존 방식(즉시 전환) 그대로 유지.
- index.html, builder.html(실제 코드 + PREVIEW_RUNTIME_JS 문자열 미러) 3곳 모두 반영. 전 프로젝트 Playwright 스윕으로 크롭 0건 확인.

**4) 블록 사진 "직접 지정" 그룹핑 (1장 개별 + 3장 묶기 같은 커스터마이징)**
- 기존엔 "N장씩 보기"를 고르면 블록에 첨부된 전체 사진이 무조건 N장 단위로 균일하게 나뉘는 구조라, "1장은 혼자 보이고 그다음 3장은 같이 보이게" 같은 걸 할 수 없었음.
- 빌더 사진 레이아웃 모달에 **"직접 지정"** 모드 추가 — 선택하면 블록의 모든 사진이 한 줄로 나열되고, 사진과 사진 사이에 클릭 가능한 구분선이 생김. 구분선을 클릭하면 인접한 두 사진이 합쳐지거나(같은 페이지) 나뉨(다른 페이지). 확정하면 블록에 `galleryGroups`(페이지별 장수 배열, 예: `[1,3,2]`)로 저장됨.
- (3)번에서 만든 슬라이드 페이지 넘김 로직을 그대로 재사용하도록 일반화 — `data-per-page` 고정값 대신 `getPageGroupSizes()`가 `data-groups`(직접 지정) 또는 균일 `perPage`(기존 2/3장씩 모드)를 알아서 판단해서 페이지 경계를 계산하도록 바꿔, 기존 고정 N장 모드도 코드 그대로 계속 동작함.
- index.html·builder.html(실제 렌더링 + PREVIEW_RUNTIME_JS) 모두 반영, `exportProjectCode`도 `galleryGroups` 내보내도록 수정. Playwright로 빌더 UI(모드 전환→구분선 토글→확정→재오픈 시 상태 복원→미리보기 iframe 반영)와 실제 렌더링(1+3 그룹 슬라이드, 크롭 없음)까지 전부 종단 테스트 완료.

**커밋**: `fc6ea7b`(SSF SHOP/Neo QLED 경로+히어로 업로드 근본 수정), `92730f5`(슬라이드 애니메이션+콘솔 스크립트→자동 마이그레이션 교체), `7f64928`(아이스크림 홈런 NFC/NFD 수정), `6258141`(직접 지정 그룹핑). 세션 종료 시점에 로컬 네트워크 문제로 마지막 커밋(`6258141`) push가 지연될 수 있음 — 다음 세션 시작 시 `git status`/`git log origin/main..HEAD`로 미푸시 커밋 있는지 먼저 확인할 것.

## 세션 요약 — 사이드바 프로젝트 항목을 2줄 레이아웃으로 재구성 (2026-07-31, 계속)

아이콘 전용으로 줄여도 여전히 겹쳐 보인다는 스크린샷 피드백 — 근본적으로 한 줄에 제목+카테고리+버튼 3개를 다 넣는 구조 자체가 문제였음. 사용자 제안대로 2줄 구조로 재구성.

- **`.project-item`를 세로 2줄로 분리**: 위쪽 줄(`.project-item-main`)엔 드래그 손잡이·색점·제목/카테고리·삭제(✕)만, 아래쪽 줄(`.project-item-actions`)엔 숨기기·Play 버튼만. 카드가 세로로 조금 길어지는 대신 제목이 다시 절대 안 잘림.
- **숨기기 버튼을 Play와 같은 알약(pill) 버튼 스타일로**: 아이콘 전용 스타일을 되돌리고 "👁 숨기기"/"🙈 보이기" 텍스트를 포함한, `→ Play`와 똑같은 테두리+둥근모서리 버튼으로 통일. 이제 공간 여유가 있어서 hover 없이도 항상 보임(전엔 좁아서 hover 시에만 나타났음).
- **버그 두 개 발견해서 고침**: (1) 현재 선택된(active) 프로젝트 행에서는 버튼 배경이 흰색으로 고정돼 있는데 글자색도 상속된 흰색이 되면서 흰 배경에 흰 글씨로 버튼 텍스트가 안 보이던 문제 — active 상태에선 배경을 투명으로 바꿔서 해결. (2) 그 상태에서 "숨김" 표시용 강조색(`--accent`)이 화이트/블루 테마에서는 `--fg`와 같은 색(거의 검정)이라, 숨겨진 프로젝트를 선택한 상태로 두면 검정 배경에 검정 글씨로 또 안 보이는 문제가 있었음 — "선택됨 + 숨김"이 동시에 겹치는 경우만 별도로 상속색을 쓰도록 예외 규칙 추가.
- **검증**: Playwright로 (1) 제목이 다시 안 잘리는지, (2) Play 버튼을 눌러 카테고리 이동이 여전히 정상 동작하는지, (3) 선택된 상태의 프로젝트에서 숨기기 버튼 텍스트 색상을 실제 계산값(computed style)까지 확인해서 흰 배경/검정 텍스트 등 각 겹침 케이스에서 전부 읽히는 색 조합이 나오는지 확인.

## 세션 요약 — 사이드바 숨기기 버튼이 프로젝트명 가리던 문제 수정 (2026-07-31, 계속)

바로 직전에 추가한 "👁 숨기기" 버튼이 텍스트 라벨("숨기기"/"보이기")까지 달고 있어서, 사이드바 한 줄에 이동(→ Play)·삭제(✕) 버튼까지 3개가 텍스트/아이콘 뒤섞여 자리를 다투다 보니 프로젝트명이 거의 안 보일 정도로 밀려남.

- **아이콘 전용으로 축소**: 텍스트 라벨을 빼고 👁/🙈 이모지 하나만 남김. `.del`(✕)과 같은 수준의 고정 18×18px 정사각형으로 만들어서 제목 영역(`flex:1`)을 거의 안 뺏도록 함.
- **"숨김" 배지 위치 이동**: 기존엔 프로젝트명과 같은 줄(`.p-title`)에 배지가 붙어있어서 제목 자체의 말줄임 여유가 줄었는데, 배지를 아래쪽 서브타이틀 줄(`.p-sub`, 카테고리·연도 옆)로 옮겨서 제목 줄은 온전히 제목만 쓰게 함.
- **검증**: Playwright로 일부러 아주 긴 프로젝트명을 넣고 확인 — 이제 제목이 잘려도 상당 부분 읽히고(전에는 거의 안 보임), 실제 프로젝트명처럼 짧은 제목("SSF SHOP" 등)은 아이콘 3개와 함께 있어도 전혀 안 잘림.

## 세션 요약 — 프로젝트 숨기기 토글 + 순서 반영 버튼이 숨김 상태도 같이 반영하도록 확장 (2026-07-31)

- **빌더 사이드바에 프로젝트별 "숨기기/보이기" 토글 추가**: 프로젝트 항목에 마우스를 올리면 "👁 숨기기" 버튼이 나타나고, 누르면 사이드바 목록에는 그대로 남아있되(흐리게 표시 + "숨김" 배지) 실제 사이트(jjaejae.com)에는 안 보이게 됨. 다시 누르면 "🙈 보이기"로 원상복구되는 단순 on/off 스위치.
- **숨김 상태가 사이트에서 정확히 반영되도록 index.html 3곳 수정**: (1) Work/Play 리스트(`renderList`)에서 필터링, (2) 프로젝트 직접 URL(`/project-id`) 접근 및 `openProject()`도 막음, (3) 프로젝트 상세페이지의 "이전/다음" 화살표 네비게이션이 숨겨진 프로젝트로는 절대 안 넘어가도록 `visibleProjects`만 순환하게 수정(관련해서 "다음 프로젝트가 1개 이하면 화살표 자체를 숨기는" 조건문도 전체 개수가 아니라 보이는 개수 기준으로 같이 수정).
- **"순서를 사이트에 반영" 버튼을 "순서·숨김 반영"으로 확장**: 사용자가 "순서 버튼을 눌러도 숨김 상태가 안 지켜지면 안 된다"고 지적 — 맞는 지적이라 기존 `/publish-order` 엔드포인트가 `order`뿐 아니라 `hidden` 필드도 같이 갱신하도록 확장(각 프로젝트의 다른 데이터는 여전히 안 건드림).
- **버그 하나 발견해서 고침**: 필드를 정규식으로 찾아 교체할 때 트레일링 `\s*`를 썼더니, 이미 값이 있던 필드(예: 기존 `order:2,`)를 교체할 때 그 뒤의 줄바꿈까지 같이 먹어버려서 다음 필드가 같은 줄에 붙어버리는 포맷 버그가 있었음(`order:3,color:...`처럼). `\s*`를 `[ \t]*`(줄바꿈은 안 먹고 같은 줄의 공백만)로 바꿔서 해결. `hidden` 필드를 완전히 지울 때(다시 보이기)는 반대로 앞의 줄바꿈까지 같이 지우는 별도 정규식을 둬서 빈 줄이 안 남게 함.
- **검증**: `publish_order()`를 `/tmp` 사본에 대해 직접 실행해서 order 필드 교체·hidden 필드 삽입/교체/완전삭제 네 가지 경우 모두 포맷이 깨끗하게 유지되는지, 최종 JS 문법이 유효한지 확인. Playwright로는 실제 빌더에서 숨기기 버튼을 누르면 배지·흐림 표시가 뜨고 IndexedDB에 `hidden:true`가 저장되는지, 다시 누르면 원상복구되는지 확인.

## 세션 요약 — 사이드바 프로젝트 순서를 사이트에 반영하는 전용 버튼 추가 (2026-07-30, 계속)

앞서(같은 날) 프로젝트 `order` 필드를 Publish 코드에 포함시키는 수정을 했는데, 실제로 써보니 사이드바에서 드래그로 순서만 바꾼 경우엔 그 변경이 IndexedDB에만 저장되고 index.html엔 전혀 반영이 안 된다는 문제가 남아있었음 — 순서가 바뀐 프로젝트 전부를 하나하나 다시 Publish 해야 실제 사이트에 반영되는데, 그걸 깜빡하기 매우 쉬움. 사용자가 "순서 전용 Publish 버튼이 있어야 정확하지 않을까"라고 제안해서 그대로 구현.

- **사이드바에 "↕ 순서를 사이트에 반영" 버튼 추가**(`+ 새 프로젝트` 버튼 아래): 클릭하면 현재 모든 프로젝트의 `{id, order}` 목록을 서버로 보내고, `server.py`의 새 `/publish-order` 엔드포인트가 index.html의 `PROJECTS` 배열에서 각 프로젝트를 id로 찾아 **`order:` 필드 하나만** 교체(없으면 새로 삽입, 있으면 값만 교체)한 뒤 기존 Publish와 동일하게 git commit + push. 프로젝트의 다른 데이터(사진, 문단, 크레딧 등)는 전혀 건드리지 않음 — 순서 변경 목적으로 프로젝트 전체를 다시 Publish할 때 생기는 위험(사진 재배치, 실수로 다른 필드 덮어쓰기 등)을 피하기 위함.
- **검증**: `publish_order()` 함수를 `/tmp` 사본에 대해 직접 실행해서 (1) 기존에 `order` 필드가 없던 프로젝트에 새로 삽입되는지, (2) 이미 있던 프로젝트는 값만 교체되고 뒤에 붙어있던 주석은 그대로 보존되는지 확인. Playwright로는 실제 빌더에서 프로젝트 3개를 만들고 드래그로 순서를 바꾼 뒤 "순서를 사이트에 반영" 버튼을 눌렀을 때, 서버로 보내는 요청 payload가 드래그 직후의 최신 순서를 정확히 담고 있는지 확인(실제 `/publish-order` 호출은 가짜 응답으로 가로채서 실제 index.html은 건드리지 않음).

## 세션 요약 — Neo QLED 8K 크레딧 데이터 입력 (2026-07-30, 계속)

사용자가 크레딧 목록 스크린샷을 첨부하며 Neo QLED 8K 프로젝트에 반영 요청. 이 프로젝트는 (기존 메모리와 달리) 이미 index.html의 실제 라이브 `PROJECTS` 배열에 활성 상태로 들어가 있었음(`credits:null`인 채로) — 그 자리에 이미지 속 20개 항목을 SSF SHOP과 동일한 `[["Label","Value"], ...]` 포맷으로 직접 입력.

**중요 — builder ↔ index.html 데이터 불일치 주의**: 이 값은 builder.html(IndexedDB)을 거치지 않고 index.html에 직접 써넣은 것. 만약 나중에 builder에서 이 프로젝트("Samsung Neo QLED 8K", id: `neo-qled-8k`)를 열어 크레딧 없이(또는 다른 값으로) Publish하면, 이번에 넣은 크레딧이 덮어써짐. builder의 IndexedDB 쪽 크레딧도 같이 채워두는 걸 권장 — 이 세션에서는 사용자의 실제 브라우저 세션(IndexedDB)에 접근할 방법이 없어서 못 했음.

**검증**: Playwright로 실제 index.html을 열어 WORK 리스트 → Neo QLED 8K 카드 클릭 → 상세페이지의 Credits 섹션까지 실제로 네비게이션해서 20개 항목이 스크린샷 속 내용과 정확히 일치하게 렌더링되는 것을 확인(4열 그리드 레이아웃도 정상).

## 세션 요약 — 사진 레이아웃 모달에서도 동영상 완전 배제 (2026-07-30, 계속)

바로 직전 세션에서 "동영상/사진 레이아웃 분리"를 실제 갤러리 렌더링(`galleryHTML`/`galleryBlockHTML`)에는 적용했지만, **레이아웃을 설정하는 모달 자체**는 안 고쳤다는 걸 사용자가 지적 — Free 모드에서 여전히 동영상이 사진들과 함께 자유배치 캔버스에 드래그 가능한 프레임으로 섞여 나왔고, "N장씩" 모드 미리보기도 동영상을 포함해서 첫 N개를 보여주고 있었음.

- **레이아웃 모달을 사진 전용으로**: `openLayoutModal`/`renderLayoutPreview`/프리셋 버튼/완료 버튼 전부 `layoutPhotoImages(block)`(동영상 제외 필터) 기준으로 동작하도록 수정. Free 모드 캔버스에는 이제 사진만 뜨고 드래그·크기조절도 사진에만 적용됨. 모달 위쪽에 "🎬 동영상은 레이아웃과 무관하게 항상 사진 위쪽에 고정 표시됩니다" 안내문을 추가해서 동영상이 왜 안 보이는지 헷갈리지 않게 함.
- **Free 모드 렌더링도 동영상 분리로 통일**: 직전 세션엔 "Free 모드는 이미 각 항목이 독립적인 위치를 가지니 굳이 분리 안 해도 된다"고 판단했었는데, 사용자 피드백을 보니 그게 아니라 Free 모드에서도 동영상이 사진들 레이아웃 조정에 끼어드는 게 문제였음 — `galleryBlockHTML`/`galleryHTML`의 "free" 분기도 "1"/"2"/"3"/"all"과 동일하게 동영상을 항상 고정 스택으로 빼도록 통일. builder.html 쪽은 이 김에 `im.free`를 각 이미지 객체에서 직접 읽도록 정리해서(기존엔 위치가 밀리면 어긋날 수 있는 배열 인덱스 매칭 방식이었음) 더 안전하게 만들었고, index.html은 발행된 정적 데이터 구조(`freeLayout`이 `images` 배열과 위치로 짝지어짐) 특성상 배열 인덱스 방식을 유지하되 사진만 걸러낼 때 인덱스가 안 어긋나도록 원본 배열을 같이 순회하며 재정렬.
- **검증**: Playwright로 블록에 Vimeo 링크 동영상을 추가한 뒤 (1) Free 모드 캔버스의 프레임 개수가 정확히 "사진 개수"와 일치하고 동영상 아이콘 프레임이 하나도 없는지, (2) "2장씩" 모드 미리보기에도 동영상이 안 섞이는지, (3) 실제 발행 미리보기에서 `.gallery-mixed` 안에 동영상 스택 + 사진만 있는 free 갤러리(정확히 사진 개수만큼의 프레임)가 나오는지 확인.

## 세션 요약 — 블록 내 사진/동영상 레이아웃 분리 + Free 모드 정렬 스냅 가이드 (2026-07-30, 계속)

- **블록 안 사진/동영상 레이아웃 독립화**: 지금까지 블록의 갤러리 모드(1장씩/2장씩/3장씩)가 동영상과 사진을 구분 없이 섞어서 같은 롤링 갤러리로 처리했음(오직 "All" 모드에서만 동영상을 위쪽에 고정으로 분리하는 로직이 있었음). 이제 "1"/"2"/"3" 모드에서도 동일하게 분리되도록 일반화 — 동영상은 항상 위에 고정으로 쌓이고, 사진들만 블록에서 고른 모드로 따로 넘겨보게 됨. **`index.html`(실제 발행 사이트)의 `galleryHTML()`과 `builder.html`(빌더 프리뷰)의 `galleryBlockHTML()`이 완전히 같은 로직을 각자 구현해서 갖고 있어서(공유 코드 아님), 두 파일 모두 동일하게 고침** — 하나만 고치면 빌더 프리뷰와 실제 사이트 동작이 어긋나게 됨.
- **Free 레이아웃 정렬 스냅 가이드 추가**: "사진을 옮길 때 스마트하게 정렬이 맞았으면 좋겠다"는 요청에 따라, 사진 레이아웃 모달의 Free 모드에서 사진을 드래그하거나 크기 조절할 때 다른 사진의 가장자리/중심선이나 캔버스 가장자리/중앙선에 가까워지면(1.5% 이내) 자동으로 딱 맞춰 붙고 분홍색 가이드 선이 나타남(디자인 툴의 스마트 가이드와 동일한 방식) — Figma/Keynote식으로 왼쪽/중앙/오른쪽(위/중앙/아래) 정렬을 각각 시도해서 가장 가까운 것에 스냅.
  - **버그 하나 발견해서 고침**: 가이드 선 색을 처음엔 `var(--accent)`로 했는데, 레이아웃 미리보기 배경이 항상 어두운색(#111)인 반면 화이트/블루 테마의 `--accent`는 거의 검정(#111111)이라 가이드 선이 사실상 안 보이는 문제가 있었음 — 테마와 무관하게 항상 잘 보이는 고정 색(밝은 핑크)으로 교체.
- **검증**: Playwright로 (1) 블록에 Vimeo 링크 동영상을 추가하고 레이아웃을 "2장씩"으로 설정한 뒤 실제 미리보기 iframe에서 `.gallery-mixed`/`.gallery-video-stack`/중첩된 `mode="2"` 사진 갤러리가 모두 존재하는지, (2) Free 모드에서 사진을 다른 사진 근처로 드래그하면 가이드 선이 뜨고 마우스를 떼면 사라지는지 확인.

## 세션 요약 — 사진 확대보기 + 드래그앤드롭 첨부 + 여러 장 함께 드래그 + 레이아웃 프리셋 + Work 리스트 순서 동기화 (2026-07-30, 계속)

한 번에 요청 5가지 (마지막 2개는 대화 중 추가):

- **블록 사진 클릭 시 확대(라이트박스)**: 사진 목록의 썸네일을 클릭하면 전체화면 오버레이로 원본 크기로 볼 수 있음(닫기 ✕, 좌우 화살표로 같은 블록의 다른 사진 탐색, ESC로 닫기). PREVIEW_RUNTIME_JS 안에만 있던 "발행된 사이트를 흉내내는" 라이트박스와는 별개로, 빌더 자체 편집 화면(`#editor-lightbox`)에 새로 만든 것 — 이미 있던 것과 헷갈리지 않게 id를 분리함.
- **사진/동영상 드래그앤드롭 첨부**: Finder에서 이미지·동영상 파일을 블록 카드 위로 바로 끌어다 놓으면 첨부됨. 이미지는 기존 "사진 추가" 버튼과 동일한 경로로 즉시 추가되고, 동영상은 서버에서 웹 최적화(재인코딩)에 실제로 시간이 걸리기 때문에 조용히 업로드하는 대신 동영상 모달을 파일명이 이미 채워진 상태로 띄워서("추가" 버튼만 누르면 됨) 확인 후 진행하도록 함. 내부적으로 있던 카드 순서변경용 드래그(파일 없음)와 실제 OS 파일 드래그(`dataTransfer.types`에 "Files" 포함)를 구분해서 서로 안 부딪히게 함.
- **선택된 사진 여러 장을 한번에 드래그로 순서 이동** (대화 중 추가 요청): 체크박스로 여러 장을 선택한 상태에서 그 중 하나를 ⠿ 손잡이로 드래그하면, 선택된 사진들 전부가 원래 순서를 유지한 채 하나의 덩어리로 함께 이동함. 기존 단일 아이템 드래그 코드(`dragItem`)에 `dragGroup`(선택된 요소 배열)을 추가하는 방식으로 구현 — 일반 블록/문단/프로젝트 드래그(선택과 무관)는 전혀 영향 없음.
- **사진 레이아웃 "1장 크게 + 나머지 모아보기" 프리셋**: "사진 레이아웃" 모달의 기존 Free(자유 배치) 모드는 이미 사진마다 임의의 x/y/w/h(%)를 직접 지정할 수 있는 기능이 있었음 — 새로운 렌더링 방식을 만들 필요 없이, Free 모드용 좌표를 자동으로 계산해주는 "프리셋" 버튼만 추가(`heroGridFreePos()`): 첫 번째 사진은 위쪽 전체 너비로 크게, 나머지는 그 아래 최대 4열 그리드로 배치. 프리셋 적용 후에도 여전히 손으로 드래그해서 미세조정 가능(그냥 시작점을 잡아주는 것).
- **빌더 사이드바 프로젝트 순서 ↔ 실제 Work 리스트 페이지 순서 동기화** (대화 중 추가 요청): 지금까지 빌더 왼쪽에서 드래그로 프로젝트 순서를 바꿔도 그 순서 정보(`order` 필드)가 Publish 코드에 아예 포함되지 않아서, 실제 사이트의 Work/Play 리스트는 `PROJECTS` 배열에 프로젝트가 추가된(커밋된) 순서로만 보였음. `exportProjectCode()`가 `order` 값도 내보내도록 하고, index.html의 `renderList()`가 카테고리로 거른 뒤 `order`로 정렬하도록 두 줄 추가 — 이제 빌더에서 순서를 바꾸고 Publish하면 실제 Work/Play 페이지 순서도 그대로 반영됨. (참고: 프로젝트 상세페이지의 "이전/다음" 화살표 네비게이션은 여전히 `PROJECTS` 배열 원본 순서를 씀 — 이번 요청 범위 밖이라 그대로 뒀지만, 필요하면 같이 맞출 수 있음.)
- **검증**: Playwright로 (1) 썸네일 클릭 시 라이트박스가 열리고 이미지가 바뀌고 ESC로 닫히는지, (2) 사진 2장 선택 후 드래그하면 둘 다 같이 옮겨가서 순서상 서로 붙어있는지, (3) 실제 File 객체가 담긴 합성 `DragEvent`를 블록에 `drop`시켜서 사진 개수가 실제로 늘어나는지, (4) 레이아웃 모달에서 프리셋 버튼을 누르면 첫 프레임이 위쪽 100%×60%, 나머지가 그 아래 4열 그리드 좌표를 정확히 갖는지 확인. Work 리스트 순서 동기화는 로직이 단순한 정렬 비교문이라 별도 브라우저 검증 없이 코드 검토로 확인.

## 세션 요약 — WORK/PLAY 그리드 "종이 롤링" 스크롤 효과 시도 → 결국 전체 원복 (2026-07-30, 계속)

사용자가 스크린샷/영상을 여러 차례 첨부하며 "스크롤하면 프로젝트 썸네일이 위아래로 종이처럼 말리는(롤링되는) 인터랙션"을 요청. 방향을 좁히는 데만 몇 차례 왕복이 필요했음: 처음 만든 스크롤 속도 기반 `skewY` 워프는 "대각선으로 기울면서 올라온다"고 반려 → "썸네일 개별이 아니라 페이지 전체가 하나의 종이처럼, 까딱거리지 않고"라는 요구로 방향(뷰포트 중심 기준 지속적인 곡률, 속도 아닌 위치 기반)을 확정 → 레퍼런스 영상을 프레임 단위로 재확인한 결과 평면 회전이 아니라 실제 곡면 왜곡이 필요하다고 판단되어 WebGL(OGL) 구현으로 진행.

- **WebGL(OGL) 구현**: 비교적 큰 작업이라 EnterPlanMode로 계획을 세운 뒤 진행. `ogl` npm 패키지를 스크래치 디렉터리에 실제로 설치해 `Camera`/`Plane`/`Texture` 소스코드를 직접 읽어 API를 검증했고, 그 과정에서 실제 버그 두 개(카메라 `far` 기본값이 100인데 DOM 픽셀 단위 좌표계에서는 카메라 거리가 수천 단위까지 커져서 클리핑되는 문제, `Renderer`가 WebGL 컨텍스트 생성 실패 시 예외 대신 `gl:null`만 남기는 문제)를 사전에 찾아 고침. CDN(`jsdelivr +esm`) 응답도 `curl`로 직접 받아 export가 실제로 존재하는지 확인.
- **이 샌드박스 환경의 근본적 한계 재확인**: Puppeteer로 로컬 서버(`http-server`)에 접속을 시도했으나 계속 실패 → `data:` URL은 되는데 `https://example.com`조차 타임아웃나는 것으로 확인 — 지난 세션에 발견한 "헤드리스 rAF 타이밍이 못 미더움" 수준이 아니라, **이 환경에서 실행되는 Chrome은 아예 외부 네트워크 접근 자체가 막혀있음**(로컬/외부 불문). 즉 이번 세션 내내 어떤 시각적 검증도 브라우저로 직접 할 수 없었고, 코드 문법 검사·라이브러리 소스 직접 확인·순수 JS 로직 시뮬레이션(곡률 계산이 중심에서 0, 가장자리에서 ±1로 정상 클램프되는지)까지만 가능했음.
- **의도치 않은 배포 발생**: 작업 도중 사용자가 builder.html에서 INFO 페이지를 Publish 하면서, `server.py`의 `git_commit_and_push()`가 `git add -A`로 작업 트리 전체를 커밋+푸시하는 구조라(주석에도 "다음 Publish에 같이 실려간다"고 명시돼있는 기존 설계) 아직 검증 전이던 WebGL 코드가 "Publish: Info page" 커밋에 같이 실려서 그대로 jjaejae.com에 배포돼버림. 사용자가 화면 녹화로 실제 라이브 화면을 보내줘서 확인한 결과, 카드 일부가 검은 박스로 비거나 화면을 가로지르는 색색의 선 아티팩트가 나타나는 렌더링 버그가 실제로 발생.
- **WebGL → 순수 CSS 방식으로 전환**: 사용자가 직접 "WebGL 말고 CSS 방식으로"를 선택. 이미지를 12개의 가로 띠(`.curl-strip`, `background-image`로 각자 다른 부분만 표시)로 나눠 쌓고, 중심에 가까운 띠는 평평하게 바깥쪽 띠일수록 `rotateX`를 크게 줘서 곡면을 근사하는 방식으로 재구현 — 외부 의존성 없음.
- **새 버그(페이지 완전 정지)**: 배포 후 "WORK/PLAY 들어가면 페이지가 멈춘다"는 리포트. 원인은 `.curl-strip`을 추가/삭제하는 코드를 감시 범위가 `subtree:true`인 `MutationObserver`가 그 변화 자체를 다시 감지해서 재구성 함수를 무한 재호출하는 **무한 루프**였음. 감시 범위를 `listGrid`의 직계 자식만(`childList:true`, subtree 제거)으로 좁혀서 수정 후 재배포.
- **최종 결정: 전체 원복**: 무한 루프를 고친 뒤에도 오류가 계속된다는 리포트를 받고, 이 환경에서 브라우저 검증 없이 추측으로 계속 고치고 배포하는 게 비생산적이라 판단 — 사용자 요청대로 효과 도입 이전 상태로 전체 원복. `git reset`이 아니라, 그 사이 섞여 들어간 다른 무관한 변경(INFO 페이지 콘텐츠, 다크테마 pill UI 등)을 건드리지 않도록 `.work-card`/`.thumb` 관련 CSS와 그리드 스크립트 블록만 직접 제거하는 방식으로 처리했고, 커밋 `2584898`(이 기능 도입 전 마지막 커밋)의 해당 섹션과 바이트 단위로 동일한지 `diff`로 확인 후 커밋·배포.
- **교훈**:
  1. 이 샌드박스의 Chrome은 네트워크 자체가 막혀있어 브라우저 자동화 검증이 완전히 불가능함 — 시각/인터랙션이 중요한 기능은 코드 검토·로직 시뮬레이션까지만 하고, 반드시 사용자가 실제로 확인해줘야 함을 미리 명확히 알릴 것.
  2. `server.py`의 Publish가 `git add -A`를 쓰기 때문에, 여러 세션(또는 사용자+세션)이 동시에 이 저장소를 건드리고 있으면 한쪽의 미완성 작업이 다른 쪽의 Publish에 의도치 않게 같이 배포될 수 있음 — 동시 작업 중임을 알게 되면 이 위험을 먼저 언급할 것.
  3. 시각적 검증이 불가능한 환경에서는 위험도가 높은(외부 라이브러리, 복잡한 좌표 동기화) 접근보다 실패해도 안전하게 저하되는(graceful degradation) 단순한 접근을 우선할 것.

## 세션 요약 — 사진 여러 장 한번에 이동 + builder에서 INFO 페이지 편집 (2026-07-30)

두 가지 요청: (1) 블록 안 사진이 많을 때 하나씩 드래그하며 스크롤해야 하는 게 불편하다는 피드백 → 체크박스 멀티셀렉트 추가. (2) index.html의 INFO 오버레이(`#info-view`, 인사말/본문·태그라인 문단들/링크)가 지금까지 하드코딩이라 builder와 무관했는데, 이것도 builder에서 편집·Publish 하고 싶다는 요청.

- **사진 멀티셀렉트 이동** (`builder.html`): 각 사진 행에 체크박스 추가. 체크하면(선택은 블록 하나로 범위 고정 — 다른 블록 사진 체크 시 이전 선택 초기화) 그 블록 사진 목록 위에 툴바가 뜨고 "맨 위로 / 맨 아래로 / 다른 블록으로 이동 / 선택 해제" 지원. 기존 한 장씩 드래그하는 방식은 그대로 유지, 이건 추가 옵션.
- **INFO 페이지 편집** (`builder.html` + `server.py`): 사이드바에 "INFO 페이지 편집" 버튼 추가 → 클릭하면 에디터/프리뷰가 프로젝트 편집에서 INFO 편집 모드로 전환. 인사말, 본문/태그라인 문단(추가·삭제·드래그 순서변경), 링크(라벨+URL, 추가·삭제)를 편집 가능. 데이터는 프로젝트와 분리된 자체 구조로 `localStorage`(`pf-builder-info`)에 저장(텍스트만 있어서 IndexedDB 불필요). 우측 프리뷰는 index.html의 `#info-view` CSS를 그대로 옮겨온 전용 미리보기.
  - PUBLISH 버튼 → `server.py`의 새 `/publish-info` 엔드포인트가 index.html의 `<div class="info-content">...</div>` 안쪽만 정확히 찾아 교체(중첩된 `.links` div까지 감안한 매칭 로직)하고 기존 PUBLISH와 동일하게 git commit + push.
  - 프로젝트 undo(Ctrl+Z)와는 별도 시스템이라 INFO 편집에는 undo가 적용되지 않음(텍스트 입력 필드 브라우저 기본 undo는 그대로 동작).
- **검증**: Playwright(headless Chromium)를 스크래치 디렉터리에 설치해 실제 builder.html(file://)을 열어 체크박스 선택 → 이동, INFO 문단 추가/타입 토글/텍스트 수정 → 우측 프리뷰 갱신까지 확인. Playwright는 별도 격리된 브라우저 프로필이라 사용자의 실제 IndexedDB/localStorage 데이터와는 무관 — 실제 데이터에 영향 없음. `/publish-info`의 index.html 치환 로직은 `/tmp` 사본에 대해 별도로 실행해 결과 diff를 확인(다른 부분은 전혀 안 건드리는 것 확인), 실제 git push는 세션 중 실행하지 않음.
- **INFO 페이지에도 GENERATE 추가** (같은 세션 이어서): INFO 편집 화면에 PUBLISH 옆 GENERATE 버튼 추가. 클릭하면 모달이 뜨고 두 모드 중 선택:
  - **새로 쓰기**: 인사말·본문을 처음부터 새로 생성.
  - **내 글 다듬기**: 텍스트를 붙여넣으면 내용/의미는 그대로 두고 문장만 학습된 문체로 다듬어줌 — 사용자가 요청한 "내가 쓴 글을 학습된 문장으로 고쳐주는" 기능.
  - 두 모드 다 프로젝트 GENERATE와 동일하게 `voice.md`(문체 규칙) + `portfolio/*.md`(승인된 예시, 없으면 `inbox/내가 쓴 글.md`)를 프롬프트에 그대로 삽입해서 같은 문체 학습 파이프라인을 씀. 방향 메모(선택) 입력 가능.
  - `server.py`에 `/generate-info` 엔드포인트 추가 (`build_info_generate_prompt`). **버그 하나 고침**: 처음 구현 때 "다듬을 글" 모드의 방향 메모를 `%s` 문자열 포매팅으로 문체 규칙 블록 뒤에 붙였는데, `voice.md`나 예시 글에 우연히 "80%" 같은 `%` 문자가 들어있으면 포매팅이 깨지는 문제가 있어서 `+` 문자열 연결로 수정.
  - 응답이 비어있는 필드(예: 아주 짧은 초안이라 본문 문단 없이 인사말 하나로 다 들어간 경우)는 기존 값을 그대로 두도록 처리 — 모델이 빈 배열을 반환해도 기존 문단이 사라지지 않음.
  - **검증**: 재시작 없이 같은 세션에서 켜둔 `server.py`가 새 코드 반영 전 상태였던 걸 놓칠 뻔함 — `curl`로 `/generate-info` 직접 호출했을 때 404가 나서 알아챔 → 서버 재시작(`python3 -u server.py`, 로그 버퍼링 문제도 같이 해결) 후 재검증. Playwright로 실제 "내 글 다듬기" 흐름(초안 입력 → 생성 → 인사말 필드에 다듬어진 문장 반영 → 우측 프리뷰 갱신)까지 실제 `claude` CLI 호출로 확인.

## 세션 요약 — INFO 문단 스타일 커스터마이징 + GENERATE 되돌리기 + PC 프리뷰 점검 + 배경 사진 첨부 (2026-07-30, 이어서)

같은 날 이어진 요청 3가지 + 대화 중 추가된 요청 1가지:

- **INFO 본문 문단 폰트/굵기/크기 커스터마이징**: 각 문단 카드에 "스타일" 토글 버튼 추가 → 펼치면 폰트(기본/가는 서체/중간 서체/굵은 서체 — 사이트가 이미 로드하는 SUIT-ExtraLight/Medium/ExtraBold 중 선택), 글자 굵기(200~900 슬라이더), 글자 크기(50~250% 슬라이더) 조절 가능. 문단의 `<p>` 태그에 인라인 `style`로 반영되며, `infoParagraphStyleAttr()`(JS)와 `_info_paragraph_style_attr()`(server.py `render_info_content_html`)가 완전히 동일한 규칙으로 각각 빌더 프리뷰/실제 Publish 결과물을 렌더링하도록 맞춤. 빌더 프리뷰(`INFO_PREVIEW_CSS`)에 실제 사이트와 같은 SUIT 폰트 3종 `@font-face` + `--font-thin/medium/bold` 변수를 새로 추가해서 폰트 선택이 프리뷰에서도 정확히 보이게 함.
- **GENERATE 되돌리기**: 프로젝트 편집 화면에는 기존 Ctrl+Z 실행취소 스택을 그대로 노출하는 "되돌리기" 버튼을 GENERATE 옆에 추가(스택이 비어있으면 그냥 아무 일도 안 함, 굳이 비활성화 처리는 안 함 — 타이핑 중엔 버튼이 다시 렌더링되지 않아 disabled 상태가 낡을 수 있어서). INFO 편집 화면은 별도 undo 시스템이 없어서, GENERATE 직전 상태(인사말+본문)를 한 슬롯만 따로 저장해뒀다가 "되돌리기" 버튼(GENERATE 직후에만 나타남)으로 복원하는 방식으로 구현 — **주의**: GENERATE 이후 수동으로 문단을 더 고치고 나서 되돌리기를 누르면 그 수동 수정까지 함께 사라짐(진짜 GENERATE 한 번만 되돌리는 단일 슬롯이라, 세밀한 단계별 undo는 아님).
- **INFO 배경 사진 첨부** (대화 중 추가 요청): "배경 사진" 섹션에 썸네일 + "새 사진 첨부" 버튼 추가. 프로젝트 사진과 달리 INFO 데이터는 `localStorage`(File 객체 저장 불가)에 저장되므로, 첨부 즉시 `server.py`의 새 `/upload-info-bg` 엔드포인트로 올려서 실제 `info-bg.jpg` 파일을 서버에서 바로 덮어씀(Pillow로 JPEG 재인코딩, 항상 같은 파일명 유지 → index.html의 CSS 참조는 안 건드려도 됨). git 커밋은 안 되어 있는 상태이므로, 다음 PUBLISH(INFO든 프로젝트든) 때 `git add -A`로 자연스럽게 같이 올라감. 빌더 프리뷰는 캐시 무효화를 위해 `info-bg.jpg?v=타임스탬프` 형태로 매번 새로 불러옴.
- **"PC 미리보기가 세로로 보이는 오류" 점검**: 스크린샷을 픽셀 단위로 직접 분석해본 결과 실제로는 정확히 16:9 랜드스케이프 박스였음(오해였을 가능성) — 다만 `sizePreviewStage()`가 프로젝트↔INFO 탭 전환 시점에는 재계산되지 않고 오직 창 리사이즈/PC·Mobile 버튼 클릭 시에만 갱신되던 구조적 허점을 발견해서, `refreshPreview()`가 실행될 때마다(즉 콘텐츠가 바뀌거나 모드가 바뀔 때마다) 항상 `sizePreviewStage()`를 먼저 호출하도록 방어적으로 고쳐둠. 이 특정 재현 경로 자체는 이번 세션에서 재현하지 못했지만, 같은 증상이 다시 보이면 언제/어떤 조작 직후였는지(창 크기 변경 여부, PC·Mobile 전환 여부, 얼마나 오래 켜둔 세션이었는지) 알려주면 좁혀서 다시 볼 수 있음.
- **검증**: Playwright로 (1) 프로젝트 제목을 바꾼 뒤 되돌리기 버튼으로 원상복구되는지, (2) 문단 스타일 패널에서 폰트/굵기/크기를 바꾸면 우측 프리뷰의 `<p>` 인라인 style에 정확히 반영되는지, (3) 프로젝트→Mobile→INFO→PC로 연속 전환한 뒤에도 박스 비율이 정확히 16:9(1.778)로 유지되는지, (4) 실제로 작은 테스트 이미지를 업로드해서 서버가 진짜 `info-bg.jpg`를 새로 썼는지(파일 크기·mtime 변화로 확인)까지 확인. **주의**: (4)번 테스트는 실제 `info-bg.jpg`를 건드리는 테스트라, 테스트 전 원본을 스크래치 디렉터리에 백업해두고 테스트 직후 즉시 MD5 체크섬까지 맞춰서 원본으로 복원함 — 실제 배경 사진은 바뀌지 않았음.

## 세션 요약 — 텍스트 드래그 버그 + 배경 사진 프리뷰 버그 + INFO 텍스트 컬러 (2026-07-30, 계속)

바로 이어서 버그 리포트 2건 + 기능 요청 1건:

- **"블록 안 텍스트를 드래그로 선택하려 하면 블록 전체가 움직이는" 버그 수정**: 원인은 사진 행/블록 카드/프로젝트 항목 전부 카드 전체 `<div>`에 `draggable="true"`가 항상 걸려 있던 것 — 안의 `<textarea>`에서 마우스로 드래그해 글자를 선택하려 하면 브라우저가 그 제스처를 카드의 HTML5 드래그로 가로채버림. **고친 방법**: `draggable="true"`를 카드 자체가 아니라 ⠿ 손잡이를 마우스로 누르고 있는 그 순간에만 동적으로 켜고(mousedown), 손을 떼면 바로 끔(mouseup)으로 바꿈 — 손잡이가 아닌 곳에서 시작한 드래그(텍스트 선택 포함)는 이제 카드를 절대 움직이지 않음. 사진 목록·블록·INFO 문단·프로젝트 사이드바 전부 동일하게 적용.
- **INFO 배경 사진이 미리보기에 안 보이던 버그 수정**: `background-image:url(...)`의 URL 값을 `JSON.stringify()`로 감쌌었는데, 그게 큰따옴표로 감싸버려서 이미 큰따옴표를 쓰던 `style="..."` HTML 속성 자체가 중간에 깨져버리는 문제였음(`style="background-image:url("info-bg.jpg?v=..."`처럼 따옴표가 겹쳐서 파싱이 깨짐). URL 값을 작은따옴표로 감싸도록 고침. 실제 사이트(index.html)는 이 버그의 영향이 없었음 — 배경 이미지 CSS는 그대로 두고 실제 파일(info-bg.jpg)만 덮어쓰는 구조라서, 문제는 오직 빌더 자체 미리보기 렌더링에만 있었음.
- **INFO 텍스트 컬러 지정 기능 추가**: 인사말 아래에 색상 피커, 각 본문 문단의 "스타일" 패널에 색상 피커 추가(타입별 기본값: 본문 `#2b2f36`, 태그라인 `#15171c`). 빌더 프리뷰와 `server.py`의 Publish 렌더링 둘 다 동일하게 반영되도록 맞췄고, 서버 쪽엔 `#rrggbb` 정규식 검증을 추가해서 손상되거나 악의적인 색상 값이 들어와도 실제 HTML에 그대로 삽입되지 않도록 방어함(테스트로 `"><script>` 형태의 값을 넣어봤을 때 스타일 속성 자체가 통째로 생략되는 것 확인).
- **버그 하나 더 고침 (색상 기능 구현 중 발견)**: 문단 스타일 컨트롤들(폰트/굵기/크기, 이번에 추가한 색상)이 문단의 **배열 위치(인덱스)**로 문단을 찾고 있었는데, 드래그로 문단 순서를 바꾸는 동작은 화면 요소만 옮기고 전체를 다시 그리지는 않아서, 드래그로 순서를 한번 바꾸면 컨트롤들이 엉뚱한 문단을 가리키게 되는 문제가 있었음(사진/블록은 원래부터 ID 기반이라 이 문제가 없었음). 문단 컨트롤도 전부 배열 위치 대신 문단의 고유 `id`로 찾도록 고쳐서 근본적으로 해결.
- **검증**: Playwright로 (1) 문단 텍스트 안에서 드래그로 글자를 선택해도 카드가 안 움직이고 실제로 텍스트가 선택되는지, (2) ⠿ 손잡이로는 여전히 정상적으로 드래그 순서변경이 되는지(회귀 확인), (3) 배경 사진이 미리보기 iframe에 실제로 로드되는지(computed backgroundImage 확인), (4) 문단을 드래그로 순서를 바꾼 다음에도 색상 지정이 올바른 문단에 적용되는지까지 확인.

## 세션 요약 — 자간 + 인사말 스타일 패널 + INFO SAVE 버튼 (2026-07-30, 계속)

- **문단 자간(letter-spacing) 조절 추가**: 기존 폰트/굵기/크기/색상에 이어 "스타일" 패널에 자간 슬라이더(-0.05em ~ 0.15em) 추가.
- **인사말도 동일한 스타일 패널로 확장**: 지금까지 인사말은 색상만 지정할 수 있었는데, 본문 문단과 완전히 같은 5개 컨트롤(폰트/글자 굵기/글자 크기/자간/글자 색상)을 갖춘 접이식 "인사말 스타일" 섹션으로 교체. 문단 패널과 인사말 패널이 코드도 공유하도록(`infoStyleFieldsHTML()` 공용 함수, `applyInfoStyleField()` 공용 핸들러) 리팩터링해서 앞으로 한쪽만 고치고 다른 쪽을 깜빡할 일이 없게 함. `server.py`도 동일하게 `_info_style_attr()` 공용 함수로 정리(문단은 `clamp(1rem, 1.7vw, 1.2rem)`, 인사말은 `clamp(1.4rem, 3.2vw, 2.3rem)` 기준 크기만 다르고 나머지 로직은 동일).
- **INFO 편집에 SAVE 버튼 추가**: GENERATE 옆에 추가. INFO 데이터는 이미 입력할 때마다 자동으로 `localStorage`에 저장되고 있어서 사실 필수는 아니지만, 프로젝트 편집의 SAVE 버튼과 동일하게 "지금 바로 저장됨"을 확인시켜주는 용도(클릭하면 SAVED ✓ 표시).
- **검증**: Playwright로 SAVE 버튼 클릭 시 확인 표시가 뜨는지, 문단 자간 슬라이더가 프리뷰의 인라인 style에 반영되는지, 인사말 스타일 패널의 5개 컨트롤을 전부 바꿨을 때 프리뷰에 정확히 반영되는지 확인. `server.py`의 `render_info_content_html`도 `/tmp` 사본에 대해 별도 실행해서 인사말/문단 둘 다 폰트·굵기·크기·자간·색상이 정확한 `style` 속성으로 나오는지 확인.

## 세션 요약 — 다크 테마 전용 "필(pill) UI" 리스킨 (2026-07-30, 계속)

사용자가 참고 스크린샷(둥근 알약형 버튼/카드, 하드 오프셋 그림자 없음, 배경 거의 검정, 주황색 섹션 라벨) 첨부하며 "빌더페이지 블랙 컬러일 때 이 스타일로 바꿔줘" 요청. **화이트/블루 테마는 기존 "각진 모서리 + 오프셋 그림자" 룩 그대로 유지**하고, **다크 테마만** 별도 스타일로 교체.

- `body[data-theme="dark"]`의 색상 변수 조정: 배경을 더 진한 무채색(`#0c0c0d`)으로, 카드 색(`--surface`)과 선택 상태용 밝은 회색(`--surface-2`, 새로 추가) 분리, `--card-shadow`를 `none`으로 바꿔서 그 변수를 쓰는 모든 요소(사이드바 항목, 새 프로젝트 버튼, 블록 카드, 사진 행, 선택 툴바, 모달, 폰트 미리보기 박스)에서 자동으로 하드 그림자가 사라지도록 함.
- `<style>` 끝부분에 "dark theme: pill UI" 섹션을 새로 추가해서, 버튼(GENERATE/PUBLISH 등)·카테고리 탭·뷰 토글(PC/Mobile 등)·사이드바 항목·블록 카드·사진 행·입력창을 전부 완전히 둥근 모서리(버튼류는 `border-radius:999px`, 카드류는 18px, 사진 썸네일은 14px, 입력창은 12px)로 바꾸고 테두리를 없앰. 섹션 제목(`.section-head`)은 참고 이미지처럼 주황 accent 색으로.
- 참고 이미지의 "2x2 점 4개" 아이콘 장식은 구현하지 않음(순수 장식 디테일이라 스코프에서 제외, 필요하면 후속 요청으로).
- **검증**: Playwright로 테마를 화이트→블루→다크 순서로 순환시키며 각각 스크린샷 비교 — 화이트/블루는 픽셀 그대로 유지되는 것 확인, 다크는 프로젝트 편집·INFO 편집·문단 스타일 패널 세 화면 모두에서 알약형 버튼/카드/썸네일과 주황 라벨이 올바르게 적용되는 것 확인.


지난 세션의 커스텀 J 커서에 이어, 사용자가 레퍼런스 영상("cipher" 스튜디오 사이트)을 첨부하며 우상단 내비(WORK/PLAY/INFO)에 호버 시 글자가 랜덤 문자로 지글거리다 정렬되는 효과를 요청. 커밋 `6d84fd7`, push 완료(`57c7d1f..6d84fd7`), jjaejae.com 배포 확인.

- **우상단 내비 대문자화**: `Work`/`Play`/`Info` → `WORK`/`PLAY`/`INFO`.
- **텍스트 스크램블 호버 효과 추가**: `#site-header nav button` 3개에 `mouseenter`마다 글자별로 랜덤 문자(A-Z + 기호)로 지글거리다 원래 단어로 정렬되는 클래식 "devries 텍스트 스크램블" 효과 적용. 글자마다 시작 프레임(0~40)과 지속 프레임(0~40 추가)을 랜덤으로 줘서 순차적으로 정렬되는 느낌을 냄.
  - **처음 구현에서 속도가 레퍼런스보다 너무 빠르다는 피드백** → 프레임 범위를 늘려 속도를 늦추던 중, **완료 판정 로직 버그를 발견**: "지금 이 프레임에 스크램블 중인 글자가 하나도 없으면 끝난 것으로 판단"하는 방식이었는데, 이게 "아직 시작 전(대기 중)"인 글자와 "이미 끝난" 글자를 구분 못 해서, 우연히 모든 글자의 시작 프레임이 0보다 크면(대부분의 경우) 첫 프레임에 바로 애니메이션이 멈춰버리는 버그였음. 각 글자가 실제로 `frame >= end`에 도달했는지(`complete` 카운트)로 판정하도록 고쳐서 해결. 헤드리스 브라우저의 `requestAnimationFrame`이 실제 디스플레이 없이 매우 빠르게(비정상적으로) 돌아서 자동화 테스트로는 체감 속도를 못 재는 환경적 한계가 있었음 — 완료 프레임 수(38~70프레임)를 직접 계산해 60fps 기준 약 0.6~1.3초로 검증함.
  - 호버할 때마다(`mouseenter`) 매번 새 랜덤 큐로 재생되도록 `cancelAnimationFrame`으로 이전 애니메이션을 취소하고 재시작.
- **커스텀 J 커서 등장 시점 조정**: 기존엔 헤더/로고와 같은 `body.past-hero`(인트로 마퀴가 완전히 뷰포트를 벗어나야 켜짐) 트리거를 커서도 같이 썼는데, 사용자가 스크린샷으로 "이 정도 스크롤됐을 때부터(WORK/PLAY 글자가 화면 하단에 살짝 보이기 시작하는 시점)" 나왔으면 좋겠다고 구체적으로 지정. `#work-play` 섹션을 관찰하는 커서 전용 `IntersectionObserver`(`body.cursor-ready`)를 새로 만들어 분리. 처음엔 `threshold:0`만 썼더니 인트로 높이(`100svh`)가 정확히 뷰포트 높이와 같아서 스크롤 0일 때부터 경계가 애매하게 걸려 커서가 바로 켜지는 버그가 있었음 → `rootMargin:"0px 0px -40px 0px"`로 40px 여유를 둬서 확실히 스크롤이 진행된 뒤에만 켜지도록 수정.
- **J 커서 클릭 정확도 문제 수정**: "J의 어디를 눌러도 클릭이 되는 부분과 안 되는 부분이 있다"는 피드백. 원인은 커서 이미지가 (지연 효과를 위한) 좌표에 이미지의 **좌상단 모서리**를 맞춰서 그려지고 있었던 것 — 실제 클릭은 항상 진짜(지연 없는) 마우스 좌표에서 일어나는데, 화면에 보이는 J 글자의 잉크는 좌상단 모서리에서 한참 떨어진 곳에 그려지니 "보이는 곳"과 "눌리는 곳"이 어긋나 보였던 것. `transform-origin:center` + `translate(-50%,-50%)`로 이미지 중심을 좌표에 맞추고 회전도 그 중심 기준으로 돌게 고쳐서, 멈춰서 클릭할 때는 보이는 J의 중앙이 곧 실제 클릭 지점이 되도록 함(단, 빠르게 움직이는 도중의 지연 잔상 자체는 의도된 "끌려가는" 효과라 그대로 둠).
- builder.html에는 이번 변경 전부 반영 안 함 — 우상단 내비와 커스텀 커서 둘 다 사이트 전역 UI라 builder.html의 프로젝트 상세 미리보기 iframe에는 애초에 안 그려지는 요소들이라 무관.
- **검증 방법**: `/watch` 스킬로 사용자가 첨부한 레퍼런스 영상(cipher 사이트)을 프레임 단위로 확인해서 정확히 어떤 효과인지(텍스트 스크램블) 먼저 파악한 뒤 구현. 이후 Puppeteer로 로컬 테스트하면서 헤드리스 rAF가 비정상적으로 빠르다는 걸 발견 — 순수 JS로 완료 로직만 따로 시뮬레이션해서 프레임 수(속도)를 검증하고, 실제 체감 속도는 로직이 레퍼런스 구현 공식과 같은 값을 쓴다는 점으로 신뢰함. **교훈: 헤드리스 자동화 테스트에서 잰 "몇 ms 걸림" 같은 실측 타이밍은 이 샌드박스 환경에서 신뢰할 수 없음(vsync 없이 rAF가 너무 빠르게 돎) — 애니메이션 로직의 정확성(프레임 수)과 실제 화면 표시 여부만 검증하고, 절대 시간(ms) 측정은 신뢰하지 말 것.**

## 세션 요약 — 헤더 겹침/라이트박스/이미지 404 근본 원인 수정 + 커스텀 커서 (2026-07-28)

지난 세션에 이어 실기기(모바일 사파리)에서 잡힌 버그들을 스크린샷 기반으로 하나씩 추적해서 고침. 커밋 `0e2a4a3` → `8b2aa70` → `61ab74e`, 전부 push 완료, jjaejae.com 배포 확인함.

- **헤더/로고/footer의 IG·Top 버튼이 스크롤 중 본문과 겹치던 문제**: 처음엔 겹칠 때만 opacity로 숨기는 JS 방식으로 만들었다가, 사용자가 "탭이 아예 사라지면 안 된다, 안 보이는 여백을 미리 둬서 겹칠 일 자체를 없애는 개념으로 해달라"고 명확히 정정함. 최종적으로: `#work-play` 섹션 높이를 100vh→90vh로 줄이고 footer 하단 패딩을 늘려서(`padding:26px 6vw 60px`) 스크롤 끝까지 내려도 WORK/PLAY 글자·footer 문구가 로고/IG·Top 자리에 안 닿게 함. 프로젝트 상세뷰는 `project-past-hero` 전환 시점을 "첫 블록의 캡션/제목이 이미 다 지나간 뒤"로 늦추고 `.p-detail` 상단 패딩도 80→112px로 늘림. 아이폰 SE(667px)~프로맥스(932px) 여러 높이로 Puppeteer 실측 후 값 확정.
- **갤러리 사진/영상 프레임에 라운드 코너(18px) 적용**: `.gallery`, `.gallery-video-frame` 둘 다. builder.html 미리보기도 동일 반영.
- **사진 클릭 시 확대 라이트박스 추가**: 클릭하면 어두운 배경 위에 확대된 사진이 뜨고 `<`/`>`(사용자 요청대로 심플한 홑화살괄호)로 이전/다음, ✕/바깥클릭/Esc로 닫힘. 동영상은 대상 아님(사진만). **테스트 중 진짜 크로미움 렌더링 버그를 발견함** — `#lightbox`를 `#project-view`(고정+큰 오버플로 스크롤 컨테이너)의 형제로 두면 특정 스크롤 위치에서 라이트박스 배경이 합성 안 되고 뒤 콘텐츠가 비쳐 보임(헤드리스/일반 크롬 둘 다 재현됨, 스크린샷 픽셀은 틀린데 `elementFromPoint`/computed style은 맞다고 나오는 렌더링-only 버그). `#lightbox`를 `#project-view`의 자식으로 옮겨서 회피함. builder.html에도 동일 라이트박스 기능 반영(단, 여기는 애초에 그런 fixed 래퍼가 없어서 문제 없음).
- **라이트박스 열렸을 때 사이트 자체의 ✕ Close 버튼과 라이트박스 ✕가 겹쳐서 둘 다 눌리던 문제**: `body.lightbox-open` 클래스로 라이트박스가 열린 동안 `#site-header` 전체를 숨김(opacity/pointer-events) — 라이트박스 자체의 ✕만 눌리게 함.
- **"아이스크림 홈런" 프로젝트 사진이 jjaejae.com에서만 깨져 보이던 버그 — 진짜 원인 발견**: 처음엔 webp 디코딩 문제로 추정하고 이 프로젝트 이미지 20장을 jpg로 재인코딩했는데(1차 커밋 `8b2aa70`), 사용자가 재확인 후에도 여전히 깨진다고 스크린샷을 다시 보내옴. 재조사 결과 **진짜 원인은 유니코드 정규화(NFC/NFD) 불일치**였음 — index.html 안의 "아이스크림 홈런" 텍스트 일부가 NFD(분해형, macOS에서 흔히 생김)로 들어가 있었는데 실제 git에 커밋된 파일명들은 전부 NFC(조합형). macOS 로컬 서버와 손으로 만든 curl 테스트(직접 타이핑한 문자열이라 우연히 NFC)는 두 방식을 같은 파일로 인식해 멀쩡해 보였지만, Vercel의 Linux 서버는 바이트 단위로 다르게 취급해서 실제 브라우저가 생성하는 요청 URL이 파일명과 안 맞아 404가 났던 것. **`index.html` 전체를 `unicodedata.normalize('NFC', ...)`로 통일**해서 해결(이 과정에서 NFD 때문에 1차 수정 때 정규식이 못 찾고 놓쳤던 webp 참조 18개도 마저 jpg로 교체됨). builder.html은 애초에 전부 NFC라 문제 없었음.
- **갤러리 사진 로딩 완료 감지 레이스 컨디션 수정**: `waitMediaReady`가 `img.complete`만 확인하고 아직 `naturalWidth`가 안 채워진 순간을 "아직 준비 안 됨"으로 오판하면, 이미지가 캐시 등으로 아주 빨리 로드된 경우 `load` 이벤트가 이미 지나가버려서 콜백이 영영 안 불리고 그 갤러리 프레임이 계속 배경색(검은 박스)으로만 보이는 문제가 있었음. `load`/`error` 리스너에만 의존하지 않고 `requestAnimationFrame` 폴링을 안전망으로 추가. `index.html`·`builder.html` 둘 다 반영.
- **커스텀 마우스 커서(jjaejae "J" 아이콘) 추가**: 사용자가 첨부한 J 아이콘 PNG를 다듬어(여백 크롭+리사이즈) `cursor-j.png`로 추가. 실제 마우스 있는 기기(`pointer:fine`)에서만 기본 커서를 숨기고 이 아이콘이 따라다님 — 매 프레임 목표 위치로 20%씩만 따라붙게 해서 살짝 지연시키고, 그 지연 거리(=속도)에 비례해 최대 ±35도 기울어졌다가 멈추면 수평으로 복귀(끌려가듯 기울어지는 효과). 이후 사용자 요청으로 **크기 70% 축소**, **메인 인사말 화면(스크롤 전)에서는 안 보이다가 스크롤로 지나야(`body.past-hero`) 나타나게** 수정. 터치 기기는 전혀 영향 없음. builder.html에는 반영 안 함(사이트 전역 UI라 프로젝트 상세 미리보기와 무관).
- **검증 방법**: 이번에도 로컬 `python3 -m http.server` + Puppeteer(headless Chrome) 조합으로 계속 확인했지만, "아이스크림 홈런" 버그는 **로컬/curl 테스트가 전부 정상으로 나와서 실제로는 사용자의 실기기 스크린샷이 없었으면 못 찾았을 버그**였음 — 서버 응답이 다 정상인데 사용자가 "그래도 깨진다"고 재차 확인해줬기 때문에 유니코드 정규화까지 파고들 수 있었음. **교훈: curl/로컬 테스트가 전부 통과해도, 실기기에서 재현되는 버그라면 사용자 말을 믿고 더 깊이(이번엔 텍스트 인코딩까지) 파봐야 함.**

## 세션 요약 — Work/Play 구조 개편 + 갤러리 버그 추가 수정 (2026-07-27, 이어서 3)

이전 세션에서 고친 모바일 갤러리 버그 이후, 사용자가 새로 캡처한 화면 기록 두 개(폰트 깜빡임, 모바일 스와이프 오류, PC 마지막 사진 잘림)를 보고 추가 버그를 고치고, 이어서 홈페이지 구조를 크게 바꿈. 전부 push 완료(`37edc27` → `0053bbf` → `b0597bd`, origin/main과 동기화됨. 이번 세션엔 네트워크 문제 없었음).

- **인트로 폰트 깜빡임(FOUT) 수정**: `font-display:swap`이 시스템 폰트로 먼저 그렸다가 SUIT 폰트로 바뀌면서 마퀴 텍스트가 덜컹거리던 문제. `<link rel="preload">`로 SUIT 폰트 3종을 즉시 받아오고 `font-display:block`으로 바꿔서 폰트 준비 전까지 잠깐 숨겼다가 정확한 폰트로만 렌더링되게 함.
- **갤러리 프레임 밀림(모바일 스와이프 시 두 사진이 겹쳐 보임 / PC 마지막 사진이 박스에 안 맞음)**: 원인은 `.gallery` 박스의 width/height가 CSS transition으로 애니메이션되는 동안, 형제 프레임들의 이미지 크기가 `height:100%`로 그 애니메이션 중인 부모 높이에 실시간 연동돼 있어서 전부 같이 늘었다 줄었다 하며 `translateX` 위치 계산과 어긋나던 것. 각 프레임에 독립적인 고정 px 크기(`sizeGalleryFrame`)를 부여해서 박스 애니메이션과 분리 — 커밋 `37edc27`.
- **`galleryMode:"all"`(영상+사진 섞인 블록) 화면 밖 넘침 버그**: 이 모드는 `.gallery`를 `height:auto!important`로 강제하는데, 최초 기준 높이(`baseHeight`)를 잡을 때 아직 크기 안 잡힌 원본 이미지/영상의 "가공 전" 크기(1000px 이상)를 그대로 읽어버려서 영상·사진이 화면 밖으로 튀어나갔음. 기준 높이를 다른 갤러리와 같은 공식(`Math.max(280, Math.min(640, innerHeight*0.6))`)으로 안정화하고 폭 초과 시 줄이는 로직 추가 — 커밋 `0053bbf`.
- **영상+사진 섞인 블록 구조 변경**: "결과 편/비교 편/SSF SHOP" 등 `galleryMode:"all"` 블록을 사용자 요청으로 재구성 — 영상은 항상 위에 고정(`.gallery-video-stack`), 사진들은 그 아래 기존 롤링 갤러리(mode "1")로 좌우 이동. `galleryHTML()`이 `images` 배열을 video/photo로 나눠서 각각 다르게 렌더링. **한 번 "사진 클릭=확대(라이트박스)+화살표로 넘기기"로 만들었다가, 사용자가 도로 "클릭하면 다음 사진" 방식을 원해서 라이트박스·화살표 전부 제거하고 원래 클릭-투-어드밴스로 되돌림** — 최종적으로는 라이트박스 기능 자체가 없음(코드에도 안 남아있음).
- **홈페이지 Work/Play 섹션 → WORK/PLAY 랜딩 + 별도 리스트 페이지로 전면 개편** (사용자가 레퍼런스 영상 두 개 첨부: "화면 기록 2026-07-27 오후 4.05.52.mov"가 이 개편의 기준):
  - 스크롤하면 기존 프로젝트 그리드 대신 화면 좌/우에 **"WORK"/"PLAY" 글자만** 크게 보임(`#work-play` 섹션, `.wp-link`).
  - 클릭하면 `/work`, `/play` 클린 URL로 이동 — `goWork()`/`goPlay()`, `applyRouteFromLocation()`에 라우팅 추가(`/info`, 프로젝트 라우팅과 동일 패턴). 헤더 nav의 Work/Play 버튼도 같은 함수로 연결(예전엔 홈으로 가서 스크롤하는 방식이었음).
  - 리스트 페이지(`#list-view`)는 2단 엇갈림(masonry) 배치 — CSS `column-count`는 아이템이 적으면 한쪽 컬럼에 몰아넣고 반대쪽을 비워버리는 문제가 있어서, JS(`renderGrid`)가 아이템을 번갈아(`i % numCols`) 두 개의 `.list-col`에 나눠 담는 방식으로 구현(모바일은 1컬럼). 이미지는 원본 비율 그대로, 순서는 항상 고정(진짜 랜덤 아님 — 사용자가 이 방식을 선택함).
  - 카테고리(태그) 필터 탭 추가 — `renderList()`가 해당 카테고리(work/play) 안의 고유 태그로 "All + 태그들" 버튼을 만듦. **처음엔 "태그가 2개 이상일 때만 표시"로 만들어서 지금처럼 프로젝트가 적어 태그가 1개뿐인 상황엔 탭이 아예 안 보이는 버그가 있었음 → 태그 1개 이상이면 항상 보이도록 수정.**
  - 프로젝트 카드(`.work-card`)는 마우스 올려야 이름·카테고리·년도(`.meta`)가 보임(기존엔 항상 보였음), 썸네일은 라운드 코너(18px) 적용.
  - **✕ Close 버튼은 프로젝트 상세뷰에서만 표시** — `openList()`에서는 `closeBtn.classList.add("show")`를 호출하지 않음(리스트 페이지는 헤더 nav나 브랜드 로고, Esc로 나감).
  - **글자 인터랙션**: WORK/PLAY 글자와 카테고리 필터 탭 텍스트 둘 다 "커서에 가까운 글자일수록 세로로 길어지는" 효과 적용(`initLetterStretch(el, opts)` — 글자를 span으로 쪼개서 mousemove 때 커서와의 거리로 `scaleY` 계산). 레퍼런스 영상 0:07~0:10 구간을 다시 확인해서 정확한 효과 확인함. 필터 탭은 호버 시 알약 모양 테두리도 같이 나타남(레퍼런스와 동일).
  - **서체 교체 2번**: 처음엔 `Fontspring-DEMO-proximanovacond-semibold.otf`로 넣었다가, 사용자가 다시 `DINNextLTPro-MediumCond.otf`로 교체 요청 — 최종적으로 `fonts/DINNextLTPro-MediumCond.woff2` 사용 중(ProximaNovaCond 파일은 삭제함). `--font-condensed` CSS 변수로 관리. 리스트 페이지 타이틀도 "WORK"/"PLAY" 대문자로 표시.
  - 프로젝트 상세페이지 하단 "All Work" 링크도 프로젝트의 실제 카테고리(work/play)에 맞는 리스트로 돌아가도록 수정.
- **builder.html은 이번 WORK/PLAY 관련 변경들(랜딩 섹션, 리스트 뷰, 글자 인터랙션, 새 서체)에 한해 반영 안 함** — builder.html의 미리보기 iframe은 프로젝트 상세 페이지만 그리고 홈페이지 Work/Play 섹션은 안 그리기 때문에 무관하다고 판단해서 의도적으로 스킵함. (단, 그 이전 세션의 갤러리 버그 수정들 — mode-all baseHeight, sizeGalleryFrame 프레임 밀림, font-display — 은 `builder.html`에도 이미 반영/커밋되어 있음.) **다음에 builder.html 쪽 프리뷰도 이 구조와 맞출 일이 생기면 이 사실을 기억할 것.**
- **검증 방법**: 매번 로컬 `python3 -m http.server`로 띄운 뒤 Puppeteer(headless Chrome)로 실제 클릭/호버/스크롤/라우팅/콘솔에러를 확인하는 방식을 계속 씀. 로컬 서버는 SPA rewrite가 없어서 직접 `/work` 같은 경로로 `page.goto`하면 404 남 — 대신 홈페이지를 로드한 뒤 JS로 해당 버튼을 클릭해서 라우팅을 재현함(실제 배포 환경에서는 `vercel.json` rewrite가 처리).

## 세션 요약 — 라이브 사이트 갤러리 버그 수정 (2026-07-27, 이어서)

바로 위 세션에서 만든 기능들(크롭·히어로 분리·동영상 첨부·레이아웃 모달)을 실제로 쓰다가 발견된 라이브 사이트 버그들을 고침.

- **프로젝트 상세뷰에서 텍스트 아래 사진이 아예 안 보이던 버그 (SSF SHOP 등)**: `openProject()`가 `#project-view`를 보이게 하기(`.open` 클래스 추가) **전에** `renderProject()`로 갤러리 크기 계산을 먼저 실행하고 있었음 — 화면이 `display:none`인 상태라 컨테이너 너비가 0으로 읽혀서, 로드 이벤트를 기다리지 않는 프레임(동영상 링크로 시작하는 블록)이 0×0으로 찌그러짐. `.open` 클래스를 먼저 추가하도록 순서를 바꾸고, `activateGalleryFrame`/`layoutFrames`에 `availWidth`가 0일 때 억지로 축소하지 않는 방어 로직도 추가.
- **Work 그리드에서 갤럭시탭 S9 썸네일이 깨져 보이던 버그**: 히어로 이미지가 예전 크롭 테스트 때 만들었다가 지워진 `-crop.avif` 파일을 계속 가리키고 있었음 — 원래 파일명으로 복구(`index.html` + builder의 IndexedDB 양쪽).
- **2/3장·전체 레이아웃에서 사진 위아래(또는 좌우)에 검은 여백(레터박스)이 생기던 문제**: 고정 크기 박스 안에 `object-fit:contain`으로 사진을 욱여넣던 방식이 원인. 1장 모드처럼 **각 사진의 실제 비율로 프레임 크기를 직접 계산**(공유 높이/너비 기준)하도록 다시 짜서 — 크롭도 안 되고 여백도 안 생기게 고침. `index.html`과 builder 미리보기 양쪽 다 동일 로직 적용.
- **블록에 동영상+사진이 섞여 있으면 사진이 클릭해야만 보이던 문제**: SSF SHOP의 Brand Film/Portfolio Film/Digital Film, 갤럭시탭 S9의 결과 편/비교 편 — 전부 Vimeo 링크가 첫 항목이라 기본 "1장씩" 모드로 사진이 클릭 뒤에 숨어 있었음. 이 5개 블록만 `galleryMode:"all"`로 지정해서 동영상+사진이 한 화면에 같이 보이게 함(범위는 "동영상+사진 섞인 블록만" — 사이트 전체 기본값은 안 건드림, 사용자 확인받고 진행).
  - 겸사겸사 갤럭시탭 S9 "결과 편" 블록의 사진 하나도 폴더 경로(`Galaxy Tab S9/`)가 빠져 있던 걸 같이 고침 — 예전 히어로 버그와 같은 종류의 실수.
- **커밋 `ac4ccb7`(레터박스/0×0/히어로 경로), `4f4acb3`(동영상+사진 섞인 블록 all모드)** — `ac4ccb7`은 이 세션 중 정상 push됨. **`4f4acb3`은 로컬 커밋만 되고 push는 안 됨** — 이 환경의 아웃바운드 네트워크 연결이 일시적으로 끊겨서(`github.com`/`google.com` 등 전부 연결 타임아웃, DNS는 되는데 TCP 연결이 안 됨) push가 계속 실패함. **다음 세션에서 제일 먼저 `git push`부터 시도해서 이 커밋을 올려야 함** — 로컬엔 안전하게 남아있음.
## 세션 요약 — 빌더 사진/동영상 편집 기능 대거 추가 (2026-07-27)

이번 세션에서 builder.html/index.html/server.py에 만든 기능들. 전부 커밋·푸시 완료(사용자가 대화 도중 여러 번 Publish를 눌러서 각 변경이 "Publish: ○○" 커밋에 섞여 들어감 — 기능별로 깔끔하게 분리된 커밋은 아니지만 전부 origin/main에 반영돼 있음).

- **사진 크롭 탭**: 블록의 각 사진 옆 "크롭" 버튼 → Free/1:1/4:5/9:16/4:3/16:9 비율 선택 + 픽셀 크기 표시("1,080 x 1,920px로 잘려요") + Cancel/Crop 버튼. 크롭은 **서버(`server.py`의 `/crop`, Pillow)에서 처리** — `file://` 이미지를 `<canvas>`에 그리면 "tainted canvas" 에러가 나서 브라우저에서는 픽셀을 못 읽기 때문. 크롭 결과는 원본을 덮어쓰지 않고 `-crop.ext`로 새 파일 저장. 애니메이션 GIF·AVIF·WEBP 전부 확인됨.
  - **되돌리기 버튼**: 크롭하면 크롭 전 원본(파일/경로)을 그 이미지 항목에 한 번만 저장해두고(재크롭해도 최초 원본 유지) "되돌리기" 버튼이 뜸 — 누르면 원상복구, 버튼도 사라짐. 블록 사진·히어로 이미지 둘 다 적용.
- **히어로 이미지 완전 분리**: 예전엔 히어로가 블록 사진 하나를 그냥 가리키는 참조라서, 그 사진을 크롭하면 블록에서 쓰이는 원본도 같이 바뀌는 문제가 있었음. 이제 히어로는 `project.heroImage`라는 독립된 슬롯(블록 이미지의 복사본)이라 히어로만 따로 크롭해도 다른 곳엔 영향 없음. "기존 이미지에서 선택"(드롭다운, 고르면 복사돼서 들어감) 또는 "새 이미지 첨부"(파일 업로드) 둘 중 하나로 설정 가능.
- **블록에 동영상 첨부**: "동영상 추가" 버튼 → 파일 첨부 / 링크 첨부(Vimeo·YouTube·직접 mp4) 두 탭. **자동 웹 최적화**: 파일이든 (Vimeo/YouTube가 아닌) 직접 링크든 서버에서 ffmpeg로 H.264/AAC mp4 재인코딩(최대 1920px, faststart) 후 프로젝트 폴더에 저장 — 서버(`server.py`)에 `/optimize-video` 엔드포인트 추가. Vimeo/YouTube 링크는 이미 최적화돼 있으니 그대로 iframe 임베드.
  - **서버 의존성 추가**: `pip install pillow`(크롭용), `ffmpeg`(이미 시스템에 설치돼 있었음, 동영상 최적화용). 서버 재시작 필요할 때마다 `python3 server.py`로 재기동.
- **사진 레이아웃 커스터마이징**: 블록마다 "사진 레이아웃" 버튼 → 모달에 **1 / 2 / 3 / All / Free** 탭 + (Free 아닐 때) 가로/세로 배치 방향 토글 + 실시간 미리보기. **Free 탭에서는 미리보기 안에서 사진을 드래그해서 위치를, 모서리를 드래그해서 크기를 자유롭게 조절** 가능 — "완료"를 눌러야 실제 저장, "Cancel"은 그냥 닫힘. 빌더 미리보기와 실제 사이트(index.html) 렌더링 로직 양쪽 다 반영. 기존 프로젝트는 필드가 없으면 자동으로 "1장씩"(기존 모습)으로 처리되어 하위호환됨.
  - **이미지 비율 보존**: 2/3/All/Free 레이아웃 전부 `object-fit:contain`으로 통일 — 박스 크기와 사진 비율이 달라도 사진을 자르지 않고 레터박스(여백)로 처리. (처음엔 `cover`로 만들어서 사진이 잘렸는데 사용자 피드백으로 수정.)
- **빈 블록 검은 박스 제거**: 사진/동영상이 하나도 없는 블록은 갤러리 영역 자체를 렌더링하지 않음(빌더 미리보기·실제 사이트 둘 다) — 예전엔 빈 검은 박스가 그대로 보였음.
- **소셜 아이콘(유튜브/비메오/인스타) 색상·모양 수정**: 비메오/인스타를 PNG 이미지로 썼더니 프로젝트 색상 설정이 안 먹혀서, 한 번은 손으로 그린 SVG로 바꿨었는데 그게 옛날 로고 모양이라 이상해 보였음. 최종적으로 **CSS `mask-image` 방식**으로 정리: 흰색 PNG(`icons/youtube.png`/`vimeo.png`/`instagram.png`)를 모양(마스크)으로만 쓰고 실제 채워지는 색은 `currentColor`(프로젝트 텍스트/아이콘 색상)를 따름 — 모양은 정확한 실제 로고 그대로 유지하면서 색은 자유롭게 바뀜. 크기도 비메오 비율(17×15) 기준으로 셋 다 통일.
- **사진/동영상을 다른 블록으로 드래그 이동**: 예전엔 시각적으로는 다른 블록 리스트로 옮겨져 보여도 실제 데이터는 원래 블록에서 안 지워지거나 아예 유실되는 버그가 있었음. `syncOrderFromDOM`이 이미지 하나를 옮길 때 **모든 블록의 이미지 풀에서 찾아서 옮기고, 원래 블록에서는 제거**하도록 고쳐서 이제 정상적으로 "이동"됨(복제도 유실도 없음).
- **사이드바 프로젝트명 오버플로 버그 수정**: 제목이 길면(`HAPPY NEW YEAR SERIES 2022` 등) 박스 밖으로 텍스트가 삐져나오던 문제 — `.info`/`.p-title`/`.p-sub`가 실은 인라인 `<span>`이라 `overflow:hidden`이 적용 안 되던 게 원인. `.info`를 세로 flex로, 제목/서브 텍스트를 block으로 고쳐서 제목은 "…"로 잘리고 카테고리·연도는 항상 깔끔한 한 줄로 나옴.
- **빌더 이름 변경**: 탭 제목/좌측 상단 로고 문구를 "Portfolio Builder" → "jjaejae Builder"로 변경.

## 세션 마무리 요약 (2026-07-26, 오후 11시경 종료)

오늘 세션은 여기서 마무리. 다음 세션에 이어서 볼 사람을 위한 요약:

- **jjaejae.com 도메인 정상 연결 완료**: Cloudflare 네임서버를 유지한 채(등록·결제가 Cloudflare라서), Cloudflare DNS에 A(`@` → `76.76.21.21`)와 CNAME(`www` → `cname.vercel-dns.com`) 레코드를 DNS-only(회색 구름)로 추가해서 해결. `curl -I https://jjaejae.com`으로 HTTP/2 200·Vercel 서빙·SSL 정상 확인함. **더 이상 도메인 이슈 없음.**
- **모바일 갤러리 사진 잘림 버그 수정**(커밋 `c5d51dd`): `.gallery`의 `min-height:280px`가 좁은 화면에서 가로로 넓은 사진의 JS 계산 높이보다 커서 사진이 강제로 늘려지고 `overflow:hidden`에 잘려 보이던 문제. 모바일(`max-width:760px`)에서 `min-height:0`으로 제거해서 해결. 라이브 배포 후 Puppeteer로 재검증하려던 중 세션 종료 — **다음에 이어서 하면 `https://jjaejae.com/galaxy-tab-s9`를 모바일 뷰포트로 열어 Campaign 블록 사진이 안 잘리는지 최종 확인 필요.**
- **"Brand Film 이미지가 안 보인다"는 반복 신고**: 두 차례 모두 실제 배포 서버(캐시 우회 curl)에는 이미 고쳐진 버전이 올라가 있는 것으로 확인됨 — 브라우저(특히 모바일 Safari)의 캐시/뒤로가기 캐시(bfcache)가 예전 상태를 보여주고 있었을 가능성이 높음. 사용자에게 탭을 완전히 새로 열거나 사이트 데이터 삭제 후 재확인 요청해둔 상태 — **여전히 안 보인다는 얘기가 다시 나오면 이번엔 캐시 문제가 아니라 진짜 버그일 수 있으니 처음부터 재조사 필요.**
- **사용자가 병행 작업 중인 것**: 대화 도중 사용자가 별도 탭에서 builder.html을 직접 사용해 Publish를 여러 번 실행함(HNY 2022 프로젝트, Galaxy Tab S9 크레딧, 갤러리 표시 모드 "1장/여러 장/전체 보기" 기능 등). 이 커밋들은 이미 git에 반영되어 있고 내가 만든 변경과 충돌 없이 잘 합쳐짐 — 다음 세션에서 `git log`로 최근 히스토리 한번 훑어보면 그 사이 무슨 기능이 추가됐는지 파악 가능.
- **로컬 테스트 팁**: 이번 세션에서 Puppeteer(headless Chrome, `puppeteer-core` + 로컬 설치된 Chrome 사용)로 실제 브라우저 동작(콘솔 에러, 이미지 로드, 스크롤, 클릭, 라우팅)을 자동 검증하는 방식을 여러 번 씀 — 스크린샷 주장만으로 판단하기 애매할 때 유용. 로컬 `file://`은 pushState 라우팅이나 SPA rewrite 재현이 안 되니, 그런 걸 테스트할 땐 실제 라이브 도메인(`https://jjaejae.com`)을 직접 여는 게 가장 정확함(로컬 파이썬 SPA 서버로 대체 시도했다가 Chrome과의 keep-alive 이슈로 계속 hang — 굳이 다시 시도할 필요 없음).

## 최근 변경 — builder.html 편의 기능 모음 (2026-07-26)

- **프로젝트 순서 변경**: 사이드바 프로젝트 항목에 드래그 핸들(`⠿`) 추가 — 블록/사진과 같은 방식으로 드래그해서 순서 변경. `project.order` 필드로 저장되며, 기존 프로젝트는 최초 로드 시 자동으로 order가 채워짐(마이그레이션). 단, 이건 빌더 안에서의 표시 순서만 바꾸는 것 — Publish는 여전히 프로젝트 단위로만 index.html을 갱신하므로, 순서를 실제 사이트에 반영하려면 별도 작업이 필요함(아직 안 만듦).
- **카테고리 탭 UI**: "카테고리"(=`project.tag`, 예: Campaign/Digital Campaign) 필드가 텍스트 입력창 대신 **알약 버튼 탭**으로 바뀜. 기존에 쓰인 카테고리가 자동으로 버튼화되고, "+ 새 카테고리"로 새 값 추가 가능(값은 `localStorage`의 `pf-builder-custom-tags`에 저장). 각 알약 우상단에 ✕ 버튼으로 삭제 가능 — 삭제하면 그 카테고리를 쓰던 다른 프로젝트들의 카테고리도 함께 비워짐(확인창 있음).
- **타이포그래피 섹션 자동 닫힘 버그 수정**: 폰트 파일을 첨부하면 `renderEditor()`가 전체를 다시 그리면서 접힘 상태가 매번 기본값(닫힘)으로 리셋되던 문제 — 열림/닫힘 상태를 `openSections`(Set)로 별도 추적하도록 고침.
- **크레딧 이미지 → 데이터 반영**: Galaxy Tab S9 프로젝트의 실제 크레딧 슬라이드 이미지를 읽어서 크레딧 22개 항목을 builder에 입력함.
- (참고) 블록을 자유롭게 추가하는 기능은 이미 있었음 — 편집 패널 "블록 (사진 세트)" 섹션 제목 옆 **"+ 블록 추가"** 버튼.

## 최근 변경 — Info/프로젝트 진입 시 주소창 URL 변경 (2026-07-26)

지금까지는 Info나 프로젝트를 열어도 전부 JS로 화면만 바뀌는 싱글 페이지 방식이라 주소가 `jjaejae.com` 그대로였음. 사용자가 "왜 jjaejae.com/info 처럼 안 바뀌냐"고 질문 → **해시(#) 방식 대신 깔끔한 경로**(`jjaejae.com/info`, `jjaejae.com/ssf-shop`)를 선택해서 구현.

- **`vercel.json` 신규 생성**: `{"rewrites":[{"source":"/((?!.*\\.).*)","destination":"/index.html"}]}` — 점(`.`)이 없는 경로(즉 `/info`, `/ssf-shop`처럼 확장자 없는 라우트)는 전부 `index.html`로 되돌려주고, 점이 있는 경로(이미지·폰트·아이콘 등 실제 파일)는 그대로 서빙됨. 이게 있어야 `/info`를 새로고침하거나 링크를 공유해도 정상적으로 열림(정적 사이트라 서버 라우팅이 따로 없어서 이 rewrite가 필수).
- **index.html JS 라우팅**: `history.pushState`로 주소를 바꾸는 `goHome()`/`goInfo()`/`goProject(id)` 래퍼 함수를 추가하고, 기존에 `openProject`/`closeProject`/`scrollIntoView`를 직접 부르던 곳(Work 카드 클릭, 헤더 nav, 프로젝트 상세뷰의 이전/다음/All Work 링크, 닫기 버튼, Esc 키)을 전부 이 래퍼로 교체. `popstate` 이벤트(뒤로/앞으로가기)와 최초 페이지 로드 시에는 `applyRouteFromLocation()`이 현재 `location.pathname`을 읽어 그에 맞는 화면을 열어줌(과거의 `location.hash` 딥링크 방식은 fallback으로 남겨둠).
- **로컬 `file://`에서 테스트할 때 주의**: `history.pushState`로 주소는 바뀌지만(예: `file:///ssf-shop`), 그 상태에서 새로고침하면 실제 로컬 파일이 없어서 에러가 남 — 이건 정상이고, 실제 배포 환경(Vercel + 위 rewrite)에서만 새로고침/직접 접속이 정상 동작함. Puppeteer로 클릭→URL 변경→뒤로가기→복귀까지 전부 확인함(에러 0건).

## 최근 변경 — 파비콘 추가 + 오늘 작업 GitHub/Vercel에 직접 push (2026-07-26)

- **파비콘**: `jjaejae.jpg`(검정 배경+흰 워드마크, 로고 이미지화 때 썼던 그 원본)를 32/192/512px로 리사이즈해서 `favicon-32.png`/`favicon-192.png`/`favicon-512.png`로 저장, `<head>`에 `<link rel="icon">` 3개(사이즈별) + `<link rel="apple-touch-icon">` 추가.
- 이번 대화에서 쌓인 로컬 변경(SUIT 폰트, 블록 캡션 위치, 코너 프레임 Email→Top, 조건부 소셜 박스, Vimeo/Instagram 로고, 파비콘)을 **직접 `git add`+`commit`+`push`**함(평소엔 builder.html의 PUBLISH 버튼이 하는 일이지만, 이번엔 index.html/builder.html을 직접 편집한 내용이라 수동으로 처리). 커밋 `b727792`, `origin/main`에 푸시 완료 → Vercel이 자동 재배포.
- **확인된 사항**: `vercel domains inspect jjaejae.com` 결과 `jjaejae.com`/`www.jjaejae.com` 도메인이 이 프로젝트(`jjaejae-portfolio-2026`)에 연결은 되어 있으나, **네임서버가 아직 Cloudflare로 되어 있고 Vercel이 요구하는 네임서버(ns1/ns2.vercel-dns.com)로 안 바뀌어 있음**(`✘` 표시). 이 상태면 `jjaejae.com`이 아직 이 배포로 정상적으로 연결 안 됐을 수 있음 — DNS/네임서버 설정은 도메인 등록기관(Cloudflare) 쪽에서 변경해야 하는 부분이라 별도 확인 필요. 우선 기존 라이브 URL(`https://jjaejae-portfolio-2026.vercel.app`)에서는 바로 확인 가능함.

## 최근 변경 — Vimeo/Instagram 소셜 아이콘을 실제 로고 이미지로 교체 (2026-07-26)

프로젝트 우하단 소셜박스(`.p-social`)의 Vimeo·Instagram 아이콘을 인라인 SVG에서 사용자가 준 실제 로고 이미지로 교체.
- **Instagram**(`~/Downloads/instagram-white-icon.webp`): 이미 배경 투명 + 흰색 아이콘이라 그대로 PNG로 재저장만 함.
- **Vimeo**(클립보드 스크린샷, 어두운 배경+흰 "v" 로고): numpy로 밝기 기반 알파 마스크를 만들어(전에 jjaejae 로고 만들 때와 같은 방식) 배경을 투명하게 벗겨내고 흰색 글리프만 남김.
- 최종 파일은 `2026 PF/icons/instagram.png`, `2026 PF/icons/vimeo.png`(루트 아래, `.vercelignore`에 안 걸림). `socialIcon()`(index.html)과 `SOCIAL_ICONS`(builder.html) 둘 다 이 두 타입만 `<img>` 태그로 바꿈 — YouTube는 로고를 안 주셔서 기존 인라인 SVG 그대로 둠.
- Puppeteer로 두 아이콘 다 정상 로드(에러 0건) 확인, 소셜박스 스크린샷으로도 육안 확인함.

## 조사 — "Brand Film 이미지 안 뜨는 오류" 재현 안 됨 + 블록 본문 Medium 적용 (2026-07-26)

- **재현 시도**: Puppeteer(headless Chrome)로 `index.html#ssf-shop`을 직접 열어 `#project-view`를 프로그래밍적으로 스크롤(hash 딥링크만으로는 안 열려서 실제 스크롤 위치까지 재현), 콘솔/네트워크 에러·이미지 로드 상태·`.gallery` 배경색/치수를 전부 확인함. **에러 0건, 이미지 정상 로드(1920×1080), 갤러리 배경/치수 정상**으로 현재 파일에서는 문제가 재현되지 않음(스크린샷으로도 확인).
- **추정 원인**: 대화 도중 사용자가 별도 브라우저 탭에서 builder.html의 **PUBLISH 버튼**을 여러 번 눌러(`git log` 확인 결과 "Publish: HAPPY NEW YEAR SERIES 2022" 커밋이 21:45, 22:12에 발생) `server.py`가 `git add -A`로 로컬 폴더 전체를 커밋·푸시함 — 이 과정에서 그 시점까지의 내 작업 중간 상태(예: 블록 캡션을 사진 위에 겹치는 오버레이로 잘못 구현했던 버전)가 같이 커밋되어 라이브 사이트에 반영됐을 가능성이 있음. 사용자가 스크린샷을 찍은 시점이 그 중간 상태였을 수 있음. 현재는 `git status`가 clean하고 로컬=원격=최신 커밋이 모두 일치하며, 그 안에 이미 캡션 위치 정정본이 포함되어 있어 문제 없음.
- **결론**: 지금 파일 기준으로는 정상 동작 확인함. 그래도 다시 한번 라이브 사이트(https://jjaejae-portfolio-2026.vercel.app)에서 강력 새로고침(캐시 무시) 후 재확인 권장 — 그래도 안 뜨면 바로 알려주면 재조사.
- **블록 본문 Medium 굵기 적용**: `.p-block-body`(블록 문단 텍스트)에 `font-family:var(--font-medium)` 추가. `index.html`/`builder.html` 미리보기 양쪽 다 반영, Puppeteer로 `getComputedStyle` 확인해서 `SUIT-Medium`이 실제 적용되는 것까지 검증함.

## 최근 변경 — 블록 캡션 위치 정정 + 크레딧 이름 폰트 (2026-07-26)

바로 아래 항목("블록 캡션 이미지 오버레이")에서 "사진 위로"를 사진에 겹치는 오버레이로 잘못 이해해서 구현했었음 — 사용자가 정정: 사진 위에 **겹치는 레이어가 아니라, 페이지 안에서 사진보다 위쪽(먼저)에 일반 텍스트로 배치**해달라는 의미였음.
- `.p-block-overlay`(절대위치+그라데이션 스크림)를 제거하고, 캡션/헤딩/본문을 다시 **일반 문서 흐름**으로 되돌림 — 순서는 `.p-caption`(라벨+번호, 하단 보더) → `.p-block-heading`(볼드) → `.p-block-body`(문단, 어두운 텍스트) → `.gallery`(사진, `margin-top:26px`). 흰 글자/그라데이션 스타일은 필요 없어져서 제거, 클래스명도 `.p-overlay-*`에서 `.p-block-heading`/`.p-block-body`로 정리.
- `index.html`과 `builder.html` 미리보기 양쪽 다 되돌림.
- **크레딧 이름 폰트**: `/Users/cheil/Library/Fonts/SUIT-Medium.otf`를 WOFF2로 변환(`fonts/SUIT-Medium.woff2`, 197KB) 후 `--font-medium` 변수로 등록, `.credit-item .value`(크레딧의 이름/값 부분, 예: "Seyoon Kim")에 적용. 라벨("Creative Director" 등)은 기존 스타일 유지.
- **TODO**: 우하단 프로젝트 소셜 박스의 Vimeo/Instagram 아이콘을 "첨부된 로고 형태"로 바꿔달라는 요청이 있었는데, 실제로 이미지가 첨부되지 않아 보류 중 — 다음 세션에서 이미지 확인 후 처리.

## 최근 변경 — 블록 캡션 이미지 오버레이 + SUIT 폰트 적용 (2026-07-26)

- **프로젝트 상세뷰 블록 캡션을 이미지 위로**: 각 블록(사진 세트)의 라벨(`Brand Film` 등)·헤딩·본문이 기존엔 사진 **아래**에 별도 텍스트로 있었는데, 이제 사진(`.gallery`) **위에 얹히는 오버레이**로 바뀜. `.p-block-overlay`가 `.gallery` 안에 절대 위치로 들어가고, 하단에서 위로 어두워지는 그라데이션(`linear-gradient(...rgba(0,0,0,.82) 100%)`)을 깔아 흰 글자가 사진 위에서도 읽히게 함. 오버레이는 `pointer-events:none`이라 클릭은 그대로 사진(`rollGallery`)에 전달됨. 기존 `.p-body`/아래-배치 관련 CSS는 `.p-overlay-heading`/`.p-overlay-body`(흰 글자용)로 교체. `galleryHTML()`에 `overlayHTML` 파라미터 추가, `renderProject()`의 블록 렌더 로직도 그에 맞게 정리. **builder.html의 미리보기(`buildPreviewBodyHTML`)와 PREVIEW_CSS도 동일하게 반영**.
- **SUIT 폰트 적용**: 사용자가 준 `SUIT-ExtraBold.otf`/`SUIT-ExtraLight.otf`(`~/Library/Fonts/`)를 Pillow 대신 **fontTools로 WOFF2 변환**해서 `2026 PF/fonts/SUIT-ExtraBold.woff2`(201KB), `SUIT-ExtraLight.woff2`(191KB)로 저장(원본 OTF 358KB/353KB보다 가벼움). `.vercelignore`에 안 걸리는 위치라 배포에도 포함됨.
  - `--font-bold`/`--font-thin` CSS 변수로 두 폰트 지정. `body` 기본값은 `--font-thin`(얇은 서체).
  - **굵은 서체 적용 대상**: `h1,h2,h3`(Work/Play 섹션 헤더, 프로젝트 타이틀 등), 마퀴 텍스트(`.lang-track span`), 사방 탭(`#site-header`, `.frame-item`), Work/Play 카드 이름(`.work-card .meta .name`), 블록 라벨(`.p-caption .label`), Info 태그라인(`.info-tagline`).
  - 그 외 본문/문단/메타 텍스트는 전부 `body`의 얇은 서체 그대로.
  - **builder.html 미리보기(iframe, `PREVIEW_CSS`)에도 동일한 @font-face + 굵은 서체 셀렉터를 반영**해서 실제 사이트와 어긋나지 않게 함(빌더 자체 UI 폰트는 그대로 둠 — 이건 도구 UI지 홈페이지가 아니라서 안 건드림).

## 최근 변경 — 코너 프레임 Email→Top 교체 + 프로젝트 소셜 아이콘 조건부 표시 (2026-07-26)

- **코너 프레임(사방 탭) 개편** — 사이트 전역에 적용(Work/Play 리스트, 프로젝트 상세뷰, Info 오버레이 등 프레임이 뜨는 모든 곳):
  - 좌하단이었던 `#frame-email`을 제거하고 그 자리에 **`#frame-ig`(IG 링크)**를 이동.
  - 우하단은 이제 **`#frame-top`("Top ↑" 버튼)** — 클릭하면 현재 활성 화면 기준으로 맨 위로 스크롤: 프로젝트 상세뷰가 열려있으면 `#project-view` 내부를, Info 오버레이가 열려있으면 `.info-scroll` 내부를, 그 외엔 `window`를 스크롤.
  - 프로젝트 상세뷰 첫 화면(`.p-main`)에서 숨기는 대상도 기존 브랜드로고/Email/IG에서 **브랜드로고/IG/Top**으로 갱신(자리가 바뀌었으므로).
- **프로젝트 자체의 소셜 아이콘(`.p-social`, 우하단 유튜브/비메오/인스타)**: 이제 `project.social`에 실제 URL이 채워진 것만 아이콘으로 렌더링하고(`"#"`나 빈 값은 제외), **하나도 없으면 `.p-social` 박스 자체를 렌더링하지 않음**. `index.html`에 `socialHTML(project)` 헬퍼 추가, `builder.html`의 실시간 미리보기(`buildPreviewBodyHTML`)도 동일한 필터링 로직으로 맞춤 — 빌더에서 채운 값이 없으면 미리보기에서도 그 아이콘/박스가 안 보임.

## 최근 변경 — 프로젝트 상세뷰 첫 화면에서 브랜드 로고/Email/IG 숨김 (2026-07-26)

프로젝트 상세뷰를 열면 첫 화면(`.p-main`)에 이미 자체 좌상단 컬러박스(제목)와 우하단 소셜 아이콘 박스가 있어서, 같은 위치에 뜨는 사이트 공용 `.brand-logo`(좌상단)·`#frame-email`(좌하단)·`#frame-ig`(우하단)이 겹쳐 보이던 걸 정리함.
- 프로젝트를 열면 이 3개는 완전히 숨김(`opacity:0`), 헤더 nav(우상단)와 `#frame-day`/`#frame-clock`(좌우 중단)은 기존처럼 40%로 그대로 보임.
- `#project-view`(`viewRoot`) 스크롤 이벤트로 스크롤량이 `.p-main` 높이의 **2/3를 넘으면** `body.project-past-hero` 클래스를 붙여서, 그 3개도 다시 기존 40%-옅게(호버 시 100%) 규칙으로 복귀. 프로젝트를 새로 열 때마다 이 클래스는 리셋됨.

## 최근 변경 — Work/Play 리스트 썸네일을 가로 비율로 (2026-07-26)

`.work-card .thumb`(Work/Play 공용, `#work-grid`/`#play-grid`의 카드)의 `aspect-ratio`를 세로형 `4/5`에서 **가로형 `16/10`**으로 변경. 2열 그리드 레이아웃은 그대로 유지.

## 버그 수정 — Galaxy Tab S9 이미지 로드 안 되던 문제 (2026-07-26)

원인: 크레딧 이미지 동기화 작업 때 개별 파일을 직접 업로드해서 넣은 이미지 10개(히어로 1개 + 블록 이미지 9개)의 `relPath`가 `"Galaxy Tab S9/"` 폴더 접두사 없이 파일명만 저장됨 (builder의 파일 업로드 핸들러는 `webkitRelativePath`가 없으면 `file.name`만 씀 — 폴더 스캔으로 넣은 이미지와 달리 접두사가 안 붙음). 그 결과 `index.html`에도 접두사 없는 잘못된 경로로 퍼블리시되어 브라우저가 사이트 루트에서 파일을 찾다가 실패.
- `index.html`의 galaxy-tab-s9 항목 이미지 경로 10곳에 `Galaxy Tab S9/` 접두사 직접 추가.
- builder의 IndexedDB(`pf-builder-db`)에 저장된 같은 프로젝트의 `relPath` 10곳도 동일하게 수정 — 그래야 다음에 Publish 눌러도 다시 깨지지 않음.
- 이후 Publish 흐름에서 다시 발생 가능: 개별 파일 업로드로 이미지를 추가할 땐 항상 폴더 접두사가 붙는지 확인 필요.

## 최근 변경 — Info 배경 사진, 톤 보정 없이 원본 그대로 (2026-07-26)

바로 아래 항목("Info 배경을 실제 사진으로 교체 + 톤 보정")에서 했던 하늘 톤 보정(밝기/채도/대비 낮추고 헤이즈 블렌드)을 되돌림 — 사용자가 원본 그대로의 진한 파란 하늘 그라데이션을 더 마음에 들어해서, `info-bg.jpg`를 **원본 `Untitled-1.jpg`을 색 보정 없이 그대로**(품질 90 JPG로만 재인코딩) 다시 저장함. 마스킹/블렌드 코드는 더 이상 쓰지 않지만 필요하면 이전 대화 기록에 남아있음. 그레인 오버레이(`.info-grain`, opacity .09)는 그대로 유지.

## 최근 변경 — Info 배경을 실제 사진(계란 빨래줄)으로 교체 + 톤 보정 (2026-07-26)

Info 오버레이 배경을 CSS 그라데이션 하늘에서 **사용자가 준 실제 사진**(`none/Ref/Untitled-1.jpg`, 빨랫줄에 걸린 계란후라이 사진)으로 교체.
- 원본은 색이 꽤 진하고 채도 높은 파란 하늘이라, 레퍼런스(`none/Ref/info.png`, Cecilia Pignocchi 톤)의 더 뿌옇고 채도 낮은 하늘색에 맞춰 **Pillow로 톤 보정**: 밝기 ×1.35, 채도 ×0.58, 대비 ×0.92, 옅은 회청색(`#d2d8d8`)과 14% 블렌드로 헤이즈 효과. 보정 전/후 하늘 색 샘플이 레퍼런스와 거의 일치하도록 값을 맞춤(둘 다 대략 RGB 135~140/173~176/193~197).
- **계란 색은 그대로 유지**: 처음엔 이미지 전체에 톤 보정을 걸어서 계란도 같이 washed-out 되는 문제가 있었음 → numpy로 "파란기(B - max(R,G))" 기반 마스크를 만들어(가장자리는 GaussianBlur로 페더링) **하늘(파란 픽셀)에만** 보정을 블렌드하고, 계란·나무 집게·빨랫줄(주황/노랑/미색 계열, blueness 낮음)은 원본 픽셀 그대로 통과시킴. 계란 중심 샘플 색이 보정 전/후 완전히 동일함을 확인함.
- 결과물은 `/Users/cheil/Desktop/jjaejae/Personal/2026 PF/info-bg.jpg`(루트, 110KB)로 저장 — **원본이 있던 `none/Ref/`는 `.vercelignore`에 걸려 배포에서 빠지므로, 반드시 이 파일처럼 루트에 저장해야 라이브 사이트에서 보임** (지난번 로고 PNG와 동일한 이유).
- `.info-bg`는 이제 이 JPG를 `background-image`로 사용(`background-size:cover; background-position:center 35%`), 기존 CSS 그라데이션 정의는 제거. 노이즈 레이어(`.info-grain`, SVG feTurbulence)는 그대로 유지하되 레퍼런스 사진의 그레인이 더 잘 보이는 편이라 opacity를 `.06 → .09`로 살짝 올림.

## 최근 변경 — 마퀴 줄 순서 변경 + 블랙 팔레트를 레퍼런스 색상으로 교체 (2026-07-26)

- **줄 순서**: 일본어 → 영어 → 한국어(가운데, 3번째 그대로 유지) → 프랑스어 → 스페인어로 재배치. `GREETINGS`와 `PALETTES`(5개 전부)의 배열 순서를 동일하게 다시 맞춤.
- **블랙 모노톤 팔레트 교체**: 사용자가 준 `mono-workshop-black-gray-color-palette.avif` 레퍼런스에서 실제 5색을 추출(`sips`로 PNG 변환 후 확인) — `#0A0B0D → #1C1F24 → #3A3F47 → #7F8792 → #E5E8ED`. 기존 흑백 교차 대신 **위(일본어)에서 아래(스페인어)로 어둡게→밝게 이어지는 그라데이션**으로 배치, 각 줄 글자색은 대비가 더 좋은 쪽(어두운 3줄은 밝은 글자, 밝은 2줄은 어두운 글자)으로 지정.

## 최근 변경 — 마퀴 클릭 시 컬러 팔레트 순환 (2026-07-26)

메인 마퀴(`#intro`) 클릭의 동작을 "Work로 스크롤"에서 **"5줄 색 팔레트 순환"**으로 바꿈(스크롤은 그대로 자연스럽게 Work로 이어지므로 기능이 사라지는 건 아님).
- 색은 더 이상 `GREETINGS` 배열에 하드코딩하지 않고, 새 `PALETTES` 배열(5개 팔레트, 각각 영어/스페인어/한국어/프랑스어/일본어 순서의 `{bg,fg}` 5쌍)에서 가져옴. `applyPalette(index)`가 `.lang-row` 5개의 인라인 배경/글자색을 갈아끼움.
- **팔레트 0 = 기존 컬러 조합 그대로**(요청대로 유지), **팔레트 1 = 블랙 모노톤**(검정/흰색 교차), 나머지 2~4는 선셋·쿨파스텔·어스톤 배리에이션. 마퀴를 클릭할 때마다 `paletteIndex`가 1씩 순환(0→1→2→3→4→0…), 새로고침하면 다시 팔레트 0으로 시작(저장 안 함 — 필요하면 나중에 localStorage로 유지시킬 수 있음).

## 최근 변경 — Work/Play 데이터 연동 (builder.html) (2026-07-26)

위 "Play" 탭/섹션 항목에서 남겨뒀던 TODO("PLAY_PROJECTS 데이터 배열/builder 편집 패널은 아직 없음")를 처리했습니다.

- **데이터 모델**: 프로젝트에 `category` 필드 추가(`"work"` | `"play"`, 기본값 `"work"`) — `newProject()`, `buildSSFSampleProject()`, `exportProjectCode()` 모두 반영. 기존에 저장된 프로젝트처럼 `category` 필드가 아예 없는 경우도 전부 "work"로 취급(하위호환, 마이그레이션 불필요).
- **index.html**: `PROJECTS`를 `category`로 나눠 `#work-grid`/`#play-grid`에 각각 렌더링. Play 프로젝트가 하나도 없으면 기존 "Coming soon" 문구 그대로 유지.
- **builder.html**: 좌측 사이드바 맨 위에 **Work / Play 탭 버튼**을 추가 — 클릭하면 그 섹션에 속한 프로젝트만 목록에 표시. "+ 새 프로젝트"는 현재 활성 탭의 섹션으로 생성됨. 편집 패널에도 프로젝트명 바로 아래 **"섹션 (Work / Play)"** 토글을 추가해서 기존 프로젝트도 언제든 섹션을 바꿀 수 있음(바꾸면 사이드바 목록에서 즉시 다른 탭으로 이동).
- Publish를 누르면 `category` 필드도 그대로 index.html에 반영되므로, Play 프로젝트를 채우고 Publish하면 실제 사이트의 `#play-grid`에 나타남.

## 최근 변경 — "Play" 탭/섹션 추가 (2026-07-26)

헤더 nav에 Work 옆 **"Play"** 탭을 추가(순서: Work → Play → Info). Work 섹션 바로 아래에 `#play` 섹션을 새로 만들고(`#play-grid`), Work와 동일한 2열 그리드/카드 CSS를 공유하도록 셀렉터를 `#work-grid, #play-grid` 등으로 일반화함. 지금은 `#play-grid` 안에 "Coming soon" 플레이스홀더 문구만 있음 — **실제 개인 작업물 콘텐츠는 사용자가 빌더에서 채울 예정** (아직 `PLAY_PROJECTS` 같은 데이터 배열이나 builder.html 쪽 편집 패널은 만들지 않음, 프론트엔드 라우팅/레이아웃만 준비된 상태). 다음에 이어서 할 일 목록에 추가해둠.

## 최근 변경 — Info 카피 다듬기 + 스크롤 버그 수정 + 브랜드 로고 이미지화 (2026-07-26)

- **Info 카피**: 첫 인사 문장에서 "AKA. jjaejae" 삭제(그냥 "아트디렉터 최재훈입니다."). 두 태그라인("A RAW NAME FOR SHARP IDEAS", "LOOK AGAIN")을 큰 헤드라인 크기에서 **본문과 같은 크기 + 볼드**로 축소(`.info-tagline` 폰트 크기를 `.info-body`와 동일하게). "LOOK AGAIN"의 대괄호 `[ ]` 제거.
- **Info 오버레이 스크롤 버그**: 내용이 길어 스크롤할 때 macOS/iOS의 elastic bounce로 뒤에 있는 Work 리스트(SSF SHOP 카드 등)가 살짝 비쳐 보이던 문제 수정. `#info-view` 자체는 이제 `overflow:hidden`(절대 스크롤 안 됨, 배경이 항상 뷰포트 전체를 덮음)이고, 그 안에 새로 넣은 `.info-scroll` 래�퍼만 `overflow-y:auto; overscroll-behavior:contain`로 스크롤을 전담 + 바깥으로 스크롤 체이닝 안 되게 막음. JS `openInfo()`/`closeInfo()`에서도 `document.documentElement.style.overflow`를 직접 토글해 이중 안전장치를 둠.
- **브랜드 로고 이미지화**: 좌상단 "jjaejae" 텍스트를 사용자가 준 실제 로고(`jjaejae.jpg`, 검정 배경+흰색 워드마크)에서 **배경 투명 + 워드마크 블랙**으로 가공한 PNG로 교체. 가공은 Pillow로 luminance 값을 알파 채널로 변환해서 만듦(`/Users/cheil/Desktop/jjaejae/Personal/2026 PF/jjaejae-wordmark-black.png`, 691×387, 원본 대비 여백 크롭됨). 헤더(`#site-header`)는 `mix-blend-mode:difference`라서 검정 이미지를 넣으면 안 보이게 되므로, **브랜드 로고는 `#site-header` 밖으로 빼서 별도의 `.brand-logo` 요소**(블렌드 모드 없음, 항상 순수 검정)로 만듦 — 표시/숨김 규칙(메인 마퀴에서 숨김 → past-hero 시 100%, project/info 오버레이에서 40%)은 헤더·코너 프레임과 동일하게 공유.

## 최근 변경 — Info 오버레이 카피 교체 (2026-07-26)

Info 오버레이 본문을 "jjaejae" 이름 유래·매니페스토 톤의 실제 카피로 교체함. 구조를 3단계 클래스로 나눔:
- `.info-lead` — 첫 인사 문장(700, 중간 크기)
- `.info-body` — 일반 본문 문단(500, 작은 크기, 여러 개)
- `.info-tagline` — "A RAW NAME FOR SHARP IDEAS", "[ LOOK AGAIN ]" 같은 굵은 독립 헤드라인(800, 큰 크기)
기존에 있던 `.bio`/`.hl`(밑줄 강조 span) 클래스는 이 구조로 대체되어 제거됨. 이후 카피를 더 수정하고 싶으면 `#info-view` 안의 `.info-content` 블록(HTML)만 편집하면 됨 — 새 문단/태그라인을 추가할 때는 `.info-body`/`.info-tagline` 클래스를 그대로 재사용.

## 최근 변경 — 디테일 페이지에서 코너 프레임 40% 옅게 (2026-07-26)

Work 리스트(마퀴만 지난 상태, `body.past-hero`)에서는 헤더/코너 프레임이 기존처럼 100% 또렷하게 보이지만, **프로젝트 상세뷰(`body.project-open`)나 Info 오버레이(`body.info-open`)에 들어가면 opacity 0.4로 옅어지도록** 분리함. 마우스를 올리면(`:hover`) 다시 opacity 1로 또렷해짐. `mix-blend-mode:difference`는 그대로 유지해서 어떤 배경색 위에서도 읽히긴 하되, 배경 위에 자연스럽게 얹히는 느낌을 줌.

## 최근 변경 — 코너 프레임(요일/시계/Email/IG) 추가 (2026-07-26)

레퍼런스(Cargo 템플릿)처럼 화면 네 모서리·중간 사이드에 작은 UI 텍스트를 띄우는 "프레임" 요소를 추가했습니다.
- 위치: 좌측 중단 `#frame-day`(요일, 예: "Saturday"), 우측 중단 `#frame-clock`(HH:MM:SS 실시간 시계), 좌하단 `#frame-email`(mailto 링크), 우하단 `#frame-ig`(Instagram 링크). 모두 `.frame-item` 클래스 공유, `mix-blend-mode:difference`로 헤더와 동일한 방식.
- **표시 규칙은 기존 헤더와 동일**: 메인 마퀴 화면에서는 안 보이고, `body.past-hero`(스크롤로 마퀴를 벗어남) / `body.project-open`(프로젝트 상세뷰) / `body.info-open`(Info 오버레이) 중 하나라도 켜지면 페이드인. 즉 "메인 롤링 화면에는 없고, 스크롤하거나 다른 페이지에 들어갔을 때 항상 떠있게" 요구사항 그대로.
- 요일/시계는 JS `setInterval(tickFrameClock, 1000)`으로 매초 갱신. 모바일(≤600px)에서는 요일/시계를 숨겨 좁은 화면에서 겹치지 않게 함(Email/IG는 유지).

## 최근 변경 — Info를 풀스크린 오버레이로 부활 (2026-07-26)

앞서 삭제했던 소개글을 **헤더 nav의 "Info" 버튼 클릭 시 열리는 풀스크린 오버레이**(`#info-view`)로 다시 추가했습니다 (스크롤로 도달하는 섹션이 아님).
- 배경은 실제 사진 대신 **CSS만으로 만든 하늘색 그라데이션 + SVG feTurbulence 노이즈 오버레이**(`.info-bg` + `.info-grain`, opacity .06, mix-blend-mode:overlay)로 레퍼런스의 "약간 노이즈 있는 하늘 사진" 느낌을 재현. 실제 이미지 파일은 쓰지 않음 — 나중에 진짜 사진으로 교체하고 싶으면 `.info-bg`의 `background`를 이미지로 바꾸면 됨.
- 내용은 큰 좌측 정렬 본문 텍스트(`Hey! 안녕하세요...`) + 일부 단어 이탤릭+밑줄 강조(`.hl`, 삼성/나이키/SSF SHOP) + Instagram/Email 링크. 이름(jjaejae)과 "Work/Info" 텍스트는 오버레이 안에 따로 안 넣고 **항상 떠 있는 헤더를 그대로 재사용**(레퍼런스처럼 좌상단 이름·우상단 nav 구조).
- `openInfo()`/`closeInfo()` 함수 추가, `data-go="info"` 클릭 시 스크롤 대신 오버레이를 열도록 라우팅 분기. Esc 키·다른 nav 클릭(Work/브랜드)으로 닫힘. `z-index:450`(헤더 500보다 낮음, project-view 400보다 높음)이라 오버레이가 열려 있어도 헤더는 계속 보이고 클릭 가능.
- 헤더 자체의 "메인 화면에서 숨김 → 스크롤 지나면 표시" 로직(`past-hero`)은 그대로 유지됨 — Info 버튼도 스크롤을 지나야 보임.

## 최근 변경 — Info 섹션 제거 + 헤더 스크롤 연동 (2026-07-26)

- 이름/소개/링크가 있던 `#info` 섹션을 **완전히 삭제**. 이제 구조는 `#intro`(마퀴) → 스크롤 → `#work`(Work 리스트)로 단순화됨.
- `#intro`(마퀴 화면)를 클릭하면 `#work`로 스무스 스크롤(`introEl.addEventListener("click", ...)`). 자연스러운 스크롤로도 바로 Work가 이어짐(사이에 있던 info가 없어졌으므로).
- 헤더(`#site-header`)는 이제 **메인(마퀴) 화면에서는 완전히 숨김**(`opacity:0`)이고, `IntersectionObserver`로 `#intro`가 뷰포트에서 벗어나면 `body.past-hero` 클래스를 붙여 페이드인. 프로젝트 상세뷰가 열려있을 때(`body.project-open`)도 항상 보이게 처리.
- 헤더 텍스트를 볼드(brand 800, nav 700)로, "Info" 탭은 섹션 삭제와 함께 제거(현재 nav는 "Work"만 남음). 브랜드 텍스트 "Jaehoon Choi" → **"jjaejae"**로 변경(헤더, `<title>`, 푸터 저작권 표기 모두). 단, SSF SHOP 크레딧의 실명 "Jaehoon Choi (@jjaejae__)"는 실제 크레딧 정보라 그대로 둠.

## 최근 변경 — 마퀴 튜닝 + 프로젝트 메인 박스 디자인 (2026-07-26)

- **마퀴**: 폰트 크기를 `vw` 대신 `svh` 기준(`clamp(2.6rem, 16svh, 9rem)`, `line-height:1`)으로 바꿔 각 줄(row) 높이를 거의 꽉 채우도록 키움. 5줄 속도(duration)도 전체적으로 더 느리게 상향(예: 한국어 64→84, 영어 76→100 등).
- **줄 순서/컬러**: 한국어가 5줄 중 정확히 가운데(3번째)에 오도록 배열 순서를 `영어 → 스페인어 → 한국어 → 프랑스어 → 일본어`로 재배치. 스페인어는 레드(`#c62828`), 프랑스어는 블루(`#3f6fd1`)로 변경, 일본어는 기존 연두색(`#e3e07a`)을 넘겨받음(5색 팔레트 재사용, 새 색상 추가 없음). 방향 교차(`reverse`)는 이제 언어별 수동 플래그가 아니라 **줄 인덱스 홀/짝으로 자동 계산**하도록 `renderGreetings()` 로직을 정리함.
- **프로젝트 메인 박스**(`.p-colorbox`, `.p-social`): 기존 라운드 코너(`border-radius:var(--pradius)`)를 없애고, 사용자가 준 레퍼런스(Cargo 스타일 카드) 그대로 **각진 모서리 + 3px 블랙 보더 + 블러 없는 오프셋 그림자**(`box-shadow:10px 10px 0 #111` / 소셜박스는 `8px 8px 0`, 모바일은 2px 보더·6px 그림자)로 교체. 배경색 자체는 그대로 `project.color`를 써서 빌더에서 커스터마이징 가능. `--pradius` CSS 변수는 더 이상 쓰이지 않아 제거함.
- **builder.html도 동일하게 수정**: 미리보기 CSS(`.p-main`/`.p-colorbox`/`.p-social`, 1223~1235번째 줄 근처)에 같은 각진+오프셋그림자 스타일을 반영해 실제 사이트와 미리보기가 어긋나지 않게 함.

## 최근 변경 — Publish 버튼 + GitHub/Vercel 배포 연동 (2026-07-26)

builder.html의 각 프로젝트 편집 화면에 **PUBLISH** 버튼을 추가했습니다. 누르면 그 프로젝트 데이터가 실제 `index.html`의 `PROJECTS` 배열에 반영되고, GitHub에 커밋·푸시되어 Vercel이 자동 재배포합니다.

- **GitHub**: public 저장소 `https://github.com/jjaejae-stack/jjaejae-portfolio-2026` — 이 폴더(`2026 PF`) 전체가 git 저장소입니다 (451MB, 이미지/영상 포함해서 그대로 커밋). `gh` CLI로 로그인되어 있고 (`gh auth status`로 확인), `gh auth setup-git`로 git push 인증도 연결해둠.
- **Vercel**: 프로젝트명 `jjaejae-portfolio-2026`, GitHub 저장소와 연결되어 **push할 때마다 자동으로 프로덕션 배포**됩니다. 라이브 URL: **https://jjaejae-portfolio-2026.vercel.app**
- **`.vercelignore`**: `builder.html`, `server.py`, `progress.md`, `Ref/`, `TXT/`는 Vercel 배포에서 제외됩니다 (git 저장소에는 포함되지만 공개 사이트에는 안 올라감 — `builder.html`을 라이브 도메인에서 직접 열어도 404).
- **동작 방식** (`server.py`의 `/publish` 엔드포인트):
  1. builder.html이 기존 "코드 내보내기"에 쓰던 `exportProjectCode()`로 프로젝트 JS 객체 텍스트를 생성해 서버로 전송
  2. 서버가 `index.html`을 문자열 단위로 파싱(주석/문자열을 건너뛰는 괄호 매칭)해서, 같은 `id`를 가진 항목이 `PROJECTS` 배열에 이미 있으면 **교체**, 없으면 **배열 끝에 추가**
  3. `git add -A && git commit && git push` 실행 (변경 없으면 커밋 생략)
  4. Vercel이 push를 감지해 자동으로 새 배포 생성
- **주의**: Publish는 **해당 프로젝트만** 반영합니다 (다른 프로젝트는 건드리지 않음). 아직 `REST_OF_PROJECTS`로 주석 처리된 나머지 7개 프로젝트를 builder에서 채워서 Publish하면, 그 프로젝트가 `PROJECTS` 배열에 새로 추가되어 라이브 사이트에 나타납니다.
- **로컬 서버 필요**: 기존과 마찬가지로 `python3 server.py`가 켜져 있어야 PUBLISH 버튼이 동작합니다. 포트(8420)가 이미 사용 중이면(이전 세션에서 백그라운드로 켜둔 채 남아있는 경우) 새로 띄우기 전에 기존 프로세스를 종료해야 합니다 (`lsof -ti :8420 | xargs kill`).

## 최근 변경 — index.html 인트로 화면 (2026-07-26)

기존에 이름/소개글이 있던 첫 화면(`#intro`)을 5개 언어 인사말 마퀴 화면으로 교체했습니다.
- `#intro`: 화면 전체(100svh)를 5등분한 색띠(`.lang-row`), 각 줄에 "안녕하세요! jjaejae의 사이트에 오신 것을 환영합니다." 문구를 한국어/영어/스페인어/프랑스어/일본어로 표시. 텍스트를 10회 반복 후 통째로 한 번 더 복제(`reps+reps`)해서 `translateX(-50%)` 애니메이션으로 무한 루프 시 이음새가 안 보이게 처리(`GREETINGS` 배열, `renderGreetings()` in index.html 스크립트 상단).
- 줄마다 `reverse` 플래그로 애니메이션 방향(좌→우 / 우→좌)을 교차시킴, 속도(`duration`)도 줄마다 다르게 지정.
- 기존 이름/소개글/링크(Jaehoon Choi / Art Director / bio / Instagram·Email 링크)는 `#info`라는 새 섹션으로 이동. 헤더의 "Info" 버튼(`data-go="info"`)이 이 섹션으로 스크롤하도록 JS 라우팅 수정. 브랜드 로고 클릭(`data-go="home"`)은 그대로 맨 위(`#intro`, 마퀴 화면)로 이동.
- 색상: 초록(#4CAF50) / 다크브라운(#2b1710) / 연두(#e3e07a) / 코랄(#ee6f63) / 블루(#3f6fd1) — 사용자가 준 레퍼런스 이미지(`Ref/Main-1.png`) 색감 참고.
- TODO: 실제 브라우저에서 각 줄 반복 횟수(현재 10회 복제)가 초광폭 모니터에서도 끊김 없이 충분한지, 문구 톤(느낌표 등) 확정 필요.

## 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 실제 공개용 포트폴리오 사이트 (정적 단일 HTML) |
| `builder.html` | 프로젝트를 만들고 편집하는 로컬 도구 (3단 패널 에디터) |
| `server.py` | `builder.html`의 GENERATE / KR↔EN 번역 버튼이 사용하는 로컬 도우미 서버 |

세 파일 모두 이 폴더(`2026 PF`)에 있고, 프로젝트 이미지 폴더(`SSF SHOP`, `NIKE 2019` 등)도 같은 위치에 있어야 상대경로가 맞습니다.

---

## 1. index.html — 실제 사이트

**구조**: 인트로(이름/소개) → 스크롤 → Work 리스트(카드) → 클릭 시 프로젝트 상세 뷰
- 프로젝트 메인: 배경 전체를 채우는 이미지/영상 + 좌상단에 라운드 컬러 박스(프로젝트명) + 우하단에 같은 색의 라운드 박스(YouTube/Vimeo/Instagram 아이콘)
- 프로젝트 상세: 사진 아래 캡션(라벨 좌측/번호 우측) + 본문. 사진은 원본 비율 그대로, 높이 고정 상태로 클릭하면 좌우로 롤링
- 하단에 크레딧 그리드

**현재 들어있는 프로젝트**: SSF SHOP 1개만 활성화되어 있습니다 (`PROJECTS` 배열). 나머지 7개(Galaxy Tab S9, Neo QLED 8K, NIKE 2019, YUNJAC, 아이스크림 홈런, 띵크어띵, HNY 2022)는 스크립트 안에 `REST_OF_PROJECTS`로 주석 처리되어 보류 중입니다 — SSF SHOP 포맷이 최종 확정되면 같은 형식으로 활성화하면 됩니다.

**SSF SHOP 프로젝트 세부**
- 컬러 `#C4402A`, 히어로는 비메오 영상(`615843658`) 자동재생
- 블록 3개: Brand Film / Portfolio Film / Digital Film — 실제 캠페인 개요·슬로건 텍스트 포함
- 실제 크레딧 전체 목록 포함 (Client, Agency, CD, AD 등)

**프로젝트 데이터 스키마** (한 항목 기준):
```js
{
  id, title, color, textColor, titleLetterSpacing, titleLineHeight,
  titleFontName, titleFontDataUrl,      // 선택: 커스텀 타이틀 폰트
  boxWidthScale, boxHeightScale,        // 선택: 컬러박스 크기 배율 (기본 1)
  meta, hero, heroType, heroVideoId,
  social:{youtube, vimeo, instagram},
  tag, year,
  blocks:[{label, heading, images:[...], paragraphs:[...]}],
  credits:[[label, value], ...] | null
}
```

---

## 2. builder.html — 제작 도구

브라우저에서 `file:///.../builder.html`로 직접 엽니다. 데이터는 IndexedDB(`pf-builder-db`)에 자동 저장됩니다.

**화면 구성**
- 좌측: 프로젝트 목록 (클릭해서 전환, `+ 새 프로젝트`로 생성, 항목 우측 ✕로 삭제)
- 가운데: 편집 패널
  - 상단 버튼 4개 — **GENERATE / SAVE / KR → EN / EN → KR** (아래 3번 참고)
  - 프로젝트명 / 컬러박스 색상·텍스트색 / 카테고리·연도 / 클라이언트·에이전시
  - **타이틀 타이포그래피** (기본 접힘, 클릭해서 펼침): 자간·행간 슬라이더, 타이틀 폰트 첨부(버튼 클릭 또는 미리보기 박스에 파일 드래그&드롭)
  - **컬러 박스 크기**: 가로/세로 슬라이더 (모바일에서는 이 값과 무관하게 자동으로 작아짐)
  - 메인 히어로(이미지/비메오 영상 자동재생 토글)
  - 소셜 링크
  - 블록(사진 세트): 블록/사진 모두 드래그로 순서 변경, 폴더째 불러오기 지원
  - 크레딧: 라벨/값 반복 입력
- 우측: 실시간 미리보기 (PC / Mobile 토글, 실제 사이트와 동일한 CSS 재사용)
  - 코드 내보내기 버튼 → `PROJECTS` 배열에 붙여넣을 수 있는 JS 객체 텍스트 생성 (클립보드 복사 가능)

**디자인 / 테마**
- 좌하단 원형 버튼으로 빌더 자체 UI 컬러 테마를 순환 전환: **화이트(블랙 텍스트) → 블루 → 다크 → 화이트…**
  - 원의 색은 "지금 상태"가 아니라 **클릭하면 바뀔 다음 테마의 색**을 미리 보여줌 (화이트 원 → 누르면 화이트 화면, 블랙 원 → 누르면 다크 화면)
  - 선택한 테마는 `localStorage`에 저장되어 다음에 열어도 유지됨
- 카드형 요소(사이드바 프로젝트 항목, 블록 카드, 사진 목록 행, 폰트 미리보기 박스, 모달)는 **각진 모서리 + 블러 없는 오프셋 그림자**(6px 6px, 블랙)로 통일 — 3개 테마 모두 같은 그림자 로직, 색상만 테마 변수(`--line-strong`, `--card-shadow`)를 따라감

**기타**
- **Ctrl/Cmd+Z** 실행취소, **Shift+Ctrl/Cmd+Z** 다시실행 (텍스트 입력 중엔 브라우저 기본 undo 우선)
- 이미지는 폴더 선택(`webkitdirectory`) 시 실제 파일의 상대경로를 그대로 기억해서, 내보낸 코드가 index.html에서 바로 동작하도록 설계됨

---

## 3. server.py — GENERATE / 번역 버튼용 로컬 서버

API 키 없이, 이미 로그인된 `claude` CLI를 그대로 호출하는 방식입니다.

**실행 방법**
```bash
cd "/Users/cheil/Desktop/jjaejae/Personal/2026 PF"
python3 server.py
```
`http://localhost:8420`에서 대기하며, 이 창을 켜둔 상태에서 builder.html의 GENERATE/KR→EN/EN→KR 버튼을 눌러야 동작합니다. 끌 때는 터미널에서 `Ctrl+C`.
지금 세션에서는 이미 백그라운드로 켜둔 상태입니다 — 컴퓨터를 재시작했거나 서버가 꺼져 있으면 위 명령을 다시 실행하면 됩니다.

**엔드포인트**
- `POST /generate` — 현재 제목/카테고리/메타 정보를 참고해 `claude -p`(haiku 모델, 호출당 $0.50 상한)로 제목·카테고리·클라이언트 라인·본문 문단을 생성. 프로젝트명과 같은 이름의 폴더가 있으면 그 안 이미지도 자동으로 찾아서 함께 반환.
- `POST /translate` — 현재 프로젝트의 제목/카테고리/메타/모든 블록의 헤드라인·본문을 한→영 또는 영→한으로 번역 (크레딧의 인명·브랜드명은 대상에서 제외).
- `GET /scan-images?folder=이름` — 해당 폴더의 이미지 목록(자연정렬)을 반환.

**참고**: 호출마다 소액의 실제 API 비용이 발생합니다(haiku 모델 기준, 1회 호출에 대략 $0.02~0.2 수준, 번역은 내용량에 따라 변동). 이미지가 매우 많은 폴더를 재귀적으로 스캔하므로, 하위 폴더가 여러 개면 예상보다 많은 이미지가 딸려올 수 있습니다 — 필요하면 빌더에서 수동으로 순서를 정리하거나 불필요한 사진을 삭제하면 됩니다.

---

## 알려진 제약 / 참고 사항

- **비메오 임베드**: 로컬 `file://` 환경에서 비메오 영상이 "특정 도메인에서만 embed 허용"으로 설정되어 있으면 재생이 막힐 수 있습니다 → 비메오 프라이버시 설정을 Anywhere로 변경 필요.
- **IndexedDB는 file:// 오리진 전체에서 공유**됩니다. 즉 이 Mac에서 file://로 여는 모든 로컬 HTML이 같은 저장소를 씁니다(builder.html 전용 스토어 이름을 쓰긴 하지만, 다른 폴더에서 연 사본이어도 같은 데이터를 보게 됨) — 테스트용 사본을 함부로 열지 않도록 주의.
- 폰트/이미지 파일은 base64 또는 상대경로로 다뤄지며, 폰트 파일이 크면 내보내기 코드 텍스트도 그만큼 길어집니다.
- GENERATE/번역 버튼은 `server.py`가 켜져 있어야 동작하며, 꺼져 있으면 안내 메시지와 함께 실행 방법을 alert로 알려줍니다.

## 다음에 이어서 할 만한 것

- [ ] 나머지 7개 프로젝트를 SSF SHOP과 같은 포맷으로 builder에서 하나씩 정리 → index.html에 반영
- [ ] Work 리스트 카드 디자인 최종 점검 (현재 SSF SHOP 1개만 있어 레이아웃 검증이 제한적)
- [ ] 필요시 Generate 프롬프트를 실제 클라이언트/캠페인 사실 기반으로 다듬기 (현재는 초안/플레이스홀더 톤 유지하도록 설계됨)
- [ ] builder.html의 3테마 카드 디자인(각진 모서리 + 오프셋 그림자)을 index.html 쪽에도 적용할지 여부 확인
- [x] ~~"Play"(개인 작업물) 섹션 데이터 연동~~ — `category` 플래그를 기존 `PROJECTS`에 추가하는 방식으로 완료 (2026-07-26, 위 "Work/Play 데이터 연동" 항목 참고). 이제 builder에서 Play 탭으로 프로젝트를 만들고 채운 뒤 Publish하면 됨.
- [ ] "Play" 섹션에 실제 콘텐츠 채우기 (아직 프로젝트 0개 — Coming soon 문구만 있음)
